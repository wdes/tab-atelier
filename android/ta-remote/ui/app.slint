export component AppWindow inherits Window {
    title: "Tab Atelier Remote";
    background: #1e1e1e;

    VerticalLayout {
        alignment: center;
        spacing: 16px;

        Text {
            text: "Tab Atelier Remote";
            color: #e0e0e0;
            font-size: 22px;
            horizontal-alignment: center;
        }

        Text {
            text: "v0.1.0 — connection coming soon";
            color: #888;
            font-size: 14px;
            horizontal-alignment: center;
        }
    }
}
